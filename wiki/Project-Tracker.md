# Project Tracker

Last reviewed: 2026-09-16.

This is the only human-maintained XAIOS project tracker. Roadmaps, milestones,
phase plans, open decisions and risks are consolidated here, and the Wiki keeps
no other planning page. It records what each item's state is and what would
change it -- not how the work was done. That reasoning belongs in the commit
that did it and in the comments beside the code.

**A numbering collision, and what was done about it.** Item numbers are assigned
on whichever branch the work happens on, and two branches allocated `B-61`
upward at the same time without seeing each other: the RISC-V and CI work on
`main` took `B-61` to `B-73`, and the WebTransport C99 port on the
`webtransport` branch took `B-61` to `B-70` for entirely different things. The
rule on this page is that a number is never reused and never renumbered, and
that rule cannot hold for both sides, so the published branch wins: `main`'s
`B-61` to `B-73` stand, and the port's rows are renumbered to `B-74` to `B-83`
in the order they were first assigned. The port's commits were already pushed
when this was found and still cite the old numbers, which is why this paragraph
exists rather than a silent edit; the numbers in this table are the authority.
The port's branch has since been merged and deleted, so those commits are
reachable from `main` and nowhere else. `B-74` is a hole in that range and stays
one: it was cited twice -- by `B-76` and by `userspace/apps/netsocktest.c` -- as
the listener-registry ceiling the ephemeral-port syscall creates, and the row
that describes that ceiling is `B-78`. Both citations said the number that had
been assigned to nothing, which a reader cannot follow, so they now say the one
that can be read. Anything assigned from now on starts at `B-84`.

## Where the tree stands

The ten aggregate targets -- `qemu-core-os-rc`, `qemu-full-os-rc`,
`qemu-developer-ux`, `qemu-operations-closure`, `qemu-network-adversarial-gate`,
`qemu-readiness-gate`, `qemu-post51-gate`, `qemu-qualification-readiness`,
`qemu-100-gate` and `qemu-release` -- were re-run serially on 2026-09-12. Eight
passed. `qemu-readiness-gate` and `qemu-full-os-rc` both failed on the same leg,
`qemu-fault-matrix`, and that failure was this machine's accumulated durable
volume rather than the guest: the three controlled-fault scenarios never ran,
because the boot died first in an unrelated self-test. It is `B-51`, it is
fixed, and `qemu-fault-matrix` passes in isolation on the same commit that
failed. **Both have since been asked again, serially, on a second Mac at
`0fba4de`, and both pass:** `qemu-readiness-gate` reports `matrix_exit_code=0`
with an empty `failures` list, and `qemu-full-os-rc` reports `status=pass`,
`milestone=42`, `qemu_full_os_complete=true`, `release_candidate=qemu-rc-1` and
thirteen validated subsystems. That answers the question the paragraph above
left open and answers nothing wider: the other eight aggregates were not re-run
in the same sitting, and these two are both QEMU gates, so the two that include
Fusion and VZ are exactly as current as they were.

They are run serially deliberately: these assert on boot markers and timings,
and this machine cut a `qemu-smoke` boot short at load average 30 and passed it
at load 9 with no change in between. The consolidated report deliberately
retains `physical_qualification=false`.

What *is* current, against `7df4fc8`, the commit build 6 was cut from:
`make release-check` passes, which requires `docs-check`, a
`check-release-package` run against the published note, and a `local-gates`
record naming that exact commit. It now also requires that CI passed on that
commit: it did not when build 6 was cut, and it could not, because the check
did not exist and the sentence it printed was about this Mac's four
environments only. The runner is a fifth and it is the one that finds what none
of the four can -- every defect in the nine red days was invisible here and
immediate there -- so `check-ci-status` asks, and says INCONCLUSIVE rather than
passing when it cannot get an answer. That record covers six gates including both
hypervisors -- `vmware-fusion-smoke`, `vmware-fusion-framebuffer-gate`,
`vz-gate`, `hypervisor-memory-matrix`, `release-image-gate` and
`vz-stress-gate`. `boot-media-gate` (72 checks) and `vm-package-gate` (five
kits) also pass on the released files.

That is emulated evidence and nothing more. **No result on this page is
physical-hardware evidence, and no released build has been booted on physical
hardware.**

### What is open

Four open rows. `B-121`, the operations closure waiting for a third durable lifecycle record, turned out to be a reboot cancelled by a virtio-blk flush that never completed -- the storage-administration window had seized the machine's own disk -- and is closed; `B-122`, the x86-64 time base having been a 1 GHz guess, is closed too, the runner's TSC being 2.4456 GHz, which is exactly why a thirty-second budget had expired after 12.3 seconds. Closing those exposed `B-123` -- the closure's x86-64 leg had never run, and the first time it did the guest panicked on a TLB shootdown that cpu 1 never acknowledged -- and `B-123` is now closed as well: cpu 1 was spinning on the network guard with interrupts masked, and cpu 0 was waiting for its acknowledgement while holding that guard. `B-100`, the NVMe self-test failing when the guest is starved, is closed too: it was a completion the driver read while the device was still writing it, and the compiler's own codegen is what named it. And `B-120`, the wakeup the runner lost between an idle CPU's check for work and its halt, is closed as well: the fix is on all three architectures, its deterministic self-test is required by name, and CI's `x86_64 Bring-up` for `9271e1d` passed all three of the smoke steps that used to fail -- the shared one, the single-CPU one and the Intel high-MMIO one. What remains is not an OS defect on this machine: `B-43`, `B-63` and `B-99` are a VMware Fusion environment on one host, where no evidence points at XAIOS, and `B-79` is a feature that was deferred with its tasks written down. `B-119`, the console lock dropping a line a gate was asserting on, was found the same way and is closed by waiting for the lock in the context that can afford it. The
count was six while eleven were closed, because nine days of red turned out to be
five separate causes and working through them found more; the RISC-V and CI work
on `main` then raised and closed sixteen, and the WebTransport C99 port added
four of its own, of which three are closed. What is left is `B-43`, `B-63`,
`B-79` and `B-99` -- and the last two of the rows this page closed in this
stretch were each reopened once by their own acceptance runs:
`B-122` fixed the mistaken clock behind its timeout, and the run after it
reproduced the timeout itself, with the budget the kernel asked for and a thread
on an idle secondary CPU that never ran. Every row is `TESTING` or `OPEN` in the table below, which
here means the fix or the measurement is still owed rather than that the code is
untried.

| | What it is | Why it is still open |
|---|---|---|
| `B-43` | A VMware Fusion session stalls about 19 s and says nothing | The mechanism this row originally named was wrong and is corrected in place. The stall is real; what causes it is not established. `B-63` is a different signature and not this. |
| `B-63` | A session is accepted, receives nothing, and both ends time out separately | Reproduced on the runner three times with the host-side capture; the mechanism is established and the row says which half of it the flow line answers. |
| `B-79` | The TLS 1.3 client handshake is written and has no 0-RTT | The handshake completes against an independently generated QUIC server flight. What is absent is 0-RTT and the session tickets it needs, a HelloRetryRequest (refused by name), and client certificates. The rest of the port, and what each remaining task's exit criterion is, is in [Current tasks](#current-tasks) below. |
| `B-99` | The Fusion load soak fails on node2 about once a minute, and no evidence points at XAIOS | The client's TCP connect times out in 7.83s and the guest's console shows no trace of that connection, while a probe straight afterwards connects in 0.07s. The failures recur about once a minute in ~39s bursts that two different client Macs hit in the same windows, so the client stacks are not the cause. A QEMU guest on the userspace network took 4000 rounds without one failure and logs 2 TCP retransmits where a comparable Fusion run logs 2887, while under Fusion the same failures appear over e1000e and vmxnet3 and over both bridged and NAT vmnet -- so the guest's stack, its sshd, its NIC driver, both clients and the physical LAN are each held out, and Fusion's virtual network on node2 is what is left. The mechanism inside vmnet is unidentified, which is why the row is open; per-port exhaustion, flow-table exhaustion, the half-open bound and the B-43/B-63 service-loop stall are all refuted from the console. |

Two things are open that are not defects:

- **No physical hardware, on any architecture.** Every result on this page comes
  from an emulator or a hypervisor. `D1` is the USB route and needs a stick in a
  machine; RISC-V has never run on anything but QEMU's `virt` board; and
  `serve-netboot.sh` has never served a real machine on any architecture.
- **Two external decisions block their own work** and nothing here moves them:
  `OD-004` (production trust-root custody) and `OD-008` (pinning an official
  DeepSeek source, whose exact release label is unresolved).

One step timed out once and did not reproduce; it is `B-39` rather than a
footnote here.

## Current tasks

**The WebTransport C99 port is paused, with its TLS 1.3 client handshake written,
tested and merged on `main`.** It completes a QUIC handshake against a server
flight built by an independent Python implementation, and 1487 host checks pin it
against RFC 8446, 8448, 9001, 7301 and 7748 in CI and under AddressSanitizer and
UndefinedBehaviorSanitizer. The tasks below are what is left of the port, in the
order they unblock each other. They are numbered from `B-84` because that is
where the tracker's sequence had reached; they are work not yet done rather than
defects, and the numbering rule on this page does not care which it is.

| # | Task | Status | Exit criterion |
|---|---|---|---|
| `B-84` | Nothing on the target drives the handshake from a socket | `NOT STARTED` | A guest opens a datagram socket, sends a real QUIC Initial, receives a ServerHello and completes a handshake. The module is exercised only from the host today, and the only on-target evidence for the syscall under it is `netsocktest` and `make qemu-datagram-ephemeral-port-gate`. **Exit gate:** `make qemu-quic-handshake-gate` boots a guest that completes a handshake and says so on the console. |
| `B-85` | QUIC transport parameters are carried as opaque bytes | `NOT STARTED` | The client's own parameters are encoded and the server's are parsed and checked against RFC 9000's rules. Today `userspace/wt/` requires only that the extension is present, so no flow-control limit, idle timeout or connection ID the peer set is actually known -- and an unchecked parameter is one a peer can make unreasonable. |
| `B-86` | There is no QUIC connection runtime | `NOT STARTED` | Packet number spaces, CRYPTO stream reassembly, ACK and loss recovery, connection IDs, Retry and Version Negotiation. The TLS module is handed messages that have already been reassembled and in order; nothing does the reassembling, so nothing yet turns the handshake into a connection. |
| `B-87` | 0-RTT is not implemented, and neither is what it needs | `NOT STARTED` | Early data: the `c e traffic` and binder branches of the key schedule, a session-ticket store for the PSK they come from, and an early-data encryption level. The level enum deliberately has no member for it, because a member that is always unavailable is worse than an absence. |
| `B-88` | A HelloRetryRequest is refused by name rather than handled | `NOT STARTED` | A server that wants a different group can be talked to. This means rewriting the transcript with the synthetic `message_hash` (RFC 8446 section 4.4.1), sending a second ClientHello and refusing a second retry. Refusing by name is honest and is a server this client cannot reach. |
| `B-89` | A CertificateRequest is answered with an empty Certificate and no CertificateVerify | `NOT STARTED` | Client certificates, if XAIOS ever has a use for them. RFC 8446 section 4.4.2 requires the empty answer a client with no certificate sends, which is what is implemented and is conformant. |
| `B-90` | No P-384 or P-521 certificate has been through the verifier | `FIXED` | The generated ECDSA fixture was P-256 only, so two of the verifier's three ECDSA branches had never run: no secp384r1 and no secp521r1 multiplication had ever been performed by `br_ecdsa_i31_vrfy_asn1` in this tree. The curve check that refuses a mismatched scheme was tested, and it is not a substitute for the branch it protects -- a dispatcher that refused everything on the longer curves would have passed every test in the file. `generate_wt_ecdsa_vectors.py` now emits all three curves from RFC 6979's published test keys (A.2.5, A.2.6 and A.2.7), with the shared 130-byte signed content emitted once because only the digest differs; P-521's key is 131 hex digits rather than 132, since ceiling(521/4) is 131 and the value is quoted exactly as the RFC wraps it. `test_wt_tls_cert.c` gained `test_ecdsa_long_curves`: for each of P-384 and P-521 it checks the parsed curve, the 97- and 133-byte uncompressed points, that the real signature verifies, that every single-byte mangling of it is refused, and that another curve's scheme is refused against it. The host suites went from 1214 checks to **1472** at that point. |
| `B-91` | The module has never been built or booted for RISC-V | `IN PROGRESS` | **The premise originally written into this row was wrong and is corrected in place.** There is no missing RISC-V cross-compiler: `compile-check` has always compiled `kernel/arch/riscv64/` with `clang --target=riscv64-unknown-elf`, and clang and lld target rv64. What was missing was the **hosted RISC-V libc sysroot**, because `scripts/build-libc.sh` defaulted `XAIOS_LIBC_ARCHES` to `aarch64 x86_64` while every kernel leg of `compile-check` covered three architectures -- so the tree built the hosted library for two of the three machines it ships on, and the userspace legs had no riscv64 sysroot to point at. **Half of this is now closed, and the two halves had to move together:** the libc default names all three architectures, and `compile-check` gained a riscv64 userspace leg beside the aarch64 and x86_64 ones. All eight `userspace/wt/src/*.c` now compile to RISC-V objects on every run -- `ELF 64-bit LSB relocatable, UCB RISC-V, RVC, double-float ABI`, 56 objects in the leg -- so a regression in this module for RISC-V is a build failure rather than a discovery. **Still open:** it has never *run* there. **To close:** a guest that completes a handshake, which is `B-84`. |
| `B-92` | Two views outlived the buffers they point into, by contract rather than by construction | `FIXED` | `wt_tls_client_receive` borrowed the Certificate message until CertificateVerify arrived, and `wt_tls_client_peer_transport_parameters` returned a view into the EncryptedExtensions buffer: both were documented, and a caller that reused a per-read scratch buffer got a use-after-scope. **The client now owns both, and each is bounded rather than copied blindly.** The leaf certificate is not kept at all -- `wt_tls_certificate_public_key` takes the key out of it while the Certificate message is still in hand, and that key is bounded by `WT_TLS_PUBLIC_KEY_MAX`, which is BearSSL's own decoder bound and already enforced. The verifier was split so the check runs over key components (`wt_tls_certificate_verify_signature_with_key`), with the certificate-decoding entry point kept as a wrapper over it, so no existing caller changed. The transport parameters are copied into the client, bounded by a new `WT_TLS_CLIENT_MAX_TRANSPORT_PARAMETERS`, and a peer whose parameters do not fit is refused rather than truncated, because truncating them would silently drop flow-control limits. This is the rule the module already applied to ALPN, whose comment says copying it means it does not point into a receive buffer the caller will reuse. **The check that proves it destroys the buffer:** `test_client_owns_what_it_keeps` delivers every message of the flight out of one scratch buffer, `memset`s that buffer to a byte the flight cannot contain the moment each call returns, and then reads the transport parameters back and verifies the CertificateVerify against the earlier Certificate. It asserts the buffer really was destroyed, so it cannot pass for the reason the old check did, and it fails when the copy is removed: `want 0a0b0c0d0e0f, got 000000000000`. The host suites go from 1472 checks to **1479**. **The change also left stale claims behind, and reading the module for the class rather than the incident found them:** the paragraph on `wt_tls_client_receive` still said the buffer was borrowed for the handshake's lifetime and that a caller with a per-read scratch buffer *must* copy, the transport-parameters getter's own contract still said its bytes point into the EncryptedExtensions message, and `wt_tls_client_clear` still listed `the borrowed views` among what it zeroes. All three were corrected, and the correction is recorded in place rather than deleted, because a header that tells a caller to keep a buffer alive for a reason that no longer exists is worse than one that says nothing. Claims about data that *is* still borrowed -- the configuration pointers, the ServerHello key share, the CertificateVerify signature -- were left alone and still say so. |
| `B-93` | The client's flight was valid only until the next call | `FIXED` | QUIC retransmits a lost Finished with the same keys, so the bytes have to outlive the call that produced them, and `wt_tls_client_receive` returned a pointer with no way to ask for it again. **The bytes always did outlive it -- they are the client's own storage -- but nothing in the interface said so, so a QUIC layer could only copy them and hope.** `wt_tls_client_flight` now returns the retained flight with its length and level, and the span is stated rather than implied: until the client is cleared or the handshake fails, which is the secrets' span, because a flight whose keys are gone cannot be retransmitted usefully. **The fix used a field that already existed and had never been written:** `flight_level` was declared in the struct and assigned nowhere, which is exactly the value an accessor needs, so the flight now records its own level instead of the producing function returning a literal. `test_flight_outlives_the_call` checks that the accessor agrees with the call that produced the bytes -- same bytes, same length, same level -- after that call has returned, that a started handshake has none, and that clearing ends the span. It fails when the retained level is removed: `at the level the call reported: want 1, got 0`. The host suites go from 1479 checks to **1487**. |

The corresponding rows in the WebTransport repository's own tracker are `WT-44`
to `WT-53`, in the same order, and that page is where the protocol-level work
that is not XAIOS-specific is recorded.

### Firmware profile results

Three profiles carry firmware evidence. **Two of them are current at
`58bd1fe`, re-collected by the targets that produce them from a clean tree:**
the macOS QEMU ARM64 profile on `node4` and the macOS VMware Fusion ARM64 one
on `node2`, each passing every gate it defines. **The third is still behind the
tree and must be re-run before it is quoted:** shared code that every profile
compiles has changed since it was collected -- the scheduler and page
allocator, the network stack, the cluster engine, and the RISC-V memory and
interrupt path -- and it needs its designated host rather than one of these.

One thing about collecting them is worth knowing before the next attempt,
because it cost a run here: the collector does not discover the firmware. It
reports `set XAIOS_AAVMF_CODE to the qualified firmware file` and refuses, even
when the file it wants is the very one the QEMU runner would have defaulted to,
so the variable has to be set explicitly and the check is on the file's
identity rather than its location.

| Profile | Collected at | Firmware | Covers |
|---|---|---|---|
| macOS QEMU ARM64 | `58bd1fe` | AAVMF/EDK2 `47765fe344818cbc464b1c14ae658fb4b854f5c2ceffa982411731eb4865594d` | boot, CPU, network, SSH, USB keyboard console, SVE2 per-task context, storage recovery, operations, shutdown, repeat boot |
| macOS VMware Fusion ARM64 | `58bd1fe` | Fusion 26.0.0, chainloader `b7fb993edf80e301b148a2076f8a9919c3d31936d2273f592d19c06b5ec1d3a5` | four-vCPU boot, storage, network, SSH lifecycle |
| Intel VPS QEMU x86_64 | `ee9c621edde5315e0da37fb3ae328baf717318ee` | OVMF/EDK2 `624e06de18b4fa535e90db7160d00d3d07d206422b89999bf1e27d920264e4e0`, QEMU 10.0.11 under TCG | all eight profile gates: boot/network/SSH, USB keyboard console, CPU matrix, platform inventory, NUMA firmware, NVMe storage, operations/shutdown, repeat boot |

The Intel profile needs its designated host (`deltasona`, Linux x86_64); no ARM
result stands in for it. Collecting it found nothing wrong with XAIOS and one
thing wrong with a gate: `q35-high-core-256-x2apic` needs about 517 seconds to
boot 256 vCPUs under TCG against a hardcoded 480, so the matrix killed a machine
that was booting correctly. Budgets now scale by
`XAIOS_QEMU_MATRIX_TIMEOUT_SCALE`; unset, nothing changes, and nothing the
scenario asserts was relaxed.

## Physical qualification: deferred, and said so

**This is a position rather than a queue, and it is written down here because
the objective it belongs to is a single-node, self-hosting, SSH-administered OS
with physical qualification explicitly deferred rather than silently assumed.**
There is no physical hardware in this project on any architecture, no physical
result is being waited on to complete any current work, and the three targets
that would need one are `OD-001`, `OD-002` and `OD-003` -- all `NOT STARTED`,
all blocked on nothing but the choice of machine.

What follows from it, and what does not:

- **Nothing is promoted for being the only evidence available.** A QEMU result
  stays *correctness and ABI only*, an Apple Virtualization.framework result
  stays *not qualification evidence*, and the rows that only hardware can settle
  keep `NEEDS HARDWARE` and stay off the active list.
- **No physical claim is made anywhere.** No physical Apple, ARM-server,
  Intel-desktop or Xeon performance, firmware compatibility, NIC behaviour, NVMe
  durability, NUMA locality, thermal behaviour or production security is
  asserted by anything in this tree. x86-64 in particular has never executed on
  an Intel or AMD processor: its evidence is QEMU on an ARM host through an
  interpreter. RISC-V's evidence is one emulated board and nothing else.
- **The deferral does not weaken an emulated gate.** They are held to the
  standard they were written to, and `physical_qualification=false` stays in the
  reports that carry it.
- **It closes only one way:** delivery order 1b -- a named machine per target
  passing the firmware, device, durability, security, ISA-state, NUMA, soak and
  benchmark contracts. `HARDWARE-READINESS.md` states the boundary and the
  contracts; this page owns the status.

## Released builds

Builds are published on the
[releases page](https://github.com/Pummelchen/XAIOS/releases); each carries a
note recording the hypervisors and firmware it was booted on and what was not
tested. What changed between them is in
[`CHANGELOG.md`](https://github.com/Pummelchen/XAIOS/blob/main/CHANGELOG.md);
this page does not repeat it.

| Build | State | Note |
|---|---|---|
| `b6` | **current** | One image per architecture instead of one image for all three, and eleven kits built for one machine each. RISC-V gets a launcher, a USB kit and a network-boot binary for the first time. Cut with `make release-check` green against `7df4fc8`. |
| `b5` | superseded | A RISC-V kernel in the shipped image, `xtop`, the screen framework, and a machine that is idle when nothing is happening. The last build to ship a single unified image. |
| `b4` | superseded | Cut from a green CI run. Adds first-boot setup, and an account and machine name a person chooses. |
| `b3` | superseded — do not use | A machine configured with SSH keys and no password account refused every key login, and it was cut while CI was red. |
| `b2` | superseded | First build to ship USB and network-boot kits. |
| `b1` | superseded | First released build. |

No released build has been booted on physical hardware.

## Status codes

| Code | Meaning |
|---|---|
| `TESTING` | Implemented, but the current acceptance run or physical qualification is still underway. |
| `IN PROGRESS` | Active implementation is incomplete. |
| `NOT STARTED` | No qualifying implementation has begun. An interface or fixture alone does not count. |
| `BLOCKED` | Work cannot proceed until the stated external decision or dependency is resolved. |
| `FAILED` | The latest required acceptance gate failed; the failure evidence must be linked in the item. |
| `DEFERRED` | Postponed on purpose. Not blocked by anything external and not abandoned -- it is simply not being worked on now, and saying so is more honest than leaving it among the open items. |
| `NEEDS HARDWARE` | Implemented as far as it can be here, and the remaining evidence can only come from physical machines. Not on the active work list: no amount of effort in this environment moves it, and leaving it among the open items makes the list read as further behind than it is. |

Resolved items carry no status code. They move to a **Resolved, kept for
reference** table in their own section, which records what the item was and
what closed it. They are not deleted, because their identifiers are cited
from the code, the gates and the release notes.

QEMU status proves correctness and ABI behavior only. Physical support and
performance require immutable evidence under the
[benchmark contract](https://github.com/Pummelchen/XAIOS/blob/main/docs/BENCHMARK-CONTRACT.md).

## Supported environments

XAIOS runs on three hypervisors. QEMU covers three architectures, so five
targets exist, but the platform contract is per hypervisor.

Firmware differences below are the hypervisor's, not XAIOS's: the system
behaves identically wherever a capability exists, and where one is absent it
degrades the same way everywhere. See
[Platform neutrality](https://github.com/Pummelchen/XAIOS/blob/main/docs/PLATFORM-NEUTRALITY.md),
which the build enforces.

| Function | QEMU ARM64 | QEMU RISC-V64 | QEMU x86_64 | VMware Fusion ARM64 | Virtualization.framework |
|---|---|---|---|---|---|
| Boots to a login | yes | yes | yes | yes | yes |
| Durable xaibootFS volume | yes | yes | yes | yes | yes |
| IPv4 by DHCP | yes | yes | yes | yes | yes |
| IPv6 by SLAAC | yes (`qemu-slaac-gate`) | yes (`qemu-slaac-gate`) | yes (`qemu-slaac-gate`) | yes, global, from a real router advertisement (`make vmware-fusion-network-gate`) | yes, unique-local only (`V-03`) |
| IPv6 by DHCPv6 | client present, gated | client present, gated | client present | client present | client present |
| SSH server | yes | yes | yes | yes | yes |
| SSH client, SFTP | yes | yes | yes | yes | yes |
| Reachable from the host | yes | yes | yes | yes | vmnet helper only, one direction at a time |
| Boots at 1, 2 and 4 GiB | yes (`qemu-memory-matrix`) | yes (`qemu-memory-matrix`) | yes (`qemu-memory-matrix`) | yes, all three run | yes, all three run |
| Multiple vCPUs | yes, 130 gated | yes, 1/2/4/8 gated | yes, 128/256 scenarios | yes, 4/4 | yes, 8/8 |
| Message-signalled interrupts | distributor | board-dependent: none on the default `virt` (PLIC, everything polls), APLIC+IMSIC on `virt,aia=aplic-imsic` (`qemu-riscv64-aia-gate`) (`P-16`) | yes | PCI | none; every queue polls (`V-02`) |
| Framebuffer console | no, serial | yes with a virtio-GPU attached, gated; serial otherwise | no, serial | yes | yes when a display device is attached (`--gui`), driven directly over virtio-GPU because firmware publishes none; serial otherwise |
| USB keyboard input | yes | yes, gated | yes | provisioned, not gated | console input over virtio |
| Entropy protocol | virtio-rng | virtio-rng (`source=device-rng`) | virtio-rng | `F-05` none exposed | yes |
| Storage transport | virtio-MMIO | virtio-MMIO; NVMe over PCIe in its own gate | virtio-PCI, NVMe | AHCI | virtio-PCI |
| Network transport | virtio-MMIO | virtio-MMIO | virtio-PCI | E1000E qualified; VMXNET3 works (`F-02`) | virtio-PCI |
| Applications gated | yes, by name | yes, by name | yes, by name | yes, by name | yes, by name |
| Boots its release image | yes, gated | yes, gated | yes, gated | yes, gated | yes, gated |
| Automated gate | full CI | full CI (`riscv-bring-up`: smoke, release configuration, CPU tiers) | full CI | `make vmware-fusion-smoke` | `make vz-gate`, `make vz-stress-gate` |
| Release-image gate | `make release-image-gate` covers all five, each booting its own architecture's image; CI runs the three QEMU rows and reports the hypervisors as skipped |  | | | |
| Verified by | CI, every push | CI, every push | CI, every push | `make local-gates`, required by `make release-check` | `make local-gates`, required by `make release-check` |
| Evidence class | correctness only | correctness only | correctness only | Fusion 26H1 lifecycle | development target, not evidence |

Device inventory differs because the hypervisors differ; the kernel discovers
what is present rather than assuming a platform, so those rows are not defects.
The rows carrying an item identifier are.

## Open bugs

Defects with no fix in place, followed by resolved ones kept for reference.

A resolved row is not deleted, because its identifier is cited from the code,
the gate or the release note that closed it -- `B-14` appears in five release
notes and a gate docstring, `B-25` in ten files -- and a citation that resolves
to nothing is worse than a short row. The reasoning behind each fix stays in
the commit that made it and in the comments beside the code; this table records
only what the defect was and what closed it.

| ID | Defect | Affects | Status | Notes |
|---|---|---|---|---|
| B-43 | A session stalls ~19 s and the guest says nothing about it | VMware Fusion ARM64 | `TESTING` | **B-63 has now reproduced this three times and they share one signature**, so the two rows describe one phenomenon: ~18.4s against a 0.37s round, a connection accepted and never serviced, and a server that answers a fresh probe in 0.08s immediately afterwards. The guest is no longer silent about it -- the close carries `packet-read-failed` or `auth-timeout` and lands 30 to 120 seconds later, outside the window that was being searched. See B-63 for the captures. **The premise originally written into this row was wrong** -- it said the server was serving a later connection while an earlier sat untouched, but the soak's round is strictly sequential, so two sequential events were read as concurrent. With that gone there is no reason to expect a starvation bug and none is there. The 18 s is the macOS client's own patience, not a guest constant. **Three Fusion load soaks have now run against the instrumentation `B-44` added, and it did not recur:** 3911 verified round trips of 256 KiB across three boots, every file byte-identical, `service loop stalled` **zero**, `stack was not polled` **zero**, and the worst poll gap 202 ms -- two orders of magnitude below the stall this row describes. That is evidence of absence rather than a cause, and the row stays open because absence over three runs is not proof. **What the search established remains worth more than the row asked for:** `network_poll_tick` has no timer, no interrupt and no thread, so sshd is the machine's network thread and any pause in its loop is a total network outage -- see `B-44`. **To close:** a recurrence carrying one of the three console lines. |
| B-48 | Every file write committed the whole metadata region | all four | `FIXED` | `write_metadata` rebuilt the region and wrote every sector of it however little moved: **1281 sectors, 640 KiB, for a 32-byte append**, measured at the block device. Nearly all of it is the node table, and an append changes one node. That is why `B-45` took file bytes to almost nothing and per-record time did not move. A commit now writes only the sectors that differ from what the target slot already holds: **4 sectors, 2 KiB**, on the same measurement. Two shadows, because the slots alternate -- a commit targets the slot mount would *not* currently choose, so what it replaces is two commits old and a single previous buffer would compare the wrong one. A shadow is believed only after its write has flushed, and any failure marks it unknown so the next commit writes the region whole; the cost of being wrong is a metadata sector silently keeping an old value, so conservative is the only acceptable direction. Costs 2.5 MiB of static buffers. **Evidence:** `tests/storage/test_xaiboot_fs_write_cost.c` counts sectors at the block device, so the figure is what the disk sees rather than a claim, and it fails against the previous code. It is wired into `hosted-test`, which the `hosted-engine` CI job runs, so the figure is measured on every run rather than only when someone remembers the file exists. `qemu-power-loss-gate`, `qemu-crash-safety-gate` and `qemu-write-ordering-gate` all pass -- which is the bar this row asked for, because the change alters which metadata sectors reach the disk. |
| B-50 | The gate stopped reading the guest's console and then blamed the guest | repository | `FIXED` | It opened 121 connections through QEMU's host forwarding and got `{banner: 114, silent: 2, unreachable: 5}`, taking 30 to 80 seconds, while the guest reported `network: stack was not polled for ms=30839` and `sshd: service loop stalled ms=30840 phase=accept`. The row asked for a run timestamping the host's opens against the guest's poll gaps, so the order of the two would be a measurement rather than an inference. **Building that measurement found the cause before it could be taken.** Nothing read QEMU's stdout while the probe loop ran. A pipe nobody reads fills; QEMU's write to it then blocks; and a guest whose console write is blocked stops polling. The gate was causing the outage it was reporting, and the 4-second failures were the host waiting on a guest the gate had stalled. A reader thread now consumes the console throughout, which is what the timestamps required anyway. Four consecutive runs since: `{banner: 120, closed: 1}` -- the limiter fires on the 121st, named `reason=rate-limit` -- in 2.8s against 30 to 80, with **zero** poll gaps reported, every time. The ordering measurement was kept and reports `no gap reported`: it costs nothing and is what a recurrence would speak through. This is the third verdict in its own right -- the instrumentation was pointing at the guest for something the host was doing. |
| B-55 | Sixteen extents was a reachable ceiling; it is now sixty-four | all four | `FIXED` | A file may be scattered across at most `XBFS_V6_MAX_EXTENTS` runs. At 16 that was reachable: on a v5 volume -- 8192 blocks, 256 nodes, which is what every machine here boots -- the smallest hole a full volume can be cut into is 16 blocks, so a maximum-size 512-block file needs about **32** extents. Sixteen could not serve it. `B-52` made that far harder to reach by taking the longest runs first; it did not move the limit. **Raised to 64, which covers that case with headroom.** It costs 768 bytes per node and 0.75 MiB across the node table, and the metadata region grows from 2560 to 3584 sectors. That was worth weighing when every commit rewrote the whole region; since `B-48` a commit writes only the sectors that changed, so a larger table costs space rather than write amplification. **The v6 node layout changes and nothing migrates it** -- the project is early enough that breaking the format is cheaper than carrying a conversion. The v5 on-disk format is a block list and is untouched; a v5 volume written by the previous build still mounts, fscks clean and snapshots, which was checked. **Still a raised ceiling and not a removed one.** A maximum-size file on a fragmented v6 volume would need about 2048 extents, and 512 inline costs 25 MiB of buffers. Removing it properly means indirect extents: a node that points at an overflow block when it runs out of inline room. **Now tested.** It rested on arithmetic when it was written, and the reason given was sound as far as it went: a v6 volume cannot be filled through the whole-file path at all, because 2M blocks against a 512-block write buffer and 1024 nodes leaves it unfillable, and without a full volume there is always a long free run and the allocator takes it in one extent. The v5 geometry can be filled -- 8192 blocks, 256 nodes, which is the volume every machine here actually boots and the one the defect was reached on. `tests/storage/test_xaiboot_fs_extent_depth.c` fills it exactly in alternating runs of 16 and 48 blocks, deletes every 16-block file, and is left with 120 holes of 16 blocks and no run longer than 16 anywhere. A 512-block file placed into that cannot be held in fewer than 32 extents by any choice of runs, so the write succeeding *is* the depth being exercised; it then reads back, snapshots and remounts, because `commit_snapshot` walking a deep extent list is the path B-52 showed halts a machine at boot. **The control is what makes it evidence:** built against the old ceiling of 16 the same test aborts on exactly that write, and the existing fragmentation test passes at either ceiling -- it was written for the placement policy, not the limit, which is why it never covered this. |
| B-60 | `qemu-operations-closure` required `ssh reboot` to exit cleanly over the connection the reboot destroys | repository | `FIXED` | `reboot` is the one command whose success ends the connection carrying it. Whether `ssh` exits 0, exits non-zero, or hangs to its own timeout is a race with the guest's shutdown, and on a runner with no hardware virtualisation the guest loses it: CI failed with `subprocess.TimeoutExpired` on the word `reboot`, from 2026-09-02. Nothing was gained by asserting it -- the gate checks the console immediately below for the lifecycle record's third durable write and the guest's third ready marker, which says the machine *came back* rather than that it accepted the request. The call now tolerates a timeout or a non-zero exit. **Weaker evidence than the rows around it, and said so deliberately:** this cannot be reproduced here, because it passes on this Mac, which is the whole problem. The basis is the CI traceback and the redundancy argument; the gate passes locally with the change, and the next CI run is what settles it. **Settled by the runner, which is the only place it could be.** `qemu-operations-closure` printed `PASS` on CI on 2026-09-12, so the reboot call tolerating a timeout or a non-zero exit is enough and the console assertions below it still hold. Getting there took B-71 out of the way first: this gate's SSH waits were never scaled for a runner with no hardware virtualisation, so for two runs it failed before reaching the reboot at all -- the row's `subprocess.TimeoutExpired` on the word `reboot` was real, and it was not the only thing standing between this gate and a pass. |
| B-61 | Two userspace linker scripts never collected RISC-V's small-data sections | riscv64 | `FIXED` | A hosted C99 application reached userspace and then failed its first `printf`, exiting 1 after a single syscall, which `kmain.c:1427` asserts on. It cost the RISC-V CI job **every run it has ever had** -- 66 of them, never green. `userspace/libc/linker.ld` and `userspace/init/linker.ld` collected `.data .data.* .ldata .ldata.*` and `.bss .bss.* .lbss .lbss.* COMMON` and never named `.srodata`, `.sdata` or `.sbss`. Under a toolchain that emits them they were placed as orphan sections wherever the linker chose. `kernel/arch/riscv64/linker.ld` has collected them since the port landed: the kernel was right and the two userspace scripts were never given the same treatment. **Why it was invisible here:** clang 18 emits small-data sections and clang 23 does not, and this machine runs 23 while CI runs 18. The fix is a no-op locally -- the RISC-V application is byte-identical at 58,792 before and after -- which is exactly what a correct fix for a defect this machine cannot see should look like. **Evidence, in a container matching the runner (Ubuntu 24.04, clang 18.1.3):** before, `helloworldc99 exited status=1 syscalls=1`; after, `status=0 syscalls=87`, `c99-runtime-smoke exited status=0 syscalls=259`, every hosted application reporting zero orphan small-data sections, and `QEMU smoke boot reached all full userspace/resource markers`. |
| B-62 | The Fusion soak's session tally counted closes it never counted accepts for | VMware Fusion ARM64 | `FIXED` | A run reported `accepted=2515 closed=2516 outstanding=-1`, which reads as a connection closed without being accepted. The extra close is `sockfd=6`, which `net_listen protocol=17 port=24002 sockfd=6` identifies as the **UDP listening socket** -- a listener is not accepted, and any descriptor the server opens itself does the same thing, so the difference was measuring the wrong population. The tally now matches closes to the descriptors that were actually accepted, and reports `closes_of_unaccepted_descriptors` separately instead of letting them corrupt the figure. Replayed against that run's console: `accepted=2515 closed=2515 outstanding=0 closes_of_unaccepted=1`. Nothing was leaking -- `free_page_trend` is `delta_pages=0` across 1257 samples. **The failure that run was reported for is separate and stays open as `B-63`.** |
| B-63 | A session is accepted, receives nothing, and both ends time out separately | VMware Fusion ARM64 | `TESTING` | **Reproduced three times in one 7138-round soak, with the host-side capture in place.** Rounds 1263, 2567 and 2610, all `sftp exited 255`, all lasting ~18.4s against a normal round of 0.37s -- the same ~19s B-43 describes, and the row's own note that 18s is the macOS client's patience rather than a guest constant. The guest's console during each failing round shows **two `net_accept` calls and one close**, where every passing round shows accepts and closes in equal number; the round after shows three of each as the stranded connection finally resolves. **The guest did log it, 30 to 120 seconds later, which is why it looked silent:** the close lands outside the failing round's correlation window and is attributed to a later round. Traced by socket rather than by timing, the three are `packet-read-failed state=7 held_ms=30014` (sockfd 2533), `auth-timeout state=5 held_ms=120030` (sockfd 5142) and `packet-read-failed state=7 held_ms=30007` (sockfd 5229). Only two `packet-read-failed` closes exist in 14276 closes in the whole soak and both are failing rounds. **The server was healthy throughout:** a `LogLevel=DEBUG3` probe fired immediately after each failure connected and completed in 0.08s. **Leading hypothesis, not established:** the connection is accepted and its inbound bytes never reach sshd, so both ends wait -- the client until its patience, the server until whichever of its own timeouts applies to the state it is stuck in. If the stack ACKs data it then fails to deliver to the accepted socket, the client's TCP has no reason to retransmit and this is exactly what it would look like. **Ruled out by this soak, which is worth as much as the hypothesis:** the socket-to-flow map never ran out -- the one `socket-to-flow map exhausted` line in the console is its own self-test (`capacity=160 filled=160 refused=1`), so B-47's mechanism is not this; sshd's loop was not asleep, it waits at most 50ms and accepts up to four per pass; the server was not wedged, the 0.08s probe says so; and there was no drop or retransmit storm -- one `retransmit=6` line exists in the entire 33 MB console and it is early, nowhere near a failure. **What was wrong in this row before:** the 200-character stderr truncation was not hiding anything -- the client's whole message is three lines -- so that half of the capture, while correct to fix, was not what mattered. **The instrumentation for that is now in the kernel.** Both ends waiting to read means a client-to-server message was lost and the client is waiting for the answer to it, and there are only two ways that happens: the stack received the bytes and the application never got them, or nothing arrived for that flow at all. `release_tcp_flow` now prints the flow's `rx_packets`, `tx_packets`, `rx_unread` and idle time **only when** `rx_unread > 0` or `packets_rx == 0` -- the two abnormal endings -- so a healthy system says nothing and the next stalled flow says which of the two it was. Silence is measured, not assumed: with the condition removed, 121 connections produced 280 release lines and every one reported `rx_unread=0 rx_packets>=1`, so the condition suppresses all of them. The trigger itself is unexercised and cannot honestly be exercised -- 200 KB pushed and reset six times produced nothing, because sshd reads what it is sent, and the condition fires precisely when it does not. **A second soak of 7235 rounds with the instrumentation in place reproduced nothing** -- zero failures, zero flow lines -- so the instrumentation has not spoken and nothing about the cause follows from the run. It is emphatically **not** evidence the fault is gone: the only change between the two soaks was the diagnostic `klog` itself, which adds a line and touches no network path, and three events in 7138 rounds is a rate at which one quiet run of the same length is unremarkable. Recorded because a silent soak is exactly the result most likely to be misremembered later as a fix. **It fired, and not where it was being waited for.** The Debian 13 interoperability job failed on CI on 2026-09-12 with `Connection to 172.17.0.1 port 38953 timed out`, and its guest console carries the whole chain: `network: tcp flow id=53 released state=5 rx_packets=43 tx_packets=101 rx_unread=208 idle_ms=0` -- CLOSE_WAIT, 208 bytes the application never read -- sitting between `sshd: service loop stalled ms=3201 phase=connections` and `connection closed reason=packet-read-failed sockfd=55 held_ms=3608`. **The mechanism is established:** the network stack is polled only from sshd's service loop (B-44), so while that loop is in its connections phase nothing processes packets; the peer waits, gives up, and the connection is reported as a failed packet read long afterwards. Three independent measurements agree on each outage -- `longest gap between polls us=9069149` from the stack, `service loop stalled ms=9101` from sshd, and `held_ms=9333` from the connection -- against a next-largest gap of 143 ms in the same run. This is the same shape as the Fusion soak: accepted, not serviced, `packet-read-failed`, and a server that answers normally a moment later. **What is still open is what the loop was doing for nine seconds.** sshd's own accounting makes the durable audit write the obvious suspect and does not support it: 1020 writes cost 34.7s across the run, about 34 ms each, which is nowhere near nine seconds in one pass. The alternative is that the guest was not running at all -- a loaded runner descheduling QEMU would produce exactly these numbers, because every clock here is the guest's. Distinguishing them needs guest CPU time against wall time, and neither reading is assumed in the meantime. **One of the two has already lost ground, from evidence that was already in the run:** sshd reports a wait overrun whenever its 50 ms kernel wait takes materially longer, and the run with three multi-second stalls reported **zero** of them. A host descheduling QEMU at random would overrun that wait constantly, since it is the longest single call in the loop. So the connections phase appears to be genuinely spending the time. **The durable write is the named suspect** -- sshd's own comment says so: *from here to the end of this function is time spent on the durable volume, and by B-44 that is time the guest has no networking* -- and only its total was recorded, which cannot answer the question a total raises. It now reports `audit_worst_us` beside `audit_us`. Here that is 32 ms worst against an 11 ms mean, no tail at all; if the runner shows the same, the write is exonerated and the time is going somewhere else in that phase. **The stall line now says what the stalled pass spent on the durable volume**, reset per pass, because a run-long total cannot separate 1020 writes at 34 ms from one nine-second pass. Verified by lowering the report threshold from 1s to 2ms and taking 530 samples here: `stalled ms=23 phase=connections durable_ms=21`, `ms=12 durable_ms=11`, `ms=11 durable_ms=10` -- **the durable write is about 90% of every stall this machine produces.** At millisecond scale that is harmless and it is the same shape the runner shows at nine seconds; if the ratio holds there, the audit write is the cause and the 34 ms mean was the red herring a mean always is. One reproduction now settles it either way. **The first fix came from reading the code, not from soaking it.** `ssh_log()` wrote every line straight to the durable volume, and a xaibootfs append carries `blk_flush()` -- a virtio flush, which is a host fsync. A connection takes about five log lines, so sshd paid five host fsyncs per connection *inside the only loop that polls the network*. The source said so already: *from here to the end of this function is time spent on the durable volume, and by B-44 that is time the guest has no networking.* Records now accumulate in a 3 KiB buffer and go out in one write when it fills or a connection ends. Measured on the rate gate: **2.30 durable writes per connection before, 0.99 after**, and the blocking time for 30 connections fell from 771 ms to 343 ms -- with `audit_bytes` byte-identical at the same connection count, so the same records are written in fewer writes. On the CI workload the ratio was 5.05 per connection, so the reduction there is larger. A second step followed from the same reading: flushing at every close paid a host fsync per connection, and a flush costs the same whether it carries forty bytes or three kilobytes, so the buffer is now drained at close only when it is at least half full. Measured on the same gate: **121 connections, 5 flushes, 0.041 per connection**, and the total time in the blocking path fell from 1324 ms to **89 ms**. Together the two steps take the exposure from about five fsyncs per connection to about one per twenty-four. **This narrows the window; it does not remove it.** One fsync can still stall for seconds on a host whose disk does, and removing that needs B-44's structural change -- a timer or a thread for the network -- which that row costs out and which is not attempted here. **Rate, and a suspicion that did not survive checking:** the Debian 13 job failed on one commit and passed on the five before it and the one after, all of which carried the same kernel. The flow instrumentation landed four commits before the failure and four runs passed with it, so it is not the cause -- the honest reading is roughly one run in six to eight, which makes **CI the most reliable reproducer available**: three in 7138 rounds on Fusion, none in 7235, none in 11690, against better than one in ten runs there. Further local soaking is not the way to catch this one. |
| B-64 | `qemu-cpu-matrix` accepted `--arch` and ignored it | repository | `FIXED` | The RISC-V CI job invokes it as `--arch riscv64` and installs only a RISC-V emulator. The filter was read from `XAIOS_QEMU_CPU_MATRIX_ARCH` alone and argv was never parsed, so the argument did nothing, all three architectures ran, and the job failed on `qemu-system-aarch64 not found` -- an emulator it was never asked to use. An argument silently discarded is worse than one rejected. It now parses `--arch`, and rejects an unknown argument rather than ignoring it. Verified in `tests/linux-parity`: the two spurious failures are gone. |
| B-65 | Seven required CPU tiers need a QEMU newer than the runner's | repository | `FIXED` | `contracts/qemu-rc-v1.json` lists twelve RISC-V boot tiers and has no opt-out key, so all twelve are required. QEMU 8.2.2 -- what Ubuntu 24.04 ships and the runner uses -- models five of them: `rv64`, `max`, `veyron-v1`, `sifive-u54` and `thead-c906`. The other seven postdate it, and noble, its updates, security and backports carry nothing newer, so the emulator had to be built. **The gate was never wrong**: it could not test what the emulator cannot model and said so rather than passing quietly. Marking the seven not-required would have turned the step green while the coverage vanished, which is the opposite of this repository's rule that an inconclusive result still exits non-zero and names the host. `tests/scripts/provision-qemu-riscv64.sh` now builds QEMU v11.1.1 pinned by commit, cached by it, and verified by requiring every model the contract names to appear in `-cpu help` -- so a pin too old fails loudly instead of testing less. The two boot gates above it stay on the distribution's QEMU deliberately: they pass there, and a gate that only ever runs against a hand-built emulator stops saying anything about the one everybody else has. Verified in `tests/linux-parity`, which is where the build had to work and had never run: cold image to twelve tiers passed in six minutes. |
| B-66 | A three-architecture gate ran on a two-architecture runner | repository | `FIXED` | `qemu-libc-gate.py` has iterated `aarch64`, `x86_64` and `riscv64` since the RISC-V port landed, and the job running it installed `qemu-system-arm` and `qemu-system-x86` -- its name still said *ARM64 and x86_64*. The RISC-V leg built three userlands, a boot medium and a signed system volume, started a runner that exited 127 saying `qemu-system-riscv64: not found`, and the gate reported `riscv64 failed; missing=['C99-LANGUAGE-PASS', ...]` -- a list of things a guest had not printed, about a guest that was never started. The failure was 0.2s after the image was created, which is the only clue that no boot happened. This is B-58's shape a second time, so the fix is both halves: the job installs `qemu-system-misc`, and the gate now resolves every architecture's emulator **before the first build** and refuses with `INCONCLUSIVE, and the fault is this host's`, naming the missing package. A run that cannot be made now says so in seconds instead of after compiling three userlands to find out. Verified in `tests/linux-parity` on the Linux CI runs: the leg passes with `qemu-system-misc` alone and zero UEFI firmware files present, so the package is both correct and sufficient -- it boots through the OpenSBI QEMU ships. |
| B-67 | The RISC-V release image boots on macOS and not on the runner | riscv64 | `FIXED` | `release-image-gate` reported `ok qemu-aarch64`, `ok qemu-x86_64`, `FAIL qemu-riscv64` with all three markers absent -- no kernel banner, no shell command surface, no SSH server -- while the same image booted here and build 6 was cut with this gate green. The runner had the emulator and the firmware, so it was not B-58's shape. `platform/qemu/run-qemu-riscv64.sh` defaulted its EDK2 firmware to `/opt/homebrew/share/qemu/edk2-riscv-code.fd` **and nothing else**, so on any machine without `/opt/homebrew` the runner exited before QEMU started. Three absent markers read exactly like a kernel that did not boot; nothing had booted at all. The AArch64 and x86-64 runners have searched a candidate list since they were written, which is the whole reason they pass on Linux and this did not. The RISC-V runner now searches too, Debian's `qemu-efi-riscv64` paths included. Four gates that drive QEMU themselves had the same gap in a milder form -- they offered `/usr/share/qemu/edk2-riscv-code.fd`, which is not Debian's path either, so they *skipped* on Linux, and a gate that skips is not a gate that passed; they now share one list in `qemu_gate_lib`. `check-riscv-firmware-paths.py` keeps the shell copy and the Python copy in step and fails outright if either lists only macOS locations. Verified in `tests/linux-parity`: the release image now boots to a login prompt with SSH up, and the gate reports `ok qemu-riscv64`. |
| B-68 | Eight RISC-V gates took a prerequisite that built half a machine | repository | `FIXED` | `qemu-core-os-rc` failed on `nvme` (exit 2) and `fragmentation` (exit 124, a timeout at its full 1080s). The aggregate could only say a marker was missing; the sub-gate log said what actually happened: `error: no initial filesystem; run scripts/build-riscv64-image.sh`. The `riscv64` make target built the kernel and stopped, while every consumer of it boots a machine -- and a machine needs an initial filesystem. Eight targets took it as a prerequisite and then booted; the gates that worked did so only because they repeated the image step in their own recipe. Rather than patch the two that failed, the target now means what its users assume it means and builds both, which retires the class. The fragmentation timeout had the same cause: with no filesystem the runner refused and the gate waited out its budget, so it looked like a slow architecture rather than a missing file. Verified from a state with the kernel and the image both deleted: `qemu-riscv64-nvme-gate` passes on `riscv64 1 polled, riscv64-aia 1 msix`, and the fragmentation leg passes in 45s against the 1080s it used to spend failing. |
| B-69 | The memory matrix believed a number that had not finished arriving | repository | `FIXED` | Split from B-68, which it was filed with and does not share a cause. On the runner it reported `x86_64 at 2048 MiB manages 199 MiB`, `riscv64 at 1024 MiB manages 0 MiB` and `riscv64 at 2048 MiB manages 2 MiB`. **Every one of those was a healthy kernel.** The telemetry line is a single JSON object several kilobytes long, the pattern was `"pmm_total_pages":\s*(\d+)` with nothing after the digits, and the read loop breaks the moment it matches -- so a read landing mid-number matched the digits that had arrived, stopped, and never read the rest. The arithmetic settles it against figures measured here on a passing run: the true count is 511198 pages, whose first five digits are 51119 pages -- exactly 199 MiB; RISC-V's 519898 truncated to three digits is 519 pages, exactly 2 MiB; and 257754 to one digit is 2 pages, 0 MiB. Three CI figures, three exact digit prefixes. The pattern now requires the trailing comma, so it reads the whole number or nothing, and *nothing* is reported as `INCONCLUSIVE, and the fault is this gate's or this host's` rather than as zero memory -- a missing line is not a measurement. `hypervisor-memory-matrix.py` had the same unterminated pattern and was hardened with it. Verified: the old pattern matches every truncation including the exact 51119 CI saw, the new one matches only the complete 511198, and the full matrix still passes on all nine boots. |
| B-70 | Whether other gates stall the guest the way B-50 did | repository | `FIXED` | B-50 was a gate that piped the guest's console and stopped reading it while it worked, until the pipe filled, QEMU's write blocked and the guest stopped polling -- reported, for a long time, as a guest defect. Fifteen gates pipe the console rather than writing it to a file and eleven of those have no concurrent reader, so the question was how many of the eleven were exposed. **The answer is none, and it took reading them to say so.** The hazard needs both halves: the console in a pipe *and* a stretch of slow work while nobody reads. Four of the eleven make no blocking call at all. In the other seven every blocking call is a build, a `truncate` or a 50ms QMP retry, and each happens with no guest alive -- `qemu-fault-matrix` rebuilds after its boot loop has ended, `qemu-storage-crash-test` in a `finally` once every boot is stopped, the rest before their `Popen`. B-50 was the only gate that held the pipe across a long network phase, which is why it was the only one affected. A first pass at this list was wrong and worth recording: it matched an unrelated `sftp` subprocess and flagged `qemu-network-poll-cadence-gate`, which writes its console to a file and cannot be affected -- grep found the shape and not the hazard. `qemu_gate_lib.Console` now carries the reader thread so a gate that does need to pause can adopt it in three lines, and its docstring says what happens to one that does not. |
| B-71 | The operations closure waited 60s for a guest the runner interprets | repository | `FIXED` | `qemu-operations-closure` failed on CI with `TimeoutError: SSH did not become ready` after 403s of a 2100s budget. Not the reboot race B-60 covers -- it never reached the reboot; it was the first wait of the first boot. **The guest was fine:** `wait_lifecycle_durable` and `wait_marker(READY)` both returned first, so the machine had booted and announced `SSH server: up and running` before the wait that failed. The gate did not import `timeout_scale` at all, which is B-39's finding in a gate missed when it was applied. **The inner timeout was the one that bit:** the outer wait was 60s but each probe inside it was given `timeout=10`, and a curve25519 key exchange on an interpreted guest can exceed that -- every probe killed at ten seconds and retried, the outer budget irrelevant because no attempt was allowed to finish. Both now scale through `smoke_timeout(arch, base)` so the RISC-V factor comes with it, and the message names the architecture, the base and the variable that declares the scale. Fixed in two steps and the intermediate one is worth keeping: with scaling at base 60 it passed the first boot's wait and failed the second's, which said the scaling was right and 180s was still short. At base 180 -- nine minutes at the CI scale -- **it passes on the runner.** |
| B-72 | Fixing the nvme gate's RISC-V legs made it outgrow its budget | repository | `FIXED` | `qemu-nvme-gate` exited 124 having used its whole 900s twice, a consequence of B-68 rather than a regression: its RISC-V legs used to fail in under a second on `no initial filesystem` and now boot four configurations for real, against a budget set when a third of the work never happened. Measured here first -- 107s, 105s, 107s, three runs, all passing -- and that figure does not scale into an answer, because one of the four guests runs on hardware virtualisation here and none do on the runner. The parity container cannot settle it either: arm64, and the x86-64 userland will not build there. So the base went to 600 to stop the budget being the thing that failed, and the runner supplied the real number: **1643s of 1800**, and the step now fails on its merits rather than on the clock. What it fails on is B-73. |
| B-73 | The AIA board waits for an MSI-X interrupt that never arrives on the runner | riscv64 | `FIXED` | With B-72's budget raised, `qemu-nvme-gate` fails for a real reason: three of its four rows pass and `riscv64-aia` is missing `nvme: MSI-X interrupt self-test passed queues=1 all_queues=1` and `controller=aia-imsic`. It is not a scaling oversight -- the row resolves its architecture to `riscv64` and already had a 1440s deadline. **The first reading of this row was that AIA is expensive under interpretation, and that is wrong.** Measured here, where RISC-V has no acceleration either, the AIA row takes **14s against plain riscv64's 28s** -- it is the cheaper of the two boards, not the dearer. What the runner's console actually shows is the row getting *far*: `nvme: async self-test passed namespaces=1 ... rounds=8`, `boot-ui: progress=50`, then `aia: identity=5 first message delivered hart=2 wired_source=1` and nothing more. That is a guest waiting for an interrupt, not a guest running slowly, and it spent the rest of its 1440s waiting for a line that was never coming. **The difference to test is the emulator:** the Core OS job uses Ubuntu's QEMU 8.2.2 and this Mac has 11.1.1, and AIA/IMSIC message delivery to an NVMe queue is exactly the kind of thing that moved between them. **It holds, and it was reproduced rather than argued.** The same row run in `tests/linux-parity` against 8.2.2 produced the runner's signature exactly: `nvme: async self-test passed ... rounds=8 async=11`, then `aia: identity=5 first message delivered hart=0 wired_source=1` repeating to the end, no MSI-X line, exit 1 at the full 1200s deadline. The same row passes in 14s on 11.1.1. So the fix is the one B-65 already built: the Core OS job now restores and provisions the same pinned QEMU the RISC-V Bring-up job does, from the same cache key, and exports `QEMU_SYSTEM_RISCV64` -- which `run-qemu-riscv64.sh` has always honoured. **What this does not establish** is whether 8.2.2's AIA is wrong or XAIOS's MSI-X setup relies on something 11.1.1 added; the guest reaches the same point on both and only one delivers. That distinction needs a QEMU bisect and does not block the runner. **Confirmed on the runner.** With the pinned QEMU provisioned, `Core OS Aggregate RC` reports *all QEMU-testable core capability gates passed*, and the nvme step fell from **1643s to 175s** -- the 1440 seconds the AIA row had been spending were all spent waiting for a message that was never going to arrive. |
| B-39 | `core-os-rc`'s fragmentation step timed out once here, and timed out on CI | all four | `FIXED` | **Two causes, both found and both fixed, and neither was the fragmentation itself.** The CI half was `B-68`: the `riscv64` make target built the kernel and not the initial filesystem, so the runner refused the boot and the gate sat waiting out its budget -- a missing file presenting as a slow architecture, which is why raising the budget looked plausible twice. With the target fixed the RISC-V leg passes in 45s. The local half was the same inversion one level down, and it is why the single sighting could never say anything: the gate waited `smoke_timeout(arch, 180)` for each boot marker, which is 720s for RISC-V, against the 360s the aggregate allows the whole step -- so `qemu-core-os-rc` killed the gate first and recorded `exited 124` with no architecture, no phase and no reason, which is exactly the bare timeout this row had as its only evidence. The wait is base 60 now, and the scaled budgets and the aggregate's raised CI cap stand on their own. **`check-aggregate-budgets.py` fails on either inversion**, which is what stops it returning. **Closed on measurement rather than on a recurrence, because the recurrence this row asked for was the unreportable form and that is the thing that was removed.** On the run at `95ecdf0` the step completed in **144.2s of its 1080s budget** with the step named in the report; three consecutive runs on a second machine passed in 129 to 144s. What remains is one local slowdown of about 3.7x whose only record was that bare timeout, and the instrumentation now names the step, the architecture, the phase and the marker, so a second one would describe itself rather than having to be argued about. |
| B-77 | The kernel's ephemeral ports overlap the DNS resolver's and NTP's | all four | `FIXED` | Three private constants in three files each believed the dynamic range was theirs: the allocator hands out from 49152 upward one at a time, the resolver drew uniformly across 49152..65535, and NTP sent from a fixed 49155 -- so the fourth ephemeral draw was NTP's port and the first was one the resolver could also choose. Both subsystems match on the receive path before the socket table does, which is why a reply arrived somewhere rather than nowhere: the two owners were told apart by which matcher ran first, not by either of them knowing the port had two. **The partition is now stated once, in `kernel/include/xaios/local_ports.h`, and enforced there.** The resolver sources from 49140..49150, NTP from 49151, and the dynamic range above belongs to the allocator alone. The header carries `#error` checks that the blocks are disjoint and that both sit below the allocator's floor, and those were run against deliberately broken values to confirm they fire -- a partition that cannot be violated by editing one constant is worth more than a comment saying where the edges are. The receive path needed nothing: the resolver matches on the port it recorded for the query and NTP matches on its own constant, so neither ever knew the range. Pre-existing for the TCP connect path, which shared the counter and the range; `B-35` widened who was exposed to it rather than introducing it. Verified: `compile-check` clean on aarch64, x86_64 and riscv64, `hosted-test` including the DNS test, and `qemu-datagram-ephemeral-port-gate` still passing with its own ports inside the dynamic range. |
| B-78 | A socket refused a listener row still reports a port that cannot receive | all four | `FIXED` | `network_stack_register_udp_listener` returned void, logged `UDP listener registry full` and did nothing once the sixteen rows were gone -- while its callers had already written the descriptor and the port. The caller was therefore told it owned an address whose every reply is dropped, because `process_udp_frame` looks the listener up by port and drops the frame when it finds none; the failure was silent and permanent, and `docs/API.md` states the guarantee without qualification. **The registry calls now return `XAIOS_OK` or `XAIOS_ERR_NO_MEMORY` and both syscalls refuse rather than report:** `NET_OPEN_UDP` registers before it writes either out-pointer, and `NET_LISTEN` frees the socket it allocated and rejects. An internal caller in `network_stack.c` had already been working around the void return by registering and then asking `network_stack_has_listener`, which is the shape of an API that was missing its answer. **Covered now, which this row said it was not:** `/bin/netsocktest` fills the registry while its three sockets are open and reports how many were handed out and whether a refusal came, and the gate requires the refusal -- a kernel that never refuses is one handing out ports that cannot receive, and from userspace that is indistinguishable from success. Measured on the gate: sixteen allocations logged, the seventeenth refused, and the three ordinary opens untouched. |
| B-79 | The TLS 1.3 handshake has its pieces and a state machine, and no 0-RTT | all four | `OPEN` | `userspace/wt/` now carries the whole client handshake: the key schedule (RFC 8446 section 7.1) split into its two phases, QUIC traffic keys, Initial secrets, the Retry integrity tag and packet protection (RFC 9001 sections 5 and 6), handshake framing and the transcript, a ClientHello builder, ServerHello, EncryptedExtensions, CertificateRequest, Certificate and CertificateVerify parsers, x25519, Finished compute and verify, the CertificateVerify signature check, the pinned-key policy (`B-80`, now closed), and `wt_tls_client.c`, the state machine that sequences them and refuses what does not belong. **1487 host checks** against RFC 8446, 8448, 9001, 7301 and 7748 run under AddressSanitizer and UndefinedBehaviorSanitizer, in CI, and the handshake completes end to end against a server flight built by an independent Python implementation (`tests/security/generate_wt_quic_flight.py`) rather than by this code -- which is what makes the client's own Finished a check and not a tautology. Five adversarial audits and the sanitizer runs have found and closed a remote stack overflow in the Retry tag, a resumption master secret derived from the wrong transcript, a packet number encoder using the wrong range, a GCM decrypt path that failed on every packet, a measurement pass writing through a NULL pointer, a verifier with two unsequenced modifications of the same variable, an AES block encrypt that computed E(0) XOR the input, a CertificateVerify digest buffer sized for SHA-256 while offering SHA-512 (`B-81`), a public key that pointed into a dead decoder frame (`B-82`), an ALPN extension read in a format RFC 7301 does not define (`B-83`), BearSSL's Curve25519 using big-endian scalars where RFC 7748 says little-endian, a fail-reason getter that reported a failure for a handshake that had succeeded, and a clear that wiped the secrets but left the state CONNECTED so the transport-parameter getter kept answering. **What is absent:** 0-RTT and the session tickets it needs, so there is no early-data encryption level at all; a HelloRetryRequest is refused by name rather than handled, which is honest but is a server this client cannot talk to; and client certificates, where the client answers a CertificateRequest with an empty Certificate (RFC 8446 section 4.4.2) and never a CertificateVerify. |
| B-94 | `qemu-readonly-medium-gate` fails on the runner about one run in four | repository | `FIXED` | **The premise written here was wrong, and the gate was racing itself.** Two things were believed and neither survived reading the evidence. It was not the read-only boot that went quiet: the captured step log from the failing run on `b08823d` says the *writable* boot never reported `medium=writable`. And the guest was never silent -- the gate cut it off. The driver prints its verdict in two `klog` calls, `...self-test passed discovery=1 ` and then `medium=writable `, and the gate stopped reading as soon as `medium=` appeared anywhere in what it had. QEMU forwards the console in chunks, so the read carrying the prefix need not carry the value, and the boot was killed holding `medium=writ`. Three sightings produced no explanation because the console that showed this went to a file the aggregate does not upload, so the CI log carried the verdict and none of the evidence. **Fixed in the gate, not in the guest.** Reading now stops only on a *complete* value -- the one the case expects, or the other one, which is a real answer -- and a boot that misses its marker carries the emulator's exit status and the tail of its console into the failure message. Neither can turn a failure into a pass. **Verified where the gate can run:** three consecutive passes, and expecting the wrong value on purpose produced `the guest reported medium=read-only, so the scratch device was attached the other way round`, which is the sentence this row spent three sightings trying to earn. Asking QEMU for the medium directly was tried and dropped: `query-block` returns nothing for these drives because they are `-drive if=none`, and `query-named-block-nodes` reports the file-level `ro` flag, which with `snapshot=on` belongs to the overlay's *backing* file and is therefore identical in both cases. A signal that cannot discriminate is worse here than none, because it would read as QEMU contradicting the guest. |
| B-95 | Twelve kernel calls printed a 64-bit value through a 32-bit specifier, or the reverse | all four | `FIXED` | Fourteen conversions across twelve calls in seven files, and how they were found is worth keeping. Two searches produced different sets: CodeQL's ten alerts and the compiler's thirteen diagnostics overlapped in nine, so the union is twelve calls -- and only eleven were fixed the first time. `riscv64/mmu.c` is compiled by the riscv64 leg and by nothing in the x86_64 sweep that produced the compiler's list, so the compiler structurally could not see it and CodeQL could. It was caught by a second read of the alert queue, not by a build, which is the argument for leaving findings where a person will look at them rather than only where a tool will. The same check had a second blind spot, `B-97`: it compiled one build configuration while every gated image ships another. `klog` was given the `format(printf, ...)` attribute that `panic_at` has always carried and that these two had never been given: `%u` against a `uint64_t` in `virtio_blk.c`, `user.c`, `riscv64/mmu.c` and three `xaiboot_fs.c` lines, `%lx` against a 32-bit BAR in `pci_ecam.c`, `%lu` against an `int` status in `remote_login.c`, and `%d` against a `uint64_t` exit code in `user.c`. None of it is cosmetic. `klog_vformat` reads `%u` as an `unsigned` and `%lu` as a `uint64_t`, so the 32-bit cases threw the real value away and the 64-bit ones read the high half of a 32-bit argument; both print a diagnostic that lies, which is the one thing the formatter's own `%c` comment says must not happen. The three `pci_ecam.c` BARs were the worst, printing stack bytes beside every address. **The attribute is the fix and the twelve edits are what it found.** It is asserted for the freestanding build only: on a hosted build `uint64_t` is `unsigned long long` where every kernel target has it as `unsigned long`, so it would report 39 width-neutral disagreements between two 64-bit types in the files `hosted-test` compiles -- typing accidents of the host's `<stdint.h>`, none of them a defect and none of them a width the formatter reads differently. Nothing goes unchecked: `compile-check` builds every kernel source freestanding on aarch64, x86_64 and riscv64 with the attribute active, which is where they were found. |
| B-96 | The kernel links every BearSSL source, including algorithms nothing calls | all four | `FIXED` | `scripts/build-bearssl.sh` compiled `find src -name '*.c'`, so `libbearssl-xapt.a` carried the whole upstream tree and ten code-scanning alerts came out of it. **What this row first said about that tree was partly wrong and is corrected here:** XAIOS does use the TLS 1.2 engine -- `br_ssl_client_reset` and the individual `set_default_*` calls -- so `ssl_*.c` could not simply go, and a reading of the file names alone would have broken `xapt`. What could go is narrower and was settled by reading the call sites: the seven `des_*` files, `ssl_engine_default_descbc.c`, `ssl_client_full.c` (which registers every suite upstream knows, DES-CBC among them) and the two `ssl_server_full_*.c` profiles, which are a server this project never is. `ssl_hs_client.c` names DES only as the engine's `ides_cbcenc` and `ides_cbcdec` hooks, which nothing ever sets, so the implementation behind them is unreachable here. It is excluded by pattern rather than by naming the files to keep, because the crypto and the engine are upstream's to get right. **Verified where the images are built:** the archive drops from 294 objects to 283, the aarch64, x86_64 and riscv64 images all link with zero undefined symbols, the guest still boots through the read-only-medium gate, and `make xapt-test` passes. The ten alerts close because the code is no longer in the archive, which is what the dismissals on those alerts pointed at. |
| B-97 | `compile-check` compiled one configuration and every image ships another | all four | `FIXED` | Every kernel leg compiled the default configuration, in which `XAIOS_BOOT_TEST_APPS` is 0, because the files default it with `#ifndef`. Every gate that boots a guest builds `make image-qemu-test`, which sets it to 1, and `remote_login.c` alone switches nine regions on it. So the arm that ships was compiled by `build-image.sh` on every run and by this target never -- which is how a format string reachable only there (`B-95`) passed both `Compile Check` and a hand-run sweep of the same flags. **Found by pushing the fix for that string and watching ten jobs go red while `Compile Check` stayed green**, which is the shape of a check that is not checking the thing it names. A leg now compiles the same aarch64 file set with `-DXAIOS_BOOT_TEST_APPS=1` and the build number, so both arms of every such `#if` in the tree are compiled here. **The general form is the point:** a gate that compiles a configuration nobody boots reports the tree clean while the shipped configuration goes unbuilt, which is the failure this page already names as worse than having no gate. Two things are worth carrying forward from doing it. The first attempt at the leg was itself broken -- the recipe is one shell command joined by backslashes, so the `@#` lines explaining it were not comments and the shell ran them; the explanation lives above the target now and the recipe says why. And it was verified on a machine that can compile rather than by reading it. |
| B-98 | `make xapt-test` had been failing and nothing ran it | repository | `FIXED` | The target is in the Makefile, in `.PHONY`, and in no CI job. Run for the first time in this session it failed: `tests/xapt/test_xapt_repo.py` passed `--version 0.2.0` to a tool whose `build_number()` has required a whole number since the project left semver behind, and it built the two paths it then asserted on from that same string, so all three had to move together. **Fixed and put under a gate:** the fixture uses a build number, and `hosted-engine` now runs `make xapt-test` after `make hosted-test`. The shape is worth more than the incident: a test target that no job invokes is not a test, it is a file recording that it passed once. |
| B-99 | The Fusion load soak fails on node2 about one round in 64, and the guest is provably not the cause | repository | `OPEN` | First run of the soak on a second machine, and it failed: **10 of 641 rounds**, at rounds 65, 129, 193, 257, 321, 385, 449, 513, 577 and 641 -- every 64th round, which at this rate is about once a minute. Each failure is `sftp exited 255 ('ssh: connect to host 192.168.18.77 port 22: Operation timed out')` and takes **7.83s** against a normal round's 0.33s, which is the shape of a TCP connect that ran out of SYN retransmissions rather than a session that was refused. **It is not B-43 and not B-63.** B-43 is an ~19s stall in which the guest is silent; B-63 is a connection the guest *accepts* and then never services. Here the guest's own account of the failing round contains no trace of the connection at all: the correlation window shows one `net_accept` and one complete session, which is the DEBUG3 probe the soak fires after a failure, and that probe connected in **0.07s**. **The guest is healthy by every measure the run keeps:** `accepted=1282, closed=1282, outstanding=0`, `free_page_trend delta_pages=0` across 641 samples, and no panic, assertion, `not polled` or `service loop stalled` line anywhere in 3.5MB of console. So the SYN was lost or delayed between the host and the bridged VM, which is node2's networking rather than XAIOS's -- and the same soak has passed three times on this Mac. **A misreading to record, because it cost time:** the console does contain `network: socket-to-flow map exhausted size=160 ... protocol=6 refusals=1`, and it is the *self-test* -- the next line says `socket-flow map exhaustion self-test passed capacity=160 filled=160 refused=1`, and the absurd `sockfd` is the test's synthetic value. B-63's row was right to rule out map exhaustion and this does not reopen it. **A second client on a different machine takes the host stack out of the frame, and was run.** The same round trip was driven from node1 -- a different physical Mac, its own stack, its own route to the guest -- against the same guest while node2's soak held it up: rounds 2 to 56 clean, then **six consecutive failures (57 to 62)**, clean to 100, then **three more (104 to 106)**, every failure **7.82 to 7.87s**, which is the client's patience and not a guest figure. The failures arrive in bursts of consecutive rounds, so the guest was unreachable for tens of seconds at a time rather than losing one SYN. Two things follow: the signature is not peculiar to node2's stack, so *node2's networking* is too narrow; and the spacing is a **period, not a count** -- at 0.93s a round, *every 64th round* and *about once a minute* are the same statement, so the earlier alignment on multiples of 128 connections was arithmetic rather than a socket limit. **Three mechanisms are now refuted from the console rather than by argument:** the per-port bound (the only `per_port` line is `syscall: socket ownership self-test passed capacity=256 per_port=128`, and no `max per-port` or `allocation denied` line exists anywhere in the run), the half-open bound (`NETWORK_TCP_MAX_HALF_OPEN` is 16 and no `SYN flood protection` line was ever printed), and the B-43/B-63 service-loop stall (zero `service loop stalled` lines in the whole capture). **The per-round host timestamps place the loss in time:** round 65 failed and round 66 did not begin until **50.5s** later, and the guest's console across that window shows it accepting and completing session after session (`net_accept connfd=154, 155, 156, 157`, `remote-login: session complete authenticated=1`). The machine was serving throughout; one client's connect was not answered while others were. **A caveat that matters:** node1's run overlapped node2 rebuilding the kernel image with the VM already running, so node2's host was loaded and the long bursts may owe something to that. A clean rerun on an idle host is what would settle it. **The simultaneous-client run was done, cleanly, and it takes the client stacks out of the frame.** With node2 and node1 probing the same Fusion guest at the same time and nothing else running, both failed **in the same windows**: node2's bursts began at t=51, 111, 168 and 232 seconds and node1's at t=111, 171 and 228, every connect timing out at **7.81 to 7.87s** and every burst lasting about 39s over six consecutive rounds. Two physical Macs, two host stacks, two routes, one guest, the same outage at the same wall-clock moment -- so the loss is the guest or the path to it, and not either client. That also retires the caveat above: this run had no concurrent rebuild. **The guest's stack and sshd are clean on a second, independent virtual network.** The identical 4 KiB put/get/remove was driven against a QEMU guest on node3 over QEMU's userspace network (`-device virtio-net-device`, host forwarding, `hvf`): **4000 rounds, zero failures**, at `43b0187`, sustaining about eleven connections a second for six minutes -- some six of the sixty-second periods this row's failures recur on, against twenty-one failures in four hundred rounds on Fusion. **The comparison is confounded by the NIC driver, and that is written down rather than glossed over.** The Fusion VM is built with `ethernet0.virtualDev = "e1000e"` and so exercises the guest's e1000e driver, while QEMU's aarch64 runner *always* attaches `virtio-net-pci,netdev=net0` as its first NIC and the guest selects virtio-net even when `XAIOS_QEMU_NETWORK_DEVICE=e1000e` is set -- confirmed both by the runner's own dry run, which emits `-device e1000e,netdev=net1` beside the unconditional `virtio-net-pci,netdev=net0`, and by the guest's `network-device: selected virtio-net`. The QEMU run therefore clears the network stack, the TCP paths and sshd **on the virtio path** and does not clear `kernel/dev/net_device.c`'s e1000e path. **The driver was separated directly, and it is not the driver.** Booting the same guest under Fusion with `XAIOS_FUSION_NIC=vmxnet3` -- the same hypervisor and the same bridged vmnet with a different NIC driver, so e1000e is held out and vmnet held in -- gives the same failures at the same cadence: **14 in 400 rounds**, burst starts at t=42, 104 and 164 seconds, gaps of 62 and 60, every connect timing out at **7.84 to 7.86s**. The guest binds the device and says so (`network-device: selected vmxnet3`, `vmxnet3: activated tx_ring=32 rx_ring=32 mtu=1500`), so this is not a driver that failed to attach. With QEMU's userspace network clean over 4000 rounds on virtio-net, and Fusion failing over both e1000e and vmxnet3, **the common factor in every failure is Fusion's virtual network on node2**. The guest's stack, its sshd, its e1000e driver and both clients are each held out by a run that changed one thing at a time, and what remains is the host's virtual network. **The earlier attempt failed in its harness, not in the guest, and that is worth recording so the next person does not repeat it:** the soak hung waiting for a marker while a VM started by hand on the same VMX booted normally, leased `c0a8124d` and brought `SSH server: up and running (tcp/22)` up. **The vmxnet3 run also shows the ~18s signature beside the 7.8s one** -- `Connection closed` after 18.14s and `Read from remote host ... Operation timed out` after 18.16s -- which is B-43's and B-63's shape on the same path. It is recorded here as a pointer and not a claim: B-63's own mechanism is established in its own row and this does not reopen it. **The address was separated too, and it is not the LAN either.** The same guest with `ethernet0.connectionType = "nat"` instead of `"bridged"` -- e1000e, so the driver is held fixed and only vmnet's mode changes -- fails the same way again: **10 in 400 rounds**, bursts at t=107 to 139 and t=169 to 200 seconds, every connect at **7.84 to 7.87s**, against the guest's NAT address `ac10c480`. Four runs now hold every XAIOS-side candidate out. Bridged/e1000e fails with 21, bridged/vmxnet3 with 14 and nat/e1000e with 10, while QEMU's userspace network on the same guest took 4000 rounds without one failure. What is common to the three failures and absent from the clean run is **Fusion's virtual network on node2, in either vmnet mode** -- not the physical LAN, not the NIC driver, not the guest's stack, not its sshd, and not either client. **What this row cannot say is why** -- nothing here identifies the mechanism inside vmnet -- but the loss is now visible from inside the guest, which is the strongest form this evidence has taken. The kernel logs a line each time a flow's RTO expires and it retransmits, and the two paths differ by three orders of magnitude on the same workload, the same client code and the same 4 KiB round trip: **Fusion on e1000e and bridged logged 2887 retransmits across 577 distinct flows in 600 rounds**, with 579, 577, 577, 577 and 577 of them at `retransmit=1` through `5` and the RTO backing off to 32s, while **QEMU on virtio-net logged 2 retransmits in 1500 rounds**. The guest's TCP is retransmitting because its segments are not being acknowledged, and it barely does so when the virtual network is QEMU's. The logging is old -- it arrived with `bf0f176`, long before this row -- so the storm is not new instrumentation and the difference is the path. **It is not flow exhaustion and not a stalled service loop, which were the two ways this could have been a XAIOS defect:** `data retransmit limit` never fires, the only `network: socket-to-flow map exhausted` lines are the self-test with its synthetic `sockfd`, `service loop stalled` is zero, and the worst poll gap in the run is 136 ms. **One inconsistency is left standing rather than smoothed over:** B-63's row records a single retransmit line in a 33 MB Fusion console, and this run logged 2887 in 1.3 MB over only 600 rounds. Both of those are Fusion, so the path does not explain the difference between them and nothing here does. What is established is the comparison above -- Fusion against QEMU, on the same workload -- and not a claim that every Fusion run looks like this one. The 24 failures in those 600 rounds were all the 7.8s connect timeout, in bursts of exactly six rounds -- 128 to 133, 262 to 267, 396 to 401 and 530 to 535 -- and the earlier ~18s shape did not recur. So the row stays open as an environment question rather than a repository one: no evidence any longer points at XAIOS, and the next step is VMware-side rather than a change to this tree. **Re-run on 2026-09-13 against a newer hypervisor, and the upgrade does not fix it.** This Mac's Fusion was updated to **26.0.1** between the runs above and this one, which was the one untested reason left to hope the fault was Fusion's and would age out. It is not: **13 failures in 641 rounds** at `9eebf88` -- rounds 129, 193, 257, 321, 358, 422, 449, 486, 513, 550, 577, 614 and 641 -- which is a **worse** rate than the 10 in 641 first recorded (about one round in 49 against one in 64), and 129 to 321 are exactly 64 apart, so the period survives the update too. Twelve of the thirteen are the same `ssh: connect to host 192.168.18.66 port 22: Operation timed out`; **round 358 is a different signature** -- `Read from remote host 192.168.18.66: Operation timed out` followed by `client_loop: send disconnect: Broken pipe` -- which is B-63's shape and not the connect timeout, so **one run produced both** and the two are not as separate as the earlier runs suggested. The guest is healthy by every measure the report keeps, exactly as before: `free_page_trend delta_pages=0` across 641 samples, `accepted=1283, closed=1283, outstanding=0`, and the DEBUG3 probe fired after each failure connected in **0.06 to 0.07s**. **What this settles is narrow and worth having:** the rate, the period and the signature are all unchanged by the hypervisor upgrade, so this is not an old-Fusion defect that 26.0.1 addresses, and waiting for vmnet to be fixed is not a plan. **Two things about this run are stated rather than glossed.** It ran at `9eebf88`, which contains this session's `network_stack.c` change -- `operations_tick()` moved from `network_poll_tick_locked()` to its caller, plus the `intr=` field on the gap line. That is behaviour-preserving (the same call, in the same place relative to the network work, still under the same guard) and no interrupt tick is armed anywhere, so the run is comparable to the earlier ones; it is recorded because a soak result on a modified network file should say so. And the report's new `closes_of_unaccepted_descriptors: 17` is **not** evidence of the guest leaking: the guest's own tally is balanced, and the figure counts closes for descriptors the host-side ledger never saw opened, which is this harness meeting its own failed connects late. What it means beyond that is not established, and it is written here as a number rather than as a finding. |







### Resolved, kept for reference

| ID | Defect | Affects | Closed by |
|---|---|---|---|
| B-127 | `reboot` on RISC-V powered the machine off | riscv64 | Found by the administrative lifecycle gate's first run on this architecture, which `B-126` had just made possible: after `ssh reboot` the machine never came back, and no lifecycle record followed. `arch_reboot()` called `sbi_shutdown()` -- the SBI system-reset extension asked to *stop* -- so a guest told to reboot disappeared, and every RISC-V gate that had ever looked like it rebooted was booting a new QEMU process instead and could not have noticed. **The fix is the call the port already had the plumbing for:** `sbi_system_reset(reset_type, reset_reason)` with `SBI_SRST_RESET_COLD_REBOOT` (1) for `arch_reboot` and `SBI_SRST_RESET_SHUTDOWN` (0) for `arch_power_off`, with the legacy shutdown extension kept as the fallback that only shutdown can use -- a reboot on firmware without SRST now parks rather than pretending, because reporting a restart that did not happen is worse than not restarting. **Evidence:** `python3 tests/scripts/qemu-operations-closure.py --skip-docker --arch riscv64` passes: `qemu-operations-closure: PASS`, with `lifecycle: record durable boots=1 unclean=0`, `boots=2 unclean=1`, `boots=3 unclean=0` after the SSH reboot, `boots=4 unclean=1` after the abrupt stop of the rebooted guest, and `boots=5 unclean=0` after the orderly shutdown. `make compile-check` exits 0 on all three architectures. |
| B-126 | The RISC-V image could not carry an SSH authorized-keys file, so the machine could not be key-administered | riscv64 | Found while extending the administrative lifecycle gate to RISC-V, which the objective's own wording asks for and which was the one architecture that gate did not cover. `make riscv64` with `XAIOS_AUTHORIZED_KEYS_FILE` set failed with `too many initfs files`; without the key it built and the new line said why: `initfs: 64 of 64 entries used`. The initial filesystem's directory was full, and the entry that did not fit is the one that turns a machine serving SSH into one an operator can administer. **The ceiling is the format's own.** A directory record is `PATH_MAX` 64 plus 28 bytes and the directory lives in `HEADER_BYTES` 8192, so 80 records is the most that fit; the number was 64 in three places (the builder, the kernel's `INITFS_MAX_FILES`, and the ABI contract) and nothing compared the builder's to the kernel's, which is how an image could be one entry from a build failure nobody was watching for. It is 80 in all three now, the static kernel tables grow by about 1.7 KiB for the sixteen extra entries, and `qemu-abi-contract` reads both files and fails if they disagree. **Evidence:** the RISC-V image builds with a key and reports `initfs: 65 of 80 entries used`, and `make qemu-abi-contract` passes with the kernel side now compared. **What it led to is the point of adding the third architecture at all:** the closure's RISC-V leg then ran the lifecycle, got past the image and the first two boots, and stopped at `reboot` -- which is `B-127`, a port whose `arch_reboot` asked firmware to stop the machine. With that fixed the leg passes: `qemu-operations-closure: PASS`, lifecycle records 1 to 5 with `unclean=1` after each abrupt or unplanned stop and `unclean=0` after the orderly one. |
| B-125 | The aggregate gate asked for an NVMe completion count the system never promised | repository | `qemu-core-os-rc.py`'s `emulated_nvme_io` check required the literal `rounds=8 async=38 cancelled=1` in the nvme gate's output. That count is how many asynchronous round trips the guest got through, so it scales with the queue count and varies by host: the gate's own stdout on this machine carries `async=39` for its two four-queue rows and `async=12` for its two single-queue ones, and `make qemu-nvme-gate` passes all four rows because it asserts a **minimum** per profile (`async_ops >= min_async`, `rounds >= 8`, `cancelled >= 1`, `malformed >= 4`, `msix == profile`). So CI's `Core OS Aggregate RC` for `9271e1d` failed with `emulated_nvme_io: missing marker: rounds=8 async=38 cancelled=1` while every sub-gate it aggregates passed, and the same marker was what failed the aggregate two commits earlier. The check now requires the phase's shape (`rounds=8 `, `cancelled=1`), the kernel's own `nvme: async self-test passed namespaces=1` line, and the gate's verdict line, which carries the per-architecture driving line the old marker was written for. Verified against the gate's real output: all four required markers match, and `make qemu-core-os-rc` passes locally end to end with the fix -- `status pass`, `all QEMU-testable core capability gates passed`. This is `B-119`'s family -- a check that fails on a number nothing promised -- and it is the second marker in this file found asserting one; the sibling it already had right requires `faults=` as a prefix for the same reason. |
| B-120 | A wakeup consumed between an idle CPU's check for work and its halt left the thread pending | x86_64, riscv64 | CI's `x86_64 Bring-up` timed out in `xaios_thread_run_group` with `joined=2 created=4` and the threads for cpu 2 and cpu 3 still `PENDING` after the whole budget, on CPUs that were online and past the scheduler barrier. `B-122` removed the mistaken clock behind the earlier shape -- the same budget then expired after **30.096 s of wall clock** rather than 12.3 s -- and the failure came back, on the default CPU model as well as the high-MMIO one, which is what ruled out the Intel server layout the row was named after; the run for `eb1692f` passed the same step, so it was a race and not a memory map. **The mechanism is the gap between the idle loop's question and its halt.** A secondary asked the thread queue, asked the timer, and halted; an interrupt delivered in that gap was taken by its handler, and the CPU then slept with the thread that interrupt announced still pending -- and nothing woke it again, because the network tick is armed on exactly one CPU and the others have no periodic interrupt at all. **Two of the three ports had already closed this and one had not.** AArch64 closes it with `sev`: `xaios_cpu_notify()` issues one after publishing a thread and it sets the wait-for-event latch that a `wfe` reaching it later returns on. x86-64 has no event register, so its idle loop now asks the queue once more with interrupts masked (`xaios_thread_pending_on_cpu`, a peek that claims nothing) and then halts and enables interrupts in one step, the `sti; hlt` pair, whose interrupt shadow means an interrupt is recognised after the halt rather than before it. RISC-V had the same hole and now waits with `sstatus.SIE` clear, which its `wfi` is required to resume from for a locally enabled interrupt that is pending -- so the wakeup stays pending and is what the wait returns on instead of being consumed first. **The window is a few instructions wide, so the check builds it rather than waiting for it.** `smp_idle_wakeup_self_test()` widens it through `x86_64_platform_set_idle_halt_probe`, wakes a CPU that is not the tick carrier, waits until it is inside the widened window, and only then hands it a thread; it then runs the same construction with the halt written the way it was, which must lose the thread. Three runs in a row: `smp: x86 idle wakeup self-test fixed=1 raced=1 control=0 control_raced=0 cpu=1 gaps=5` -- `raced=1` is the re-check being what kept the wakeup, and `control=0` is the kernel as it was losing it, with the thread recovered afterwards so the control reports rather than panics. The gate requires the `passed` line by name and requires the named refusal where the machine has one CPU, and the other two architectures are required by their own smokes to say the check does not apply to them. **Evidence:** CI's `x86_64 Bring-up` for `9271e1d` passed `Run SMP x86_64 smoke`, `Run conservative FXSAVE fallback` and `Run Intel server high-MMIO layout` -- the second of which failed on the previous commit for a reason of its own, and the third is the step that lost threads twice and passed once before this -- with the self-test's verdict in the console; locally, three `make qemu-x86_64-smoke` runs, its single-CPU row, `make qemu-smoke`, `make qemu-riscv64-smoke`, the x86-64 and AArch64 operations closures and `make compile-check` all pass. |
| B-100 | The NVMe driver could read a completion while the device was still writing it | riscv64 | The shape this row was opened with: under a starved host the self-test's stress phase failed with `XAIOS_ERR_IO`, and once it was instrumented every sighting had the same signature -- `io completion rejected qid=1 cid=0 ... slot=none`, a completion whose command identifier is zero and which matches no request, while the host's rings and the device's own registers were both correct. The driver was taught to survive it (a controller restart and one retry) and the mechanism stayed open. **It is a torn read, and the compiler's codegen is the proof.** `nvme_completion_t` is packed and its `cid` and `status` fields share one 32-bit dword -- the command identifier in the low half, the status and its phase tag in the high half. The waits copied the whole entry and *then* checked the phase, and that copy is not one access: `llvm-objdump` of the aarch64 build shows `ldrh w6, [entry, #14]` for the phase and reaches for `cid` only after the comparison has passed. A completion the device is still writing is therefore read as a fresh phase with a command identifier that has not been written yet -- a completion that looks successful and matches no request, which is also why the request behind it never completed: the driver consumed the ring slot and the real completion was skipped. **The fix reads the phase first, in one access.** `completion_in_phase()` loads that dword once with an acquire and only then lets the rest of the entry be read, because the phase is the last thing the device writes; the aarch64 code is now `ldar w8, [entry, #12]` followed by `ubfx w9, w8, #16, #1`, and RISC-V has `lw` plus `fence r, rw`. **A reproduction landed in the recovery path first, and named the same thing:** `nvme: self-test failed step=controller-restart` with `admin completion rejected opcode=1 cid=0 expected=18 sq_head=0` and `trace[1] cq_head=1 cid=0 matched=0` -- the create-I/O-submission-queue completion (cid 18) was never seen because its entry was read mid-write. **What decides torn-versus-genuine on the next sighting is in the driver now:** a refused completion is read again and the driver prints whether the two reads agree, so a genuine device answer cannot be mistaken for this again. **Evidence:** `make qemu-nvme-gate` passes all four rows, and `python3 tests/scripts/nvme-stress-soak.py --row riscv64-aia --runs 40 --load 8` reports `runs=40 of 40 reproductions=0 other_failures=0` on the host that reproduced it -- against a defect that appeared at run 3 of the first loaded attempt and about once in twenty-five loaded runs before that. Forty clean runs against that rate is evidence and not proof, and this row says so rather than claiming more; what does not depend on the rate is the codegen, which was a defect on its own terms. |
| B-124 | The operations closure could not tell a refused reboot from a machine going down | repository | `ssh_reboot` ran `reboot` over ssh with `ok=None`, which is how it survived a command whose success destroys the connection carrying it -- and it also meant a guest that *answered* and declined looked exactly like a guest that went down mid-command. The next phase waits for lifecycle records a refusal will never produce, so the run failed with a sentence about the records rather than about the refusal, which is precisely the confusion `B-121` cost days to unpick. The command is run directly now and its status is read: 0 is the request accepted, 255 is ssh saying the connection failed, which is what a shutdown racing the channel looks like, and anything else is the guest's own answer, raised with its words. The x86-64 closure passes with the change; the two cases it distinguishes cannot both be produced on demand here -- a guest that refuses a reboot has no way to be asked to -- so what is verified is that the accepting path still passes. |
| B-123 | A CPU spinning with interrupts masked could not answer a TLB shootdown, and the x86-64 operations closure panicked on it | x86_64 | The closure's x86-64 leg had never run -- its AArch64 leg always failed first -- and the first time it did, the third boot panicked during the DNS checks: `x86_64: panic: TLB shootdown timeout`, from `x86_64_platform_invalidate_page_all`. The line above it, `waiting for cpu=1 apic_id=1 generation=7134 acknowledged=7133 online=1`, named the CPU that had not answered and nothing else. **The refusal was widened until it could say what the silent CPU was doing, and that is what named the cycle.** A CPU now publishes `waiting_for` (`xaios_cpu_state_t`, written by `xaios_cpu_note_wait`, named by each reentrant guard through the new `XAIOS_REENTRANT_LOCK_INIT(name)`), and the x86-64 record keeps `interrupts_taken`, `last_vector`, `shootdowns_handled`, `shootdowns_polled` and `shootdown_lock_wait`. Those counters must not cost a host round trip each time they are written, so bring-up puts each CPU's ordinal in `IA32_TSC_AUX` and reads it with `RDTSCP` -- `x86_64: per-CPU identity source=rdtscp`, or `apic-id` where the CPU has no RDTSCP, which is correct and slower. The next reproduction said `interrupts=7590 last_vector=35 shootdowns_handled=7132 shootdown_lock_wait=0 waiting_for=network guard`, with the initiator at `waiting_for=tlb shootdown in progress`. **So the answer was not late, it was impossible.** A reentrant guard masks interrupts before it spins and keeps them masked for the whole critical section -- which is exactly what makes it safe to take from a handler -- so cpu 1 waiting for the network guard could not take the shootdown interrupt, while cpu 0 was inside a shootdown and holding that same guard. A guard's holder maps and unmaps pages inside its critical section, which is a shootdown, so the two CPUs wait for each other until the budget runs out. **The spinning CPU now answers the request the interrupt would have carried.** `xaios_cpu_service_shootdown_request()` is called from `xaios_cpu_relax()`, so every spin in the kernel that runs with interrupts masked answers by hand -- it invalidates and stores the acknowledgement with no interrupt involved. The request is published address-before-generation so a CPU that sees a generation also sees that generation's address, and an acknowledgement is monotone so the interrupt and the poll cannot un-acknowledge each other when they overlap; one load gates the whole thing when no shootdown is in flight, and the shootdown lock's own spin answers too. **The cycle is now built on purpose rather than waited for.** `smp_shootdown_ack_self_test()` takes a guard on the boot CPU, has a thread on another CPU spin for it, and shoots down inside the guard; its verdict line in `make qemu-x86_64-smoke` is `smp: x86 shootdown acknowledgement self-test fixed=1 control=0 polled=1/0 interrupted=1/1 cpu=1`. `control=0` is the kernel as it was -- the spin's answer suppressed, with a 50 ms budget -- and it is *required* to time out, so a pass cannot mean the cycle was never built; `polled=1/0` and the assertion that the fixed run was answered by the spin rather than by an interrupt that landed before the worker masked are what make the pass mean something. The gate requires the `passed` line by name, and AArch64 and RISC-V, which have no acknowledgement to wait for -- hardware-broadcast `tlbi` and a firmware fence call -- are required by their own smokes to print that the check does not apply to them. **Evidence:** `qemu-operations-closure.py --skip-docker --arch x86_64` passes **three times in a row** where it panicked in **three of the four runs made before the change**, the AArch64 closure passes, `make qemu-x86_64-smoke`, `make qemu-smoke` and `make qemu-riscv64-smoke` pass with their new markers required, and `make compile-check` exits 0 on all three architectures. The closure boots the non-test image, where the boot UI gates `klog`, so there the self-test is evidence through its assertions rather than through its line. **The hook is a weak reference, and CI is what said so.** `xaios_cpu_relax()` is inline in a header that `make hosted-test` also compiles -- it builds `kernel/fs/vfs.c` against a hosted libc -- and on an x86-64 host that inline referred to a symbol only the kernel defines, so the hosted job failed to link: `vfs.c:(.text+0x2a57): undefined reference to 'xaios_cpu_service_shootdown_request'`. The declaration is weak now, so a build without the kernel's definition gets a null address and skips while the kernel's own definition is what the machine calls; that this cannot silently become the skip in a kernel is the self-test's job, because it fails unless the spin path answers, and the smoke requires its verdict. Verified by cross-compiling a translation unit that calls `xaios_cpu_relax()` for x86_64 and linking it both without and with a definition (the first links, the second resolves to the definition), and by disassembling the linked kernel, where the call target is the definition rather than zero. |
| B-121 | The operations closure waits for a third durable lifecycle record that never arrives | all four | `Core OS Aggregate RC` has been red on `main` at least since `00b9bbb`, with `operations exited 1 after 271s of a 2100s budget` and `operational_lifecycle_closure: missing marker: qemu-operations-closure: PASS`. The step's own output -- which the aggregate prints now, and did not when this was first read -- says exactly where: `qemu-operations-closure.py:91 wait_lifecycle_durable` raised `TimeoutError: missing 3 durable lifecycle record(s); saw ['lifecycle: record durable state=running boots=1 unclean=0 storage=disk status=ok', 'lifecycle: record durable state=running boots=2 unclean=1 storage=disk status=ok']`. Two boots wrote their record, the third did not, and the gate that asks whether the system persists reports a missing PASS marker instead of a defect. **What is not established:** whether the third boot failed, whether it wrote its record and the console lost the line (`B-119` is exactly that class, and this console is the kind that drops under load), or whether the wait's window is short for a loaded runner. The artifact for that run is 1,132 bytes and holds one guest line, so next to nothing of the run was kept. **Reproduced here on the first attempt, and named.** `XAIOS_BOOT_TEST_APPS=1 python3 tests/scripts/qemu-operations-closure.py --skip-docker --arch aarch64` shows what the closure's own console cannot: the reboot *was* requested, and then **cancelled by the storage flush** -- `operations: power action scheduled action=2`, then `operations: storage quiesced flushed=4 unsupported=0 failed=2`, then `operations: power action cancelled because storage flush failed`. The two failures are virtio-blk flushes that never complete: `virtio-blk: flush completion timeout avail=18306 used=18305 target=18306` and `virtio-blk: flush completion timeout avail=5 used=4 target=5` -- one on a heavily used queue and one on a nearly unused one, at the same moment, and `recover_queue` runs after each. So the third lifecycle record never appears because **the machine cannot reboot**: `operations_tick` cancels the power action when any device's flush fails (`block_flush_all` counts anything but `XAIOS_OK` as a failure, and `XAIOS_ERR_UNSUPPORTED` too), and the closure then waits out its deadline for a boot that never comes. **The gate cannot see any of this, and that is half the row.** The closure boots the *non-test* image (`make image`, with no `XAIOS_BOOT_TEST_APPS=1`), where the boot UI gates `klog` output off; only the two direct console writes (`lifecycle: record ...` from `operations.c` and `SSH server: up and running` from `boot_ui.c`) reach the serial log. Every line that would have named the refusal is suppressed -- which is why this row, and `B-115` before it, read as a missing marker on a machine that was working well enough to answer SSH. **Owed:** why the flush completion never arrives -- the request is published (`avail` is one ahead of `used`) and the notify is written unconditionally, so the device either never consumed it or never reported it -- and whether a *reboot* should be cancelled by a flush failure at all: a machine asked to come back that cannot flush one volume should say so and come back, or refuse by name, rather than stay up in silence. `ssh_reboot` passing `ok=None` is the third part: it swallows the refusal the guest does send. **And it is fixed, with the duplicate ownership at the root of it.** `virtio_block_open_administration_window` takes "the first block device nothing else has taken", and the registry it consults did not contain the disk the machine had just booted from: the primary is configured by `virtio_block_init`, which never called `block_device_note_taken`, while all three `open_*` paths do. So the window opened the machine's own disk, and `start_handle` re-negotiates the features -- which resets the device and clears its queue -- and then programs *its own* rings into it. Every later request from the machine's own handle, including the flush a reboot depends on, then goes to rings the device no longer reads: 5-second timeout, `recover_queue`, and `block_flush_all` failing the power action. The identity in the timeout lines is the proof: `device=virtio-blk base=0xa000000 capacity=20613` and `device=virtio-blk-h base=0xa000000 capacity=20613` -- two handles, one disk. **The fix has two halves.** The primary registers itself as taken, and every `open_*` path now refuses a device the registry says is owned, before touching it, naming the slot and base. **Verified end to end:** `python3 tests/scripts/qemu-operations-closure.py --skip-docker` now passes, with the console showing the full sequence the row was opened for -- `boots=1 unclean=0`, `boots=2 unclean=1`, `operations: power action scheduled action=2`, `boots=3 unclean=0` (the reboot happened), then `action=1` and `boots=5`. **What it would have cost outside the gate:** the disk seized that way is, on an installed machine, the one holding the running filesystem, and the comment above the registry already records the machine coming up once without a working shell for exactly this shape. |
| B-122 | The x86-64 time base was a guess of 1 GHz, so every timeout, sleep and clock reading on that architecture was scaled by the machine's real TSC over that guess | x86_64 | `discover_timer_apic` reads the TSC frequency from CPUID leaf 0x15 (crystal frequency and ratio) and falls back to leaf 0x16 (a base frequency in MHz), and then, when neither answers, **assumes 1 GHz**. The CPU models this kernel is gated on report `ratio=0/0 crystal_hz=0` and nothing in 0x16, so the guess was the value in use, and `timer_now_ns()` is `rdtsc` divided by it: every timeout, every sleep, every wall-clock reading on x86-64 was scaled by the machine's real TSC over one billion. **Found from `B-120`, and the arithmetic is exact.** That row's thread-join budget is thirty seconds; on the CI runner it expired after 12.3 seconds of wall clock, which puts the runner's TSC at about 2.4 GHz. The CPU model was never the cause -- only how far the guess was from the truth on the host underneath. **The guess is replaced by a measurement.** When neither CPUID leaf answers, the kernel now measures the TSC against the PIT, the one clock on an x86 machine whose frequency is fixed by definition (14.31818 MHz / 12): channel 2 in mode 0 with the gate held low, the full count programmed, the gate released, and the TSC read across the interval the counter takes to reach zero. It is measured **twice and the two readings are required to agree within a twentieth**, because a single reading from a channel that might be wired differently is the kind of number this defect was made of; if they disagree, or the channel does not answer, the old guess stands and the console says so. The discovery line now ends `source=pit tsc_hz=<measured>`, so a machine whose clock is still a guess is readable off the console. **Measured here:** `x86_64: timer discovery tsc=1 deadline=0 ratio=0/0 crystal_hz=0 source=pit tsc_hz=999971541`, and the two PIT readings agreed -- this host's TSC really is 1 GHz, which is why the guess survived a year of gates on this machine, and why `B-120` does not reproduce here. The x86-64 smoke passes with the measurement in place; the boot spends about 110 ms on it, and only when CPUID has nothing to say. **Accepted on the runner, and the arithmetic closes.** `x86_64: timer discovery tsc=1 deadline=0 ratio=0/0 crystal_hz=0 source=pit tsc_hz=2445625955` -- **2.4456 GHz**, not 1, measured the same way in all three boots of that job (2,445,625,955 / 2,445,323,968 / 2,445,308,801 Hz, each from two PIT readings that agreed). The old guess was wrong by a factor of 2.4456, and the budget that expired after 12.3 seconds was 30 / 2.4456 = 12.27 seconds: the sighting and the measurement agree to three significant figures. **And the job that was red is green:** `x86_64 Bring-up` and `Hosted C99 libc` both pass, and the only failing job left in the run is `Core OS Aggregate RC`, which is `B-121`. The row keeps its `OPEN` status for the second half below, which the same run confirms rather than contradicts -- `lapic` came out 115,658,845 / 169,648,168 / 164,735,927 Hz in three boots of one job against 22.0 and 55.2 MHz on this Mac: same derivation, five answers. **The same discovery has a second half, and it is not fixed.** `g_lapic_frequency` -- which sets the scheduler tick interval -- is derived from `elapsed_tsc` measured across a single `sti; hlt` waiting for one APIC interrupt, so it includes the whole interrupt's *host* latency: on this machine it read **22,031,284 Hz** in one run and **55,216,540 Hz** in another, same host, same CPU model, two and a half times apart. The tick interval follows that number, so a loaded machine boots with the scheduler ticking at the wrong rate. Deriving it from the calibrated TSC instead -- by polling the APIC timer's current count across a known TSC delta -- is the fix this half owes. **And the tick frequency, which was the same defect seen from the other side.** `g_lapic_frequency` -- which sets the periodic tick's interval -- was derived from `elapsed_tsc` across a single `sti; hlt` waiting for one APIC interrupt, so it *was* the host's interrupt latency: this machine reported 22,031,284 Hz in one boot and 55,216,540 Hz in another, and the runner's job reported 115,658,845 / 169,648,168 / 164,735,927 Hz across three boots. It is now measured by running the counter down over a million of its own counts and timing that with the calibrated TSC, which involves no interrupt at all; the interrupt-latency figure is still printed, as `interrupt_tsc=`, because it is the noise that used to be the answer. **Measured here, three boots: `lapic_hz=1002971351 / 1007039021 / 1009034881`, a 0.6 % spread against the 2.5x spread of the derivation it replaces**, with `interrupt_tsc` varying 1,549,000 to 2,234,000 in the same three boots. The tick interval is built from this number, so the scheduler now ticks at the rate it says it does. |
| B-119 | The console lock drops lines, so a gate can fail on a marker the kernel did print | all four | `klog()` took the console lock with a try and dropped a contended line immediately, and `B-117` made that loss visible without preventing it. **This row decided the question that one left open:** CI's `x86_64 Bring-up` for `34e85ac` failed with one missing marker, `smp: x86 secondary worker barrier passed ready=`, and the console carried `klog: 1 log lines dropped` exactly where it would have been printed -- a line the kernel cannot have skipped, since that function returns through one of two `klog` calls and the other trips a `kassert`. The reporting was widened first (the note lives in `qemu_gate_lib.dropped_line_note`, `qemu-x86_64-smoke.py` prints it, and the milestone and aggregate wrappers print the failing step's own output). **Then the loss itself, by waiting rather than dropping.** A thread-context line now waits up to **5 ms** for the console lock -- that is what the lock is for -- while an interrupt-context line keeps a 200-microsecond bound, because the holder can be the thread that handler interrupted, which cannot run again until the handler returns; the console lock is a leaf, so the long wait cannot deadlock against anything. **Measured on the gate this family was found with** (`qemu-smoke.py --arch x86_64`, whose console carries the drop count): dropping on the first try lost **2 and 4** lines over two idle runs; a uniform 200-microsecond wait changed nothing (**3, 1, 1** over three) because the holder writes a whole line to a console that traps per character under emulation; the context-dependent wait lost **0, 0, 0** idle and **0, 0** under eight busy loops. AArch64 and RISC-V smokes pass, and `qemu-network-poll-cadence-gate` passes with a worst gap of **15.4 ms** and no outage past a second. A line that still loses the race is still dropped and still counted. **The runner then dropped one more line, and this fix's predicate was why.** CI's `x86_64 Bring-up` for `ee1819c` failed with one missing marker, the same `smp: x86 secondary worker barrier passed ready=`, and the console carried `klog: 1 log lines dropped, the console lock was held` where it would have been printed. The budget was chosen by whether interrupts were *masked*, and that line is printed from inside the scheduler guard -- masked, in thread context, with no handler anywhere near it -- so it took the two-hundred-microsecond bound meant for a handler and lost a race it had five milliseconds to win. **The question the wait actually asks is whether this CPU is inside a trap handler**, because only there can the lock's holder be the context the handler interrupted. Each CPU now carries a trap depth (`xaios_cpu_state_t.interrupt_depth`, maintained through the one x86-64 entry), and the ring-3 syscall is deliberately *not* counted: a syscall runs on behalf of a thread and may wait like one. The first version counted it, and the smoke's own report line said so on its first run -- `klog: 1 log lines dropped, the console lock was held in_handler=1 masked=0`, a syscall context reported as a handler; with that corrected, three x86-64 smokes report **zero** drops. AArch64 and RISC-V answer the same question with the mask, which is what they did before and is conservative. The report now names the context -- `in_handler=N masked=N` -- and `qemu_gate_lib.dropped_line_note` passes it on with a gate's failure, so the next loss says which budget was wrong instead of leaving it to be inferred. |
| B-112 | The fault matrix and the update gates were gates that no CI job ran, on any architecture | all four | Both existed in the Makefile and in `.PHONY`, passed when run by hand, and were invoked by nothing in `.github/workflows/`. The aggregate core-OS gate runs `qemu-fault-injection`, which is a different gate about a different property. This is `B-98`'s shape -- a target that no job invokes is a file recording that it once passed -- and it is worth a row rather than a note because the fault matrix is the only thing that asks the question the panic machinery exists to answer: does the machine fault where it was told to, and does it say so the way the other two architectures say it. **The update gates went in first**, as steps in the jobs that already build each machine -- `build-and-smoke` (AArch64), `x86-bring-up`, `riscv-bring-up` -- with their reports and consoles added to those jobs' uploaded evidence, so "it updates itself" is produced automatically rather than by hand. **The fault matrix followed, and the third leg of it is the part worth keeping.** AArch64 and x86-64 went in at 2m20 and 10m34 on the runner; the x86-64 target did not exist until `B-118` and took three fixes before it could run at all. RISC-V was left out, on the belief that `riscv-bring-up` was near its 90-minute cap. **That belief was never measured and it was wrong:** the job runs end to end in **seven minutes and ten seconds** -- smoke 1m56, update gate 1m10, release configuration 1m29, CPU tiers 1m57 -- and the gate itself is **1m45** here and **3m05** on the runner, where the figure the deferral rested on had the build inside it. So it went in too, and passes. **Verified on the runner, all three:** AArch64 2m20, x86-64 10m34, RISC-V 3m05, each with its scenarios' consoles uploaded so a failure names the machine rather than the gate. **The cost of the wrong belief was a round of deferral**, which is the argument `B-106` and `P-2` already make from the other direction: measure a budget, do not reason about one. |
| B-118 | The fault matrix had no x86-64 target, and would not have worked if it had | x86_64 | `B-112` said the fault matrix and "their x86-64 counterpart" existed in the Makefile. Two of those three existed. There was no `qemu-x86_64-fault-matrix`, and when one was added the gate reported all three scenarios as `the boot ended before the fault injector ran` -- not a guest defect, and not a pass either. Working out why took three separate faults, all of the same family this session keeps finding: something built, booted or asserted for a machine that was not the one under test. **First, the gate built the wrong architecture.** `BUILD_COMMANDS` runs `./scripts/build-image.sh`, which defaults to AArch64 and is told otherwise by `XAIOS_TARGET_ARCH` -- which the fault build never set. So an x86-64 run built an AArch64 image, left the x86-64 one as whatever was in `build/`, and booted that. The build environment is now made in one place (`build_env()`) and sets the architecture it is building for, for the fault images and for the normal-image rebuild at the end alike. **Second, the x86-64 machine faulted silently.** With the injector finally in the image, the page scenario still reported inconclusive: `exception_trigger_page_fault_for_test()` on x86-64 took the fault but never printed the line AArch64 and RISC-V both print, so the gate -- whose marker for "the injector ran" is that line -- read the scenario as never having started. A port that behaves the same and narrates differently is indistinguishable from one that did not behave, which is exactly what this gate exists to detect. x86-64 now says `exceptions: triggering controlled page fault at 0x...` like the others. **Third, the gate asserted AArch64's vocabulary on x86-64.** With all three scenarios actually running, they failed instead of reporting inconclusive, because the expected strings were `class=data-abort-current` and `class=instruction-abort-current` -- AArch64 ESR decodings -- and the shared sentence `controlled page fault reported`. x86-64 has no class string; its page-fault frame carries an error code, and its three scenarios are told apart by it: `error=0x0` not-present, `error=0x3` present-and-written, `error=0x11` present-and-fetched. And it halts with `controlled x86_64 exception reported`, naming itself. The gate now accepts x86-64's own vocabulary the way it already accepted RISC-V's, and the sentence is per-port. **This is `B-116`'s mistake in the gate rather than in the application:** a test written from the port that ran it first, asserting that port's answers on another. The difference is that here it was caught before anything depended on it, because the target had never existed to run. **Verified:** `make qemu-x86_64-fault-matrix` passes all three scenarios in about four minutes; `make qemu-fault-matrix` (AArch64) and `make qemu-riscv64-fault-matrix` still pass, so the shared vocabulary change is not a regression. |
| B-117 | `klog` threw log lines away when the console lock was held, and said nothing | all four | `klog()` takes the console lock with a **try**, deliberately: it is called from contexts that must not block, so a contended line is dropped rather than waited for. What was wrong is that it was dropped **silently**. Gates assert on console lines, so a line that was lost is indistinguishable from work that did not happen -- and that is the shape of a whole class of intermittent failures this repository has been carrying: a marker missing under load, on a machine that otherwise looks perfect. **Found from one of those.** The CI run for `e81e5f6` failed its aggregate gate with `x86_64 exited 2 after 204s of a 720s budget, load 12.57 to 0.69`, and the step's console -- 4,511 lines, ending at a login prompt with SSH up -- was missing exactly one marker, `smp: x86 secondary worker barrier passed ready=`. Neither that line nor its counterpart, `smp: x86 secondary worker barrier timed out ready=%u online=%u`, appears anywhere in it, so the kernel either lost the line or had it shredded by a concurrent writer, and from the outside there was no way to tell which. **Measured rather than argued:** with the fix in place, `python3 tests/scripts/qemu-smoke.py --arch x86_64` passes every marker on an idle machine and its console carries **nine** `klog: N log lines dropped` reports. The loss is routine, not hypothetical, and it was previously invisible. **Fixed in two places, because the count is only useful if it is read.** The kernel now counts contended drops and reports them from the next line that does get through, written while the lock is held: `klog: N log lines dropped, the console lock was held`. And `qemu-smoke.py` prints the total with its failure list, so a missing marker is accompanied by "this may be a line that was lost rather than work that did not happen" instead of leaving the reader to guess. **What this does not claim:** that the CI failure above *was* a dropped line. It says the two are indistinguishable without the count, which is now printed, so the next occurrence decides it. `B-39`, `B-50`, `B-70` and the operations-closure marker that fails intermittently are all in this family and are worth re-reading with a drop count beside them. **Verified:** `make compile-check` exits 0 on aarch64, x86_64 and riscv64; the shared smoke passes on x86-64 with all markers and reports the nine drops; the AArch64 and RISC-V smokes are unchanged. |
| B-109 | Every port's user-entry symbols were named after AArch64 | all four | `kernel/arch/x86_64/entry.S` and `kernel/arch/riscv64/entry.S` exported functions called `aarch64_enter_user` and `aarch64_enter_user_thread`, and the shared callers named them: `kernel/sched/thread.c` declared `extern uint64_t aarch64_enter_user_thread(...)` and `kernel/user/user.c` declared `aarch64_enter_user`. The names came from the port that had the mechanism first and were kept as it was shared, so it was one defect in three ports rather than three. **It was not cosmetic, and it produced a wrong answer before it was fixed.** Resolving a RISC-V panic backtrace against the RISC-V kernel printed `aarch64_enter_user_thread+0x32` for one frame, which reads unmistakably as a symbol table belonging to the wrong architecture -- the exact failure `resolve-panic.py` exists to refuse. The pairing was in fact correct and the investigation went looking for an AArch64 kernel that had never been involved. A name that lies about the machine is the thing this repository's first rule is about, and this one had already cost an hour of a diagnosis. **Fixed by naming the shared mechanism for the system:** `xaios_enter_user` and `xaios_enter_user_thread`, across the three ports and the two shared callers, with the reasoning written at the canonical definition in `kernel/arch/aarch64/entry.S` and a pointer to it at both extern declarations, so the reason travels with the name rather than living in a row nobody reads. **Verified:** `make compile-check` exits 0 on aarch64, x86_64 and riscv64 -- which is what says the assembly symbols and the C declarations still agree -- and the shared smoke passes on AArch64 and on x86-64 with every marker, plus the RISC-V smoke, which exercises the port that had the most of these. |
| B-115 | The shared smoke booted a machine it had not asked for, so x86-64 had no milestone gate | x86_64, repository | The objective asks the system to update itself on all three architectures. `qemu-update-gate` (milestone 68) and `qemu-riscv64-update-gate` existed; x86-64 had none, and adding one failed with exactly 214 markers missing -- not a budget (600 seconds instead of 120 gives the same 214) and not a missing capability, because the console the gate read contained no kernel log at all. **The cause is one line of the Makefile and one line of the gate.** This gate reached the guest through `make <arch runner target>`. For AArch64 that target is a bare runner invocation; for x86-64 it is `qemu-x86_64: image-x86_64`, and `image-x86_64` runs `build-image.sh` *without* `XAIOS_BOOT_TEST_APPS=1` -- so the last thing the gate did before booting was overwrite the boot image with the configuration it was not asking about. Without the boot test apps the splash owns the console and suppresses the kernel log, and `qemu-installed-disk-gate.py` had already written down what that looks like: "the machine comes up perfectly and the gate sees none of it". Measured rather than argued: the same runner under the same environment gave **6,246 bytes** of console, `Loaded: UEFI firmware` and no `XAIOS Build`, against **273,401 bytes**, `XAIOS Build` and `kheap: self-test passed` with the test image in place. **Why the first attempt to fix it changed nothing, which is the part worth keeping:** `qemu-smoke.py` was made to build the test image before booting, and it made no difference, because the `make` target rebuilt over it one line later. The gate now builds the architecture's test image itself and then starts the runner directly (`qemu_runner(arch)`), which is what `make qemu-aarch64` had been doing for AArch64 all along. That also removes the luck the AArch64 leg was relying on: it passed because the test image happened to be the one in `build/`, not because anything asked for it, and `B-31` is what this repository's history says that luck costs. RISC-V has no test-image make target -- its image comes from two scripts -- so the RISC-V leg still depends on its own gates having built it, and the row says so rather than implying otherwise. **Two more defects were behind it, both fixed.** `B-116`: with the machine finally visible, the shared smoke failed on `/bin/xaiosctl` for a command that had rendered correctly. And the x86-64 update gate itself is now shipped: `qemu-x86_64-update-gate` runs milestone 68 and passes. **Evidence:** `python3 tests/scripts/qemu-smoke.py --arch x86_64` passes -- "reached all full userspace/resource markers" -- where it had never passed; `make qemu-x86_64-update-gate` passes; `python3 tests/scripts/qemu-smoke.py` (AArch64) still passes; `make qemu-riscv64-update-gate` still passes. |
| B-116 | The x86-64 build was not the machine the test was written for: a control self-test asserted an AArch64 answer | x86_64 | With the shared smoke finally booting the right image, x86-64 failed on four markers, and the first was `/bin/xaiosctl: command rendering test failed: xaiosctl hardware --json`. The command had rendered correctly. Its self-test required the string `"avx2":"unknown"`, which is an AArch64 and RISC-V answer: `kernel/arch/aarch64/cpu_features.c` and `kernel/arch/riscv64/cpu_features.c` hard-code `avx2 = 0`, while `kernel/arch/x86_64/cpu_features.c` reads it from CPUID -- so on a machine that has AVX2, the honest answer is not "unknown" and the test called the correct answer a failure. The human marker `cpu_vendor=unknown` was the same mistake about the same machine. **Fixed by asserting presence rather than value:** the markers are now `cpu_vendor=` and `"avx2":`, which still require the field to render and no longer require the machine to be a particular one. The table's own comment already said several of these fields "depend on what the machine actually has"; this entry was the one that contradicted it. **Recorded because of how it was found:** the defect is not that the feature detection was wrong -- it was right -- but that nothing had ever run this self-test on x86-64, because the milestone gates could not boot the machine at all (`B-115`). One defect hid the other for as long as the first one lasted. **Evidence:** the shared x86-64 smoke passes with all markers, and the x86-64 update gate passes on top of it; AArch64 and RISC-V smokes are unchanged. |
| B-113 | The storage-administration window was a device's position in one test bench's enumeration, so installing onto a blank disk could not work on a machine with fewer disks | x86_64, riscv64 | `kmain.c` opened the scratch disk with `virtio_block_open_slot(5U, ...)`, which the PCI transport carries to **enumeration ordinal 4** -- the fifth block device, in the order the QEMU test bench attaches five. A machine XAIOS has been installed onto has two, so the guest reported `no pci device of type 2 at ordinal 4; 2 present` and the spare was never opened. The window was not unreachable for want of an address -- the name it prints is transport-independent, which is why the kernel prints `/dev/vblk5` everywhere -- it was unreachable because a device was identified by where it sat in one bench's device order. The install phase of the x86-64 and RISC-V gates could only be skipped and say so, and on a real two-disk machine installing was impossible rather than merely unproven. **Fixed by preferring the configured window and falling back to the first device nothing else has taken.** The configured window is tried first, so a test bench behaves exactly as it did and the repository's primary gates are untouched; if it is absent, the window becomes the first block device no other volume has claimed. On a one-disk machine that is nothing and the caller is told so, which is right -- there is no spare; on a machine with a spare it is the spare, whatever order the firmware enumerated the bus in. The device keeps the caller's logical slot, so it is named `/dev/vblk5` wherever it was found, which is the point: a device's name is a name, not a position. The choice is logged rather than silent -- `storage-admin: window slot=5 is not attached; using the first unclaimed block device, ordinal=1`. **The fallback needed a registry, and that is the part worth keeping.** Opening a device twice is not refused by the device and was not checked by the driver: each open configures a queue and registers a completion, so a second one would fight the first for the same virtqueue. A blind scan would therefore have been a way to corrupt the volumes that were already mounted. `virtio_blk.c` now records which physical devices it has taken, identified by transport and by the address of the device's common configuration structure rather than by an ordinal -- the same device is reached by different ordinals depending on which lookup finds it, so an ordinal would not have identified anything. **Evidence, all three architectures and every phase:** `make qemu-x86_64-installed-disk-gate` passes all four -- both installed-disk boots, an install onto the blank disk, and **the disk XAIOS wrote booted on its own with `SSH server running on the installed system`**; `make qemu-installed-disk-gate` (AArch64) still passes all four, its target attached on MMIO bus 5 so the configured window resolves and the fallback never runs; and `make qemu-riscv64-installed-disk-gate` runs the install phase and passes. `make compile-check` exits 0 on all three. The `install_phase_skip` keys are removed from both profiles, and the skip machinery itself is kept, so an architecture that genuinely cannot run the phase still has a way to say so. |
| B-114 | The install check could not pass on x86-64, and the first successful install there was reported as a failure | x86_64, repository | The `INSTALL` check in `qemu-installed-disk-gate.py` required `files=5 bytes=\d{8,}` -- an eight-digit byte count calibrated on an AArch64 ESP of ten-plus megabytes. An x86-64 ESP carries no GRUB chainloader and is about 9.6 MB, which is seven digits, so that regex could never match on that architecture. Nothing noticed because the phase was skipped there (`B-113`); the moment the phase ran, the gate reported `MISS installed onto the blank disk` for an install whose own log said `install: self-test passed target=/dev/vblk5 files=5 bytes=9591720 esp=/dev/vblk5p1`. **The consequence was worse than a confusing message:** the phase after it is guarded by `if install_ok:`, so the false failure meant the gate never booted the disk XAIOS had just written -- the one piece of evidence the whole gate exists to produce. **Fixed** by using the per-architecture `ESP_BYTES_DIGITS` the boot checks already use. That calibration is what this file gained when the x86-64 profile was written, and this check was left out of it; the same omission in the `TARGET_VOLUME` name was `B-103`. Evidence: the gate now passes all four phases on x86-64, including the installed disk booting to an SSH server. |
| B-108 | RISC-V could not boot an installed disk: the secondary hart stacks were a quarter of the size already proven too small, with no guard under them | riscv64 | The gate never passed until this. Boot 1 died in most runs with `unhandled trap cause=1 epc=0x4` -- an instruction access fault at address 4 -- and in another with `scause=0xc, sepc=stval=0x80f2fc90`, while a manual run of the same kernel went through completely. A race, and every surrounding change moved it. **The cause is a stack, and the code had already been told so once.** `SECONDARY_STACK_BYTES` (`kernel/arch/riscv64/smp.c`) was **16 KiB with no guard page**, while the boot hart's stack is **256 KiB with a page unmapped underneath it**, and `kernel/arch/riscv64/linker.ld` records why: sixty-four was not enough, because the deepest chain this kernel takes is storage -- an installer walking a FAT directory from inside a syscall from inside osctl -- and an overflow there did not fault, it quietly overwrote the per-CPU current-process table. A thread running on any hart other than the boot hart takes its **entire syscall chain on that hart's stack**, and the nested user-thread run (`xaios_thread_join` borrowing the CPU, `B-02`) multiplies the depth: every level adds a 288-byte trap frame, a 112-byte stub frame and the C frames of the chain below it. So the deepest chain in the kernel ran on a quarter of a size already known to be insufficient, with nothing underneath it to hit. **That explains the shape as well as the fault:** an overflow writes into whatever the linker placed next, which is how a frame comes to hold a garbage stack pointer; execution then arrives in non-executable memory, which is `scause=0xc` with `sepc == stval`; and whether it happens at all depends on which hart ran the deep chain and how deep the nesting got, which is why the address moved every time anything around it changed. **Fixed by giving the secondary stacks the boot stack's own treatment:** 256 KiB each, and a guard page under each that `vmm_init` unmaps exactly as it does for the boot stack, so the next overflow is a page fault at an address that names itself rather than a corruption that surfaces somewhere else entirely. The boot log confirms all eight: `vmm: 8 secondary stack guard pages left unmapped`. **Evidence:** `make qemu-riscv64-installed-disk-gate` passes **twice in a row**, both boots green on both runs -- every marker including `state written by the previous boot survived` -- where it had never passed before. Two passes against a race is evidence and not proof, and this row says so rather than claiming more; what does not depend on the race is that a stack a quarter of a proven-insufficient size with no guard is a defect on its own terms. `make qemu-riscv64-smoke` also passes with all thirteen C99 markers, and `make compile-check` exits 0 on all three architectures. **Still not in CI, now for a budget reason rather than a failure:** see `B-103`. |
| B-104 | RISC-V could not boot an XAIOS-installed disk: a five-second join that was a speed assertion, and a kernel fault behind it | riscv64 | Two faults, both closed. The first was a budget: the C99 thread test's join allowed five seconds, an installed-disk boot has three harts because EDK2 keeps one, the scheduler put two of the test's three workers on the same hart, and with no preemption on this port the third worker's first instruction waited for the second to finish -- so the budget was measuring the machine's hart count rather than whether a thread finishes. It now waits a minute, for the reason written beside it. That fix moved the failure rather than ending it, which is what the row recorded at the time, and the fault it exposed is `B-108`: the secondary hart stacks. Both are fixed and the gate passes; the join timeout no longer appears and the deep chain now has the room this kernel had already established it needs. |
| B-111 | The RISC-V panic dump printed every register under the wrong name, and ten of them from memory it never wrote | riscv64 | The register block is the first thing a reader looks at in a fault report, and on this port it was wrong in three ways at once. `capture_gp_regs` stores the RISC-V registers **by ABI name in ABI order** -- ra, sp, gp, tp, t0-t2, s0-s1, a0-a7 -- while `render_gp_regs` fell through to the AArch64 branch, which prints `r[0..30]` as `x0`..`x30` and `r[31]` as `SP`. AArch64's capture fills exactly that shape (its own comment says `r[0..30] = x0..x30, r[31] = SP`); this port's does not. So every label named a different register than the value printed beneath it, the ten `x17`..`x30` lines were printed from a stack array the capture never wrote, and the `SP` line printed whatever happened to be at `r[31]`. **It is the same defect as `B-107`, in the same function's sibling:** an `#else` written for AArch64 that catches RISC-V as well, which is what happens when a port is added by widening another port's code. **And it cost real work before it was found.** Two rounds of diagnosis reasoned from `x2 = 0x0` and `SP = 0x0` as evidence that a corrupted frame had lost its stack pointer. `r[2]` on this port is `gp`, which is legitimately zero, and the `SP` line was unwritten memory. The stack pointer was in the dump the whole time, one line away, under the name `sp`. The lesson is `B-107`'s: a diagnostic that misnames its subject is worse than one that says less, because it is believed. **Fixed:** the capture now stores all thirty-one named general-purpose registers (all except `x0`, which is hardwired zero and cannot be anything else), the renderer has its own RISC-V branch that prints them by ABI name in capture order, and the `SP = r[31]` line is printed only by the two ports that actually put a stack pointer there -- this one prints `sp` in the list, which is what a RISC-V reader looks for. **Verified three ways, and the third is the one that was owed.** The capture and the renderer agree register for register (`check-panic-registers.py`, with two controls); all three architectures build (`make compile-check` exits 0); and a deliberate panic has now been **read back**. That last one needed the gate changed first: `qemu-fault-matrix` kept its console in memory and wrote no log, and it stopped reading the moment its markers matched -- which is *before* the register block, so it kept the finding and threw away the evidence. It now keeps every scenario's console under `build/qemu-fault-matrix-<arch>-<scenario>.log` and reads on for the halt banner, and `make qemu-riscv64-fault-matrix` passes with three consoles kept. The page scenario's dump reads `ra = 0x80211950` (kernel text), `sp = 0x811204b0` and `s0 = 0x811204d0` (a stack), and **`gp = 0x0`** -- which is the direct confirmation that the old dump's `x2 = 0x0`, the value two rounds of diagnosis treated as a lost stack pointer, was the `gp` register and nothing else. The three scenarios report distinct causes (`scause` 0xc, 0xd and 0xf), each by ABI name, and no dump prints an `SP` line. |
| B-110 | A panic dump was shredded by the other harts' logging, and on RISC-V it was not even given a quiet machine | all four | Boot 2 of the RISC-V installed-disk gate halted, and the only evidence a halted machine leaves was unreadable: the log contained `System halted. Manual reset required.` **inside** a `user: rejected syscall=11 arg0=0x3fc0008065 arg1=0x17 reason=fs-open-denied` line, and the dump's frames were split through other lines byte for byte -- `user: reje=512 touchected ed=1 bsyscall=11 arg0locks=1 generation=365`, `#2  5 arg10x802000ee`. Two console streams were writing at once and neither could be read. **The cause is in the code and it is deliberate on one side.** `panic_putc`/`panic_puts` in `kernel/core/panic.c` write through `klog_write` and `klog_puts`, the *unlocked* variants -- the comment beside the numeric helpers says why: "safe in panic", because a dying machine must not take a lock another hart may already hold. Every other hart writes through paths that take `g_klog_lock`, and a lock serialises the ordinary writers against each other and against nothing the panic path does. So the two streams interleave a character at a time whenever any hart is still logging -- which on an SMP machine is always. **A second defect in the same function, found while fixing the first:** `panic_at` disables interrupts with `msr daifset` on AArch64 and `cli` on x86-64 and had **no RISC-V branch**, so a panic on that port kept taking interrupts while it dumped. **Fixing the ordinary writers was not enough, and the next run showed why.** With them suppressed the dump was still shredded, because the interleaving was then **two panics at once**: CPU 2 and CPU 3 each reported `ERROR: controlled page fault reported`, filing from the same `kernel/arch/riscv64/exception.c`, and the two dumps ran through each other. Suppressing ordinary logging cannot help with that, because the panic path must not suppress itself. **So the console has one owner.** `klog_console_panic_claim()` is a test-and-set: the first hart to panic gets the console and dumps, and a hart that panics while a dump is running counts itself and halts without printing -- counted rather than silent, because the running dump reports it. The ordinary paths ask `klog_suppressed_by_panic()` and drop what they were asked to print, and the dump's banner carries both numbers: `console claimed: N log writes from other harts suppressed` and `console claimed: N further harts panicked while this dump ran and were not printed`. The panic path's own output is untouched, because the whole point of it is that it does not take the lock; the four guarded entry points (`klog`, `klog_level`, `klog_write_atomic`, `klog_console_write`) are exactly the ordinary ones, and that nothing outside `panic.c` calls the unlocked writers was checked rather than assumed. `klog_level` is guarded whole rather than at its prefix, because it prints its prefix through `klog()` and its message through `klog_vformat` directly, and suppressing one half of every line would have been the same defect in a different hat. `panic_at` also gains the RISC-V interrupt-disable branch the other two ports already had. **What this costs, stated rather than glossed:** a hart that panics second loses its own dump. On the evidence above the two faults were the same fault on two CPUs, so little is lost; on a machine where they were different, the count in the banner is the only sign that a second one exists, and the run has to be repeated to see it. That is the trade accepted to make the first dump readable at all. **Verified:** `make compile-check` exits 0 on aarch64, x86_64 and riscv64, and the RISC-V kernel builds. The claim, the four guards and both banner lines are in the built kernel. The end-to-end check -- one halt, one readable dump -- is the run this change was made for. |

| B-107 | A RISC-V panic said it was an AArch64 panic, and the resolver believed it | riscv64 | `render_sys_regs` in `kernel/core/panic.c` was `#if defined(__x86_64__) ... #else ... AArch64 registers ... #endif`. The `#else` was written for AArch64 and catches RISC-V, so a panic on this port printed `ELR_EL1`, `ESR_EL1`, `FAR_EL1`, `SPSR_EL1` and `CurrentEL` for what `capture_sys_regs` had read out of `sepc`, `scause`, `stval` and `sstatus`: the values were right and every label belonged to another machine. **That is a diagnostic defect with teeth, because something reads those labels.** `tests/scripts/resolve-panic.py:panic_machine()` decides from the register block which machine produced a panic, and it had no RISC-V branch at all. Demonstrated on the real dump from the RISC-V outbound-fragmentation leg: `panic_machine(...) = 'aarch64'` while `elf_machine(build/kernel-riscv64/kernel.elf) = 'RISC-V'`. So the tool **refused to resolve against the kernel that had actually produced the panic** and told the reader to go and find an AArch64 one -- and had an AArch64 kernel been supplied, the architecture check would have passed and the addresses would have been resolved against the wrong symbol table with no hedging whatsoever, which is the precise failure its own comment says the check exists to prevent ("every address lands somewhere in some symbol table, so a wrong build does not fail, it lies"). **The lie is measurable.** Frame `#0 0x802171ba` resolves to `virtio_block_init+0x56` against the AArch64 kernel and to `panic_at+0x8a` against the RISC-V one -- and a panic dump whose top frame is the panic routine is the answer a reader expects. **Fixed in two files, because one was not enough.** RISC-V gets its own branch and its own labels, with the reasoning beside it; and `panic_machine()` reads `scause`/`sepc`/`stval` first, while `elf_machine()` names RISC-V in the same vocabulary -- it returned the raw `RISC-V` before, so a corrected panic side answering `riscv64` would have made the tool refuse a *correct* pairing. A dump with no register block now says that the architecture could not be read and that the check did not run, instead of resolving in silence. **One more wrong line, removed rather than translated:** `CurrentEL` is AArch64's encoding, with the mode in bits [3:2], and this port passed the constant 1 for supervisor -- which the shared renderer shifted right by two and printed as `(EL0)`. Every kernel panic on RISC-V therefore reported user mode. A wrong privilege level in a fault report is worse than no line, so `sstatus` is printed and left to be read, with the reason written down. Controls, all run: the relabelled real dump identifies as `riscv64` and resolves against the RISC-V kernel; the same dump against the AArch64 kernel is refused by name, quoting both architectures; and AArch64 and x86-64 dumps are still identified as before. |
| B-106 | The RISC-V libc leg's deadline was the one budget in the tree that ignored the declared host scale | repository | On the CI run for `ffb4a50` the job `Hosted C99 libc (ARM64, x86_64 and RISC-V)` failed with `RuntimeError: riscv64 failed; runner was still going when the deadline passed; missing=[every C99 marker, the first one included]; first output line: 'OpenSBI v1.3'`, while the same job passed on the immediately preceding run with the same tree one commit earlier. **What this row first said was wrong, and is corrected here.** It claimed the failure report named the guest rather than the clock, and that the distinction was left for the reader to notice. Neither is true: `qemu-libc-gate.py` computes exactly that distinction and prints `runner was still going when the deadline passed`, under a comment that says why -- "a guest that booted and fell short of the markers is a different finding from a runner that never started one". The reporting was already right. **The real defect is a budget.** The deadline was `env.get("XAIOS_LIBC_QEMU_TIMEOUT", "720" if arch == "riscv64" else "180")` -- a literal pair, and the only per-leg budget in the tree not derived through `qemu_gate_lib.smoke_timeout`, which scales by architecture and then by `XAIOS_GATE_TIMEOUT_SCALE`. The workflow sets that scale to **3 for every job**, because a GitHub runner has no hardware virtualisation and interprets every guest, so this leg was given 720 seconds where the project's own convention gives it 2160. `XAIOS_LIBC_QEMU_TIMEOUT` is set nowhere in the repository, so the literals always applied and the declaration never reached this gate. **Fixed by routing the default through the shared helper:** `smoke_timeout(arch, 180)`, which reproduces the old numbers exactly on a machine that declares no scale (720 / 180 / 180) and applies the declared one where it is declared (2160 / 540 / 540), with `XAIOS_LIBC_QEMU_TIMEOUT` still overriding. Verified by evaluating the helper at both scales, and by reading the call sites that show it is the only gate that was not already doing this. **What this does not close:** why the RISC-V leg needed more time at all on that run. The budget defect is real and fixed; the guest's behaviour on that run is not explained by it, and is not claimed either way here. |
| B-105 | The recorded cause of the x86-64 install skip was wrong, and it was recorded as established | x86_64 | The tracker and the gate's own skip text both said the storage-administration window "is opened by MMIO bus slot ... and 5 is the MMIO bus number ... an x86-64 machine has no such slot". It is not, and the window is not unreachable on PCI for want of an address. `virtio_block_open_slot(5U)` reaches `virtio_transport_find_at`, which tries the MMIO window and then falls back to PCI, and PCI **does** carry logical slot 5: `matching_ordinal_for_slot` (`kernel/dev/virtio/virtio_transport_pci.c:155`) maps it to enumeration ordinal 4, the fifth block device in the order the QEMU test bench attaches its disks. The guest's own line says an ordinal, not a missing slot: `virtio-blk-h: no pci device of type 2 at ordinal 4; 2 present`. The fallback is visibly working in the same family of logs on the other architecture -- an AArch64 install run reports `no pci device of type 2 at ordinal 3; 1 present` while looking up window 4 -- so the mechanism was never in doubt once the map was read. **The real defect is narrower and worse:** a device is identified by its *position in one test bench's enumeration*, so a window that exists as a name on every transport resolves only on the machine whose disk order happens to match. Corrected in the gate's skip text and in the `1c` row, with the file and function named rather than an argument made. Recorded as a defect because the previous text claimed the cause was "established rather than guessed" when it had been guessed. |
| B-103 | The RISC-V installed-disk gate never ran, and nothing could make it | riscv64 | Three independent breakages in `tests/scripts/qemu-installed-disk-gate.py`, any one of which stopped it, and no job that ran it: (i) the `riscv64` profile had no `esp_bytes_digits` key, so the script died at import with `KeyError: 'esp_bytes_digits'` and exit 2 -- the key was added to `aarch64` and `x86_64` when the x86-64 profile was written and not to this one; (ii) the profile's build list ran `build-riscv64.sh` and `build-riscv64-image.sh` and stopped there, so the disk builder refused with `missing: build/riscv64-uefi/BOOTRISCV64.EFI` -- the loader comes from a third script, `build-riscv64-boot-media.sh`, because the image builder deliberately builds no UEFI loader ("this board has neither") and an installed disk is booted through firmware; (iii) the profile named its target volume `/dev/vblk1` while `kernel/core/kmain.c` defines `XAIOS_INSTALL_TARGET "/dev/vblk5"` unconditionally, which made both install-phase expectations unmatchable -- a string the kernel can never print. **Nothing ran it:** CI's `installed-disk` job ran the AArch64 variant alone, so two of three profiles of a gate were exercised nowhere and a committed regression had no way to show. **Fixed by removing the guessing rather than correcting it:** the install-target volume is now read out of `kmain.c` instead of restated per architecture, so the two cannot drift again; the RISC-V profile gains the loader step and a measured ESP floor (eight digits, `files=5 bytes=16894192` -- the first draft copied x86-64's seven, which is weaker than AArch64's and was a wrong answer that looked considered); every profile's required keys are checked on every invocation of the gate, against all three architectures rather than the one being run; and `scripts/make-installed-disk.sh` now names the script that actually builds the artifact it reports missing, which for RISC-V was neither the loader's builder nor the kernel's. **Evidence:** before, `make qemu-riscv64-installed-disk-gate` exited 2 at import; after, it builds the disk, boots it twice, and reports a real boot failure (`B-104`) instead of an error about a missing key. **Not yet in CI, and the reason has changed since this row was written.** It said the gate would be red every run until `B-104` was closed; `B-104` and `B-108` are both closed and `make qemu-riscv64-installed-disk-gate` passes twice in a row. What holds it back now is cost: the gate waits out its whole per-boot deadline on every boot, because the guest reaches a login prompt and stays there, so it takes twice that deadline -- about 24 minutes here, and with the host scale the workflow declares it would be over an hour on a runner, inside a job that also runs the smoke, the release gate and the CPU matrix under a 90-minute cap. Putting it in CI is a budget decision rather than a correctness one, and it is owed. |
| B-102 | One format, two chunk-size caps, and the writer had the smaller one | all four | `XAI_FS_MAX_CHUNK_SIZE` in `engine/src/xai_fs_writer.c` was **16 MiB** while `engine/src/xai_fs.c`, `tools/xaios_xai_fs.py` and `docs/MODELFS-FORMAT.md` all said **64 MiB**, so the C writer could neither format nor accept a volume in the range the format documents, the reader opens and the host tool writes. **The cause is a mechanical change that missed one file:** `78bafd1` "raise the chunk cap to where it stops binding" touched the reader, the Python test and `wiki/Filesystem-and-Storage.md`, and the writer's constant was left where it was -- and the reader's own comment argues the higher cap is where the limit stops binding, so the intent was never in doubt. Nothing reported it because no two of the four are ever read together, which is `B-77`'s shape again: a value copied across a boundary with nothing comparing the copies. **Fixed by making the writer say what the format says**, and held by `tests/repository/check-xai-fs-chunk-bounds.py`, which reads all four by name and fails when any one disagrees. Control run: putting `16777216` back in the writer fails it, naming all four values. The bounds are a documented format property and not a per-caller policy, so a caller that wants smaller chunks asks for them; it does not get to redefine the format. |
| B-101 | The published Wiki silently went backwards, and nothing failed | repository | `publish-wiki` copied `wiki/*.md` into a `--depth 1` clone of the Wiki repository and pushed, so whichever run of the job finished last decided what the Wiki said, whatever commit it was for. On 2026-09-13 the run for `023afed0` spent about six hours queued and pushed at **12:54:21Z**, thirty-five seconds *after* the run for `8345bb2` had pushed a newer copy at 12:53:46Z, so the published `Project-Tracker.md` went from 186,340 bytes to 179,190 -- the row for `OD-011` and the corrected `B-100` rate, the output of the previous session's last six commits, removed by a job that reported `success`. **Both runs succeeded and neither said anything**, which is what made it invisible: the Wiki repository's history is the only place it shows, as a commit whose parent already contained the newer page. Found by comparing the published pages against `wiki/` while reading the repository for a session handover, and confirmed by content rather than by argument -- the published `Project-Tracker.md` is byte-identical (md5 `92c7ba7271f0eabde905e54cdd027ae3`) to the repository's copy at `023afed0`, against `4f5bebb0d92cd1e8d6b62c92a485e8d8` at `8345bb2`. Every other one of the 26 pages was already level, so one page and one race. **Fixed in three parts, because one was not enough to be sure.** The job now carries `concurrency: group: publish-wiki`, so two runs cannot write at once; it resolves the tip of `main` with `git ls-remote` and **stands down unless its own commit is still that tip**, which is the actual defect -- publishing is only meaningful for the current commit, and a newer push has a run of its own; and a rejected push is re-applied to the published tip and retried up to three times, because a person can still edit the published pages between the clone and the push. Standing down is not a failure and does not fail the run, which is the correct report: this run is declining to publish something that is no longer current. **The check that would have caught it in a day is `B-101`'s other half.** `tests/repository/check-wiki-parity.py` reads the published Wiki back from the remote and compares every page byte for byte against `wiki/`, and `publish-wiki` runs it after pushing, which is the "post-push byte comparison" risk `R-012` had claimed existed since it was written. It runs with no credential against the public Wiki and with `GITHUB_TOKEN` when there is one; `XAIOS_WIKI_DIR` points it at a checkout so it can be exercised with no network, which is how its controls were run: an identical tree passes, one appended byte fails naming the line, and a deleted page fails naming the page. It is deliberately outside `docs-check`, which runs inside `qemu-core-os-rc` under a 120-second budget and must work with no network. |
| B-80 | Nothing on XAIOS could verify a server certificate | all four | The operator chose a **pinned operator key**, the mechanism `xapt` already uses, over a trust store, and the policy is written in `userspace/wt/src/wt_tls_pin.c`. It holds one key and answers one question, and it is deliberately unable to answer anything else: an unset pin returns `WT_TLS_PIN_NO_PIN`, which is a refusal, so there is no state of the structure that means "accept any key" -- a policy object whose default is trust is one that eventually gets constructed, not configured, and then used. A certificate has to pass **two independent checks**: it must carry the pinned key (`wt_tls_pinned_key_accepts_certificate`, on the leaf, whose key is compared as bytes so that one key has exactly one representation) and its signature over the transcript must verify under that key (`wt_tls_certificate_verify_signature`). Neither alone is sufficient and both are required: a pin by itself accepts a certificate anyone could copy, and a signature check by itself accepts any self-signed key an attacker generated. A mismatch is `unknown_ca` and a missing pin is `handshake_failure`, because "this is not who we pinned" and "we never pinned anyone" are different operational problems. Component setters refuse a modulus with a leading zero byte, an odd-length hex string, a non-hex character and a key that does not fit, and every refusal leaves the pin unset rather than half configured. 80 checks hold the policy, including an EC pin against a real P-256 certificate generated for the purpose. **What a pin does not do** is on the header and in the wiki: it does not authenticate a name, and a deployment that pins gives up being redirected. |
| B-83 | The EncryptedExtensions ALPN answer was read in a format RFC 7301 does not define | all four | `wt_tls_parse_encrypted_extensions` read the server's `application_layer_protocol_negotiation` extension as `protocol_name<1..255>` -- one length byte then the name. RFC 7301 section 3.1 says the extension_data of a ServerHello is "structured the same as described above for the client 'extension_data', except that the 'ProtocolNameList' MUST contain exactly one 'ProtocolName'", so it begins with a **two-byte list length** and a conformant server's answer was refused. It would also have accepted a non-conformant message, so this is a refusal-of-the-right-thing defect and not only a parsing one. It survived four audits and 711 checks because nothing in the repository carried ALPN: RFC 8448's EncryptedExtensions has none, and the only other fixtures were written by hand to match the parser. What found it was generating the QUIC server flight from an independent implementation -- which had first been written to agree with the parser, and did not find it either until it was corrected to the RFC and the two disagreed. The parser is fixed, the flattened form is now refused by a regression test, and the fixture was regenerated. |
| B-81 | The CertificateVerify verifier hashed into a buffer sized for SHA-256 | all four | `wt_tls_certificate_verify_signature` picks its hash from the signature SCHEME and hashed the signed content into `uint8_t digest[WT_TLS_HASH_LEN]` -- 32 bytes, because that constant is the SHA-256 length and the length of everything in the key schedule. The module offers RSA-PSS-RSAE with SHA-384 and SHA-512 and ECDSA P-384 and P-521, which need 48 and 64. A server that answered a CertificateVerify with scheme `0x0805` or `0x0806` made `sha384`/`sha512` write past the local, on the stack, before the signature was examined -- reachable from the wire by an unauthenticated peer. Found by asking what the length actually is rather than what it is called, and demonstrated with AddressSanitizer: `stack-buffer-overflow ... [1328, 1360) 'digest' <== Memory access at offset 1360 overflows this variable`. The buffer is now sized for the largest hash any supported scheme uses, with a build-time assertion that it is, both dispatch paths refuse a hash length that does not fit rather than trusting the vtable's descriptor, and the two longer RSA schemes are driven in the test with a garbage signature -- enough, because the overflow happens while the content is hashed, so the check is the sanitizer and not the return value. The second defect of this class in the port, after the Retry integrity tag's unbounded copy recorded under `B-79`, and the same lesson: a length named after one thing and used for another is a length nobody checked. |
| B-82 | The public key read out of a certificate pointed into a dead stack frame | all four | `wt_tls_certificate_public_key` returned BearSSL's own `br_rsa_public_key` and `br_ec_public_key`, whose `n`, `e` and `q` point into `br_x509_decoder_context`'s `pkey_data` array -- a member of the decoder, which is a local in every function that parses a certificate. So the key it reported was a pointer into a frame that had already returned: a use-after-return that read whatever the next call left there, and that read as a correct key whenever nothing had overwritten it. The test that existed passed for that reason; sweeping the stack showed the modulus changing. Found while building the pinned-key policy (`B-80`), which is the first caller that had to keep a key across a call. A public key is now copied into storage it owns, sized to BearSSL's own key buffer bound (`BR_X509_BUFSIZE_KEY`), and the test sweeps the stack and requires the modulus not to move, and requires a key rebuilt from the operator's bytes and from hex to equal the one read out of the certificate. |
| B-75 | The kernel's answer to `net_open_udp` reached every field except the one that carried it | all four | The port is the point of the call, and the first version wrote it back into a field of the request structure the caller had passed. The caller read zero, every time, for the ephemeral case only -- and the reason is worth keeping: the value written was `request.port`, read out of the caller's own buffer, and the compiler was within its rights to forward the value it had already loaded from there before the call. Nothing is what it looks like from the call site. Two things came out of it. The answer now travels through an out-pointer of its own, `out_port`, which is the convention every other output in that structure already follows (`out_sockfd`, `addr_out_ptr`, and `NET_ACCEPT`'s own `port`) and which leaves the request read-once. And because the defect was really that two independently-written struct definitions have to agree field for field with nothing checking them, `tests/repository/check-syscall-abi.py` now compares them by name and order in `docs-check`. Both controls run: dropping `out_sockfd` from the kernel header fails it, and reordering two fields fails it. Held by `make qemu-datagram-ephemeral-port-gate`, which reads the kernel's allocation log and what the caller was told and requires the two to agree; with the field-based write-back restored the gate is red on exactly `first_port is zero`. |
| B-76 | XAIOS had no way to open a datagram socket with a port the kernel chose | all four | `NET_LISTEN` refuses port zero, and `net_bind_udp`/`net_sendto` cannot be used without naming a port first, so a QUIC client had no way to exist: it has nothing to send from and no address a peer could reply to. That is the whole of the WebTransport C99 port's client side, and it was blocking `WT-35` rather than being part of it. `XAIOS_SYSCALL_NET_OPEN_UDP` (55) binds a datagram socket from the dynamic range, reports the port through `out_port`, registers it so a reply can be routed back and releases it on close. A port the caller did not name is never handed to a second open while the first holder is open -- `kernel_socket_alloc` does not check for one, so without that the two descriptors would share a port and the reply lookup, which stops at the first match, would leave the second one unreachable and silent. The counter's wrap was wrong in the same way the existing TCP path's is: `uint16_t` cannot hold 65536, so the value after 65535 is zero, and a reset written as "below the range start" never fires for it -- one draw in 16384 returned port zero and reported "no port" on a machine with 16383 free. Both paths now share one draw, and only the datagram path asks about collisions, because a connected descriptor's `port` holds the *peer's* port and cannot be read as a set of local ones. The ceiling this creates is `B-78`. Held by `make qemu-datagram-ephemeral-port-gate`, which also runs in CI's `build-and-smoke` job. |
| B-59 | A machine with no entropy source panicked at boot, in the self-test that exists to prove it refuses gracefully | all four | `entropy_is_production_grade()` requires both a good source label and a pool that was actually seeded. `entropy_swap_source_for_test()` moves the label and nothing else, so the `admin-control` self-test swapped in `DEVICE_RNG`, asserted the host-key gate would then return OK, and on a machine with no random device the pool is never seeded -- the gate correctly refused, and `admin_control.c:1278` killed the machine. The refusal logic was right; the self-test asserted something that cannot be true on the one configuration `F-05` exists to describe. `make qemu-docker-no-rng-suite` starts exactly such a machine on purpose, which is why only that leg died, and it took `Linux OpenSSH Interoperability` red from 2026-09-06. **This is a real machine's behaviour, not a CI artefact:** any host with no RNG would have done the same. **Fixed** by asserting what is true in each configuration -- both halves where the pool is seeded, and where it is not, that a production-grade *label* is refused too -- and saying so on the console rather than skipping quietly, because a machine with no entropy is supposed to say so. **Evidence, both ways on one machine:** before, a no-RNG boot halts at `Line: 1278` after 70,297 bytes of console; after, it reaches `admin-control: self-test passed` and 269,830 bytes with no panic. SSH stays withheld either way, which is correct. |
| B-58 | Two CI jobs were missing packages the work they run requires | repository | `hosted-engine` installs `clang python3 python3-cryptography` and runs `make hosted-test`, which since `dc290bf3` cross-checks the FAT volume XAIOS writes against `mdir`. With no mtools the job failed `/bin/sh: 2: mdir: not found` and then `fat: mtools cannot see BOOTRISCV64.EFI on the volume XAIOS wrote` -- which reads as a filesystem defect and is a missing package. `core-os-release-candidate` drives `qemu-core-os-rc`, whose `qemu-nvme-gate` and `qemu-outbound-fragmentation-gate` both name `riscv64` as a prerequisite, and installed no RISC-V emulator; the pinned QEMU it builds is `qemu-system-aarch64` only. That never surfaced as a missing binary because the RISC-V runner died earlier on `B-56`. Both package lists corrected. **Not visible from this machine:** every one of these runs on a Mac where the tools are present, which is the same reason `B-57` survived nine days. |
| B-57 | `dd bs=1m` works on a Mac and fails on Linux, and CI is Linux | repository | BSD `dd` takes lowercase size suffixes; GNU `dd` stops with `dd: invalid number: '1m'`. `scripts/build-riscv64-boot-media.sh` created its boot medium that way, so on CI it failed before writing anything -- and the log showed `qemu-libc-gate` reporting a guest that had not booted, because the gate could only say the image build returned non-zero. Three other unguarded uses existed, two of them on paths Linux runs. Every one now uses a plain byte count, which needs no suffix and cannot be ambiguous; `write-usb.sh` keeps its lowercase spelling because the line after it retries with the uppercase one, which is the deliberate pattern for a script that runs on whichever machine is holding the stick. `tests/repository/check-portable-dd.py` requires that, allows the guarded retry, and fails on the exact line that broke CI when the old spelling is put back. |
| B-56 | Every RISC-V boot in CI failed at a missing AArch64 artifact, and was reported as a guest that would not boot | repository | `run-qemu-riscv64.sh` seeds the guest's `/models` by copying `build/xaios-xaifs.img`, which is produced by `scripts/build-image.sh` -- the AArch64 and x86_64 builder. Neither RISC-V build script made it, so on any machine building RISC-V alone the runner died at `cp` under `set -e` before QEMU started. CI's RISC-V job builds only RISC-V; this tree works only because a developer here builds everything. **The reporting was the same fault as `B-51`:** the gate printed `missing targets: ['spinlock: early single-core try-lock self-test passed', ...]`, blaming the guest's boot for a host file that was never built. The guest had printed nothing because it had never been started. It also took both RISC-V legs of `qemu-nvme-gate` down, which reported every marker missing and empty metrics for the same reason. **Fixed:** `build-riscv64-image.sh` creates the fixture with the same standalone builder the other architectures use, so the RISC-V build produces everything the RISC-V runner boots; and the runner names the missing file and the command that makes it rather than dying at `cp`. **It took two passes, and the first one was the instructive failure.** Fixing the models volume alone moved the `cp` one line down: the next CI run died on `build/xaios-riscv64-system.img`, the signed A/B volume, which only `build-riscv64-boot-media.sh` produced. The verification was at fault as much as the fix -- it removed the single file just repaired, which tests the repair and not the situation. The runner's dependencies were then enumerated rather than guessed: five build artefacts, of which the kernel and initfs were already built here, the storage-admin volume has a `dd` fallback, and the boot medium is optional. Both missing volumes are now produced by `build-riscv64-image.sh`, and the runner names either one rather than dying at `cp`. **Evidence:** with `build/` emptied completely -- which is what a fresh runner has and what a developer's machine never has -- `build-riscv64.sh`, `build-riscv64-image.sh` and `qemu-smoke.py --arch riscv64` all pass in sequence. |
| B-49 | A data sector number truncated above 32 MiB | all four | `absolute_data_sector` took a `uint16_t`, which stops at 65535. Block 65536 is 32 MiB into the data region and a v6 volume is allowed a gibibyte, so every caller -- all of which pass a 64-bit block number out of an extent -- had its argument truncated at the call site. A read or a write past that point went to a sector near the start of the region instead: no error, no short count, the wrong bytes at the wrong place. Not reachable by the v5 volumes anything currently boots, which is why it survived; v6 raised the ceiling and made it reachable. The parameter is now `uint64_t`, which is what every caller already had. The 16-bit block numbers elsewhere in the file are the legacy v3/v4/v5 on-disk node formats, where 16 bits is the format and v5's 8192 sectors are well inside it. **Evidence:** `tests/storage/test_xaiboot_fs_large_volume.c` writes 140 files of 256 KiB -- 35 MiB, past the boundary with room on either side -- and reads every one back after all of them are written, which is the pass that matters because a wrapped write damages a file written earlier. Against the previous code the read fails outright. |
| B-54 | The release-image gate passed a row that booted a kernel from the previous build | repository | `boot_vz` handed the harness a stale A/B system volume, and the loader prefers a verified slot over the kernel on the medium -- so the guest printed `XAIOS Build 5 kernel starting` during a test of build 6's image while the gate reported success. The QEMU path sets `XAIOS_SYSTEM_VOLUME_IMAGE=none` for exactly this reason, three lines above the call. Nothing noticed because the marker was `XAIOS Build \d+ kernel starting`, which any build satisfies. The two system volumes are dropped from the harness call and the marker now names the current build. |
| B-53 | Nothing stopped a packaging script from shipping a kernel built to fault on purpose | repository | `make qemu-fault-matrix` compiles three deliberately halting kernels into the same `build/kernel*/kernel.elf` every packaging script reads, and restores a normal image only if it finishes. A netboot binary was built during one, looked like a release binary in every checkable respect, and was caught only by `boot-media-gate` booting it. The kernel now writes a marker into its own bytes under the same `#if`, and `build-arch-image.sh` and `build-netboot-image.sh` both refuse a kernel carrying it without booting anything. `check-fault-test-marker.py` keeps the three copies of that string in step, because a grep that can no longer match is a check that cannot fail. |
| B-52 | A machine that had run long enough stopped booting | all four | `allocate_extents` took the first sixteen free runs in address order, and the low blocks of a volume are the most broken up, so it collected fragments out of the rubble at the bottom and refused the write with the volume two-thirds empty. The same path snapshots a file, and the boot-time update self-test stages a snapshot and asserts it worked -- so the machine halted in the cyan screen with `fsck` calling the volume sound. Blocks are now taken longest run first, which is the best any policy can do against a sixteen-extent limit. Closed by `tests/storage/test_xaiboot_fs_fragmentation.c`, which fails against the old allocator, and by the machine's own 64-boot volume going from `commit = -4` to `commit = 0`. The ceiling itself is `B-55`. |
| B-51 | `qemu-fault-matrix` booted the machine's accumulated state, and said the wrong thing when that state failed it | repository | It was the only gate in `qemu-readiness-gate` booting `build/xaios-persistent.img` unisolated; some twenty siblings pass `persistent=` and reset it. On a volume 64 boots deep the guest panicked in `update_stage()` before any fault was injected, and the gate printed `missing targets: [...]` -- an accusation about fault handling for a condition the host caused. It would have said the same guest-shaped sentence with no guest at all. Each scenario now gets its own volume, and a boot that never reaches the injector reports `INCONCLUSIVE, not a guest fault-handling defect` with the reason and exits 2, distinct from the 1 a real failure returns. Proven both ways on one machine and one commit. |
| B-01 | Outbound ProxyJump failed host key verification | x86_64 builds | `known_hosts` was read through a single 4 KiB buffer, so a host whose entry lay past it looked like a host with no entry. The full FreeBSD bidirectional suite now passes under emulation, 19 of 19. |
| B-03 | `vmnet-helper` spun a core while idle | Virtualization.framework | It returned without reading the packet that woke it, so vmnet's event never cleared. It now reads first and drops the frame if there is nowhere to send it: 20 ms of CPU over 60 idle seconds, measured on the rebuilt binary. |
| B-04 | Fusion intermittently got no DHCP offer | VMware Fusion ARM64 | One hard-coded transaction id on every boot, and a budget split evenly between OFFER and ACK that gave the first phase only three attempts. The budget now goes to the phase that needs patience; total raised 15 s to 30 s, paid only where no server answers. |
| B-05 | One fixed kernel link address blocked a 1 GiB profile | all four | The AArch64 kernel is built position-independent and the loader applies its 384 relative relocations wherever firmware places it. x86-64 stays fixed-address by necessity. `make qemu-memory-matrix` boots all three at 1024 MiB. |
| B-06 | Virtualization.framework booted to nothing below ~3.5 GiB | Virtualization.framework | It was B-05: a kernel fixed at `0x90000000` on a platform with no memory there until it has enough. `vz-gate` reads `XAIOS_VZ_MEMORY_MIB` and passes at 1024, 2048 and 4096 MiB -- it had been pinned at the one value where the defect cannot occur. |
| B-07 | Applications were never run on two of the four images | Fusion, Virtualization.framework | Only QEMU built with `XAIOS_BOOT_TEST_APPS=1`, so both hypervisor gates checked kernel markers a guest can satisfy while every program a person would type is broken. Both now build the applications and assert eight application markers per boot. |
| B-08 | An unclean-boot marker put the guest into rescue mode | all four | The shared durable image accumulated hard power-offs until the rescue marker latched. `vz-gate` generates a fresh volume per run and both hypervisor gates treat `rescue=1` as a fault. The latching itself is by design -- see [[Operations and Recovery\|Operations-and-Recovery]]. |
| B-11 | Userspace and the identity map were the same addresses | all four | Userspace began at 4 GiB, inside the kernel's own identity map, so a 4 GiB machine handed the kernel's memory to userspace. The window moved and `vmm_init` caps the identity map at `XAIOS_USER_BASE`. Two years of 2 GiB gates could not have seen it. |
| B-12 | Fusion faulted on the firmware framebuffer at 4 GiB | VMware Fusion ARM64 | Firmware places the framebuffer above RAM, so at 4 GiB it lands outside what the kernel identity-maps. `kmain` maps it from `boot->framebuffer_base` straight after `vmm_init`. `XAIOS_FUSION_MEMSIZE` exists so the gate can run at the size that shows it. |
| B-14 | The x86_64 guest wrote to the medium it booted from | QEMU x86_64 | The runner attached the boot drive writable where AArch64 attaches it read-only. The self-test now asks whether the device advertises `VIRTIO_BLK_F_RO` and verifies the refusal; `make qemu-readonly-medium-gate` exercises that branch, which had never executed. |
| B-15 | An intermittent fatal assertion on VMware Fusion | VMware Fusion ARM64 | One occurrence, never repeated in 217 boots. The remedy this row first claimed did not work: subtracting the load base alone is not enough and the kernel carries no DWARF. The panic screen now names `tests/scripts/resolve-panic.py`, which does the arithmetic against the symbol table. |
| B-19 | The relocated kernel was placed without its required alignment | all four | The loader took firmware's page-aligned address while the kernel's segments declare `p_align 0x10000`; landing 4 KiB off makes the SMMU reject its stream table. The loader now rounds to the strongest `p_align`, and `offset_in_64k` is a required marker in three gates. |
| B-23 | A v6 volume sometimes did not come back from a power cut | all four | The mirror fallback called `set_active_v5()`, so a v6 volume with a torn primary validated whatever data block sat at the v5 mirror offset. It now searches the layouts it knows. Proven by `tests/storage/test_xaiboot_fs_v6.c` with a negative control; the CI failure itself was never reproduced locally. |
| B-24 | Renaming a directory on a v6 volume wrote 191 KiB past a static array | all four | `g_path_transaction` was still declared at the v5 node count while the walk used the v6 one. Fixed by sizing rather than clamping, with a compile-time assertion and a boot-time self-test so a future maximum cannot reintroduce it. Found while fixing B-23; nothing reported it. |
| B-25 | After reverting a Fusion snapshot the guest refused every command | VMware Fusion ARM64 | sshd freed a remote-login session context only after a command succeeded, while every other path allocated one -- and socket handles are never reused. Deterministic at 64 connections, not intermittent. The table now evicts least-recently-used and `make qemu-ssh-session-exhaustion-gate` holds it. |
| B-26 | `qemu-docker-network-suite` failed its native xtop check | all four | Three stale markers and one real defect: `\x1b[42;30m` is the basic-colour form xtop stopped emitting when it moved into the screen framework, and the help title reaches the stream only as changed cells. |
| B-27 | A full-screen program's terminal restore was truncated | all four | A length constant beside a literal had drifted -- 24 written for a 29-byte string -- so five bytes never left the program. Every escape length in the serve path now comes from `sizeof` of a named string. |
| B-29 | A userspace UDP socket could not send | all four | `XAIOS_SYSCALL_NET_SEND` required a flow, and for UDP only an *inbound* datagram ever created one, so a bound socket that had received nothing could not speak first. `network_stack_udp_sendto` creates the flow and transmits; `/bin/netmqtest` was the first program in the tree to try. |
| B-31 | `qemu-libc-gate` booted a RISC-V image its dependency never built | all four | The make dependency built AArch64 and x86-64 while the gate iterated three architectures, so the RISC-V leg booted whatever `build/` held. Shown rather than argued: with `kmain.c` edited so it no longer emits the marker the gate requires, the old gate reported `PASS: hosted runtime executed on riscv64` with the kernel byte-identical afterwards, and the new one rebuilt it and failed on exactly the five `exit_code=` markers that format string produces. Same command, same tree, opposite verdicts. The gate now builds every image it boots from one table that is both the list built and the list booted. Fixing it found RISC-V needs a third build step -- the signed A/B system volume, which the runner copies into its boot state and which carries a kernel the loader prefers -- that no libc target ran, including the one documented as building its own image. |
| B-02 | Thread join failed under load, twice | all four | `user_thread_worker` cleared the CPU to the kernel unconditionally, so a worker entered from inside a process's own `xaios_thread_join` left the outer syscall with no current process and the kernel's address space. It now saves and restores the CPU's process, address space and thread slot. The window is reached deliberately by `/bin/joinnest`: a process cannot pin a thread to its own CPU -- `select_user_cpu` refuses that -- so a helper takes a worker CPU, a victim is pinned to the same CPU (the pinned path never asks whether it is busy), and the helper joins the victim, which only returns by running it nested with a user process bound. `make qemu-thread-join-soak` is green on all three architectures and red with the fix reverted, ending at `join lost its process context id=17 owner=11 cpu=1 before=11 after=0`. Whether that defect produced the two original sightings is still inference; what changed is that a recurrence now fails a gate instead of being noticed twice and explained neither time. |
| B-28 | An SSH session is refused once in several hundred under sustained load | VMware Fusion ARM64 | The refusal was the per-address accept-rate limit: 120 per 60 seconds, and a soak round opens two connections -- the transfer and the status probe after it -- so round 61 carried connection 121 and was closed before a byte of SSH was spoken. `make qemu-ssh-connection-rate-gate` reproduces it deterministically and is red without the fix. Authentication now credits the connection back, since the limit exists to bound a peer that has proved nothing. Confirmed on Fusion at 4.2x the original exposure: **2463 rounds, 1.29 GB, and not one refusal on accept** -- zero rate-limit, capacity or slot-exhaustion markers in the whole run. It was invisible for two years because all three pre-serve refusal paths reported somewhere nobody reads; each now names its reason with a counter. The row conflated two signatures, and the other one is `B-38`. |
| B-34 | `dns_self_test` reported three things it did not test | all four | It printed `dnssec=local-chain tcp-fallback=enabled aaaa=enabled` while exercising only name encoding, decoding and the cache. It now exercises them: walks a committed signed chain (`kernel/net/dns_selftest_chain.h`, regenerable from `generate-dnssec-fixture.py --target kernel`), rejects a forged and an expired signature, validates an AAAA RRset and refuses an A answer offered as AAAA, drives truncation in both directions, and checks both deadlines. It reads no wall clock and transmits nothing, so it runs on a machine with no NIC at boot. Control run here rather than taken on report: with `dnssec_verify_address` accepting everything the boot halts at the address assertion. |
| B-35 | One deadline covered a whole DNSSEC chain | all four | `started_ns` was stamped once per resolve and never by `start_query`, so the root-DNSKEY, DS, child-DNSKEY and answer queries shared a single 15 s budget against a 5 s retransmit timer -- a longer chain spent it and returned a cancellation a caller could not tell from a refusal. Split into `query_started_ns` (per query, a new field rather than a reuse of `sent_ns`, which a retransmit moves) and `walk_started_ns` with a stated 45 s walk budget. Per-query alone would have left a deep name composing `2N+2` timers into a bound nobody wrote down, which is the same defect in a longer form. Both expirations complete as `XAIOS_ERR_CANCELLED` and print `dnssec-timeout`. |
| B-36 | A malformed argument was reported as a DNSSEC failure | all four | `XAIOS_ERR_INVALID` meant both "bad arguments" and "the chain did not validate", and both printed `dnssec-unverified`; `qemu-operations-closure` accepted that string, so a malformed invocation read there as a fail-closed resolver. `nslookup` now validates its own arguments first and `dnssec-unverified` is reserved for a chain that was walked and refused. A fourth case the row missed: a hostname of 64 bytes or more also returned `XAIOS_ERR_INVALID` from the resolver, so `XAIOS_DNS_MAX_HOSTNAME` is published and the shell checks length itself. The gate now rejects all five malformed forms and runs a well-formed lookup afterwards, so it cannot be satisfied by refusing every `nslookup`. |
| B-37 | The outbound SSH client could not run without a terminal | all four | It prompted for the identity file's passphrase on every invocation, including for a key that has none, and `-o` was not a recognised token at all -- so with no PTY it printed `key passphrase:` and blocked indefinitely, measured at 45 s and still going. Three changes: the key is read before deciding, by the loader itself rather than a second parser, so a plain key is never asked about; `-o BatchMode=yes` is understood on both `ssh` and `scp` and **any other `-o` is refused rather than ignored**, since a silently dropped option is a false pass; and because the app cannot tell whether it has a PTY, a 60 s bound on *silence* at the prompt ends an unattended one while every keystroke restarts it, so a person mid-passphrase is never cut off. `make qemu-outbound-batch-mode-gate` holds all nine cases. Two images, because a private key reaches the guest only by being packed in: the kernel refuses any runtime write whose bytes contain `"BEGIN "`, confirmed as policy by bisection rather than assumed. |
| B-33 | Two aggregate gates certified a `printf` for the DNSSEC path | all four | Under `XAIOS_BOOT_TEST_APPS` -- the configuration every QEMU gate uses -- `nettest` had two bare log calls with no code behind them, and the row recorded one gate requiring them. It was two: `qemu-smoke` required both strings, and `qemu-core-os-rc`'s `userspace_dns` capability required the second under a comment claiming "nettest drives the deterministic DNS fixture". Fixed by making the claim true rather than renaming it: the resolver answers `selftest` and `forged.selftest` from the committed chain, boot-test builds only, and `nettest` resolves both for A and AAAA, compares them to the fixture's own addresses, requires the tampered zone to be refused, and requires the repeat to come from cache. When the forged zone does validate the kernel returns the success to userspace rather than swallowing it, so userspace is what calls it a failure. Both marker strings kept byte-identical, so neither gate needed editing and both comments are now true. |
| B-42 | The DNSSEC self-test fixture and its anchors ship in release images | all four | Kept, deliberately, and the reason it can be kept was made checkable. Measured against the same tree with the self-test compiled out: 7,409 bytes of `dns.o` -- 3,008 code, 1,445 fixture chain, and 2,951 of `kassert` and klog strings, so the fixture is not even the largest part -- and 4,640 bytes of `kernel.elf`, 0.44%, with no change to the fixed 64 MiB boot image or the initfs. Guarding it would take release DNSSEC coverage to zero: `kassert` is never compiled out, no gate anywhere greps the marker, and B-33's fixture zone is test-only, so this walk is the only DNSSEC verification a release machine performs. **The real exposure was elsewhere:** that the IANA anchors are restored was true, unasserted and unobservable -- a release image prints no klog to serial, so a surviving test anchor would have shipped silently. The self-test now reads the anchor table after restoring it and panics on the test tag, `dns_configure` logs the anchors in force, and a release boot shows `dns: trust anchors count=2 tags=20326,38696` through `xaiosctl logs --component dns`. Control run here: removing the restore halts the boot at `assertion failed: anchor_count == XAIOS_DNSSEC_MAX_ANCHORS`. Two corrections to this row as I filed it -- `production-source-audit` only rejects unfinished-work markers and says nothing about test data in a shipping binary, and the boot image is fixed-size so it can never move. |
| B-40 | `send_all` could freeze the whole server for as long as a peer trickled | all four | The 10 s bound measured the *gap* between bytes, and `stalled_since` reset on any byte taken, so a peer accepting a slice inside each window renewed it forever. Worse, when the bound did fire it only abandoned the packet -- nothing closed the channel, so the next tick retried the same write and held the single-threaded server another 10 s. Replaced with a rate: in each window the peer must take at least 10 KiB, body and MAC sharing one allowance so it cannot get a fresh one mid-packet. 1 KiB/s is below a 9.6 kbit/s data call, and the trade is stated in `sshd.h` -- a peer slower than that loses its connection rather than every other session losing the server. An aborted transmit is marked silent, or the cure spends a second window writing a disconnect into a socket that just refused a packet. Held by `make qemu-sshd-transmit-rate-gate`: a peer at 641 B/s stops being served after 102 s while 144 ordinary sessions run, and a peer that keeps up moves 14.5 MB untouched. |
| B-41 | A failing channel was logged, never closed, and starved the ones behind it | all four | The row named `sshd.c:3199`; that line logs and continues. The early `return` was inside `ssh_channel_tick()` in `ssh_channel.c`, on the first channel whose `screen_flush` failed -- leaving it active and skipping every later channel. The consequence the row described was right, the location was not. The per-channel body is now its own function returning OK, channel-failed or transport-failed, and the loop closes the failing channel and continues; a forwarded connection whose far end went away gets EOF and CHANNEL_CLOSE and leaves the session healthy. Each closure prints its reason on the console, because `ssh_log` writes only to the audit file on the durable volume, which is the same blind spot that hid B-28. Held by `make qemu-sshd-channel-starvation-gate`: a second session's forward carried 40 lines in the 20 s after another session's forward failed, against 0 before the fix. |
| B-38 | An SFTP session stalls when the file contains the alternate-screen sequence | all four | `ssh_channel_send_data` ran every channel's bytes through the screen framework's filter, which watches for `ESC [ ? 1 0 4 9 h` and then paints rather than forwards. SFTP carries arbitrary binary, so a file containing those eight bytes had its payload eaten mid-transfer and the length-prefixed framing desynchronised; forwards and agent channels had the same exposure. Fixed by gating on what the channel is, held by `make qemu-sftp-binary-passthrough-gate`, red without the fix with its control payload passing in the same run. **The leak this row alleged does not exist:** a 1658-round soak, idled 330 s past the 300 s reclaim and then tallied, counted 3316 accepts against 3317 closes -- nothing outstanding. The missing `net_close` that suggested orphaning was a 1.4 s console window looking at a 300 s timeout. **The ~19 s stall is a separate defect and is `B-43`:** it recurred after this fix, and the soak payload provably cannot contain `ESC[`, so it was never this. |
| B-46 | A full-size SSH packet would be refused rather than short-written | all four | `SSH_MAX_PACKET_SIZE` is 35000 and the network syscalls refuse anything over `SOCKET_BUFFER_SIZE`, 16384. Three places that chunk a transfer clamped to `SSH_MAX_PACKET_SIZE - 9` -- 34991 -- so a chunk that size came back `net-send-denied` rather than short-written: the transfer fails instead of taking longer. Unreachable today because `SSH_CHANNEL_MAX_PACKET` is 10240, which is what made it a cliff waiting for the day someone raised the channel maximum for a good reason. The three sites now clamp to `SSH_WIRE_MAX_CHUNK`, and that day is guarded twice: `ssh_channel.h` fails the build if the channel maximum no longer fits -- the same `typedef char x[(cond)?1:-1]` guard `xaiboot_fs.c` uses against a future node count -- and `check-ssh-wire-bound.py`, in `docs-check`, fails if the chunk and the kernel's buffer size drift apart. The literal is written in userspace because `compile-check`'s userspace pass has no `-Ikernel/include` and `userspace/include/xaios/` holds only `types.h`; a number copied across that boundary is how these two disagreed, so the check reads both files. Both controls run: setting the chunk back to 34991 fails the check, and raising the channel maximum to 20480 fails the check and the build. |
| B-44 | sshd is the machine's network thread, and nothing said so | all four | Kept deliberately, and the silence fixed. `kmain` disables preemption and calls `timer_disable()` **before** starting sshd, so sshd is not merely the only network process -- it is the only thing on the boot CPU. A timer cadence would need the stack made interrupt-safe (`xaios_reentrant_lock` identifies its holder by CPU id and its own header forbids taking it from interrupt context) *and* preemption restored, which is a change to the machine's execution model; `network_poll_tick_locked` also begins with `operations_tick()`, which flushes block devices and can call `arch_power_off()`. A dedicated thread costs one of three workers permanently, because the scheduler runs one thread to completion per worker CPU. What could not be defended was that the coupling was invisible. The stack now measures the gap between consecutive polls whenever a listener is registered -- never otherwise, since a metric that fires on an idle machine is one nobody reads -- and prints past one second. Measured here: **55-93 ms idle, 164-297 ms under load, zero gaps past a second**, which is consistent with B-43 not reproducing in 1281 connections. **On the CI runner that bound does not hold, and the difference is what B-43 and B-63 have been.** The Debian 13 interoperability job on 2026-09-12 recorded `network: longest gap between polls us=9069149` and `us=8939957` -- nine seconds -- against a next-largest gap of 143 ms in the same run. Each matches a `service loop stalled ms=9101 phase=connections` and `ms=8976` to within 30 ms, and each is followed by `connection closed reason=packet-read-failed` with a `held_ms` equal to the stall. Three independent measurements of the same outage, from the network stack, from sshd's own loop timer, and from the connection's lifetime. The coupling this row documents as costed and understood is therefore **bounded only by whatever else the loop or the host does**, and on a loaded runner that is seconds. The measurement in this row was honest and was taken on the wrong machine. Documented in `wiki/Architecture.md`, `kernel/README.md` and `docs/NETWORK-SSH-STATUS.md` with the costed alternatives. |
| B-47 | A full socket map accepted the connection and dropped it silently | all four | **The row said this was unreachable because the map is sized to the flow table. That was wrong.** A row is keyed by *descriptor*, not flow, and `release_tcp_flow` never clears one where `release_udp_flow` does -- so a TCP row outlives its flow, and occupancy is open mapped descriptors, bounded by the kernel socket table's 256 minimum against the map's 160 rows. A process holding descriptors whose flows have died fills the map with an empty flow table; sizing was never the protection. `net_accept` and `net_connect` now refuse rather than returning a descriptor with no flow, UDP recv and sendto log because the datagram is already delivered, and exhaustion prints once then every 64th so a retry loop cannot flood the console. `make qemu-socket-flow-map-gate` fills all 160 rows through the kernel's own self-test -- it cannot be filled from outside -- takes the refusal, and **checks that re-mapping an existing descriptor still succeeds on a full table**, which is the failure a naive refusal would introduce. Red with the defect restored: the guest halts before the login prompt. |
| B-45 | Every 32-byte audit append rewrote the whole log file | all four | `xaiboot_fs_write_fd_locked` read an entire file and wrote it back for each append, so `ssh_log` cost **7.05 whole-file read-modify-writes per connection**, and `load_authorized_keys` re-read the key file on every publickey attempt. Both sit inside sshd's loop, which by `B-44` is the machine's network thread. Fixed: FNV-1a is a resumable fold, so a node's recorded hash is a position in its own hash and an append needs one tail-sector read, one write per touched block, an incremental hash and the metadata commit -- the file is never read and never restaged. Anything the fast path cannot do falls back to the unchanged whole-file path, so the set of writes that succeed is identical. Measured over 40 connections: rewrites 7.05 to **0**, file blocks 47.8 to 0, key-file reads 2.0 to 0. Crash consistency is preserved rather than traded: exactly one block holding committed bytes is touched and only above `size`, so either version hashes to the committed hash. The boot self-test proves 200 appends took the fast path with zero file reads, a mid-file write falls back, a write past `write_limit()` is refused, and a volume filled to its last block refuses an append and leaves the file unchanged. |
| B-30 | `operations-closure` blamed the wrong boot for a missing unclean record | all four | The gate killed boot 1 once it reached SSH and required boot 2 to notice. When it failed with `unclean_boots=0 boots=1` this row blamed a durability race, on the reasoning that the failing run took 72.9 s against 241.9 s so the boot must have been killed sooner. Both halves were wrong. The timing is an artefact of `run_step` measuring the whole step, which aborts after 2 of ~9 boots on failure -- the short run is the consequence, not the cause. And the record is durable at the 52% stage, hundreds of lines before sshd starts, so program order rules the race out. The clue was `boots=1` and nobody read it: `g_boots` starts at 1 and rises only from a record found on disk, so boot 2 found *no record at all* and the fault was in boot 1, which had reached SSH without ever writing one. Reproduced deliberately by giving boot 1 a state volume the kernel refuses, which falls back to memory and reaches SSH just as fast. Boot 1 now waits for the guest's own durability verdict -- `durable`, `volatile`, `unwritten` or `absent`, on the console rather than via klog, which release builds suppress -- and `operations_init` takes a `durable_storage` argument so a memory-backed record cannot report itself durable. |
| B-32 | The CI permissions check could not see a job-level override | repository | It matched the workflow's top-level `permissions:` block and stopped at `jobs:`, so a job could grant itself anything and the check still reported the boundary as read-only. It now parses the workflow, computes each job's effective permissions under GitHub's replacement semantics, and requires every elevation above the default to be named in an allowlist inside the check -- a stale entry fails too. It parses only the subset this workflow uses and refuses to vouch for anything it cannot read, rather than passing. |

## Open problems and recommended refactors

Two problems are open and one is closed. Each is written here with what was
tried, what did not work, and what would actually close it, because two of the
three have already cost more in method than in engineering. **`P-1` is the one
that closed**, and it closed by the refactor it recommended: `OD-011` chose the
timer, stage three-b landed it, and the worst gap between polls fell from 55-93
ms idle and 164-297 ms under load to 24 ms idle and 29 ms under load.

### P-1 — A connection is dropped whenever sshd's loop blocks

Covers `B-43`, `B-63`, and the structural half of `B-44`.

**Mechanism, established, and since closed.** The network stack was polled only
from sshd's service loop. While that loop is inside any blocking call, nothing processes packets:
the peer waits, gives up at its own patience (about 18s for the macOS client),
and the connection surfaces much later as `packet-read-failed` or
`auth-timeout`. Three independent clocks agree on each outage -- `network:
longest gap between polls us=9069149` from the stack, `service loop stalled
ms=9101 phase=connections` from sshd, and `held_ms=9333` from the connection --
against a next-largest gap of 143 ms in the same run.

**What blocked the loop.** `ssh_log()` wrote every line straight to the durable
volume, and a xaibootfs append carries `blk_flush()`, which is a virtio flush and
therefore a host fsync. A connection produces about five log lines, so sshd paid
five host fsyncs per connection inside the only loop that moves packets.

**What was tried, and what it was worth:**

| Attempt | Outcome |
|---|---|
| Three Fusion load soaks, ~26,000 round trips | Soak 1 reproduced three times; soaks 2 and 3 reproduced nothing. As a *diagnostic* this failed -- the mechanism was stated in a comment directly above the offending lines. |
| Following stranded sockets by number rather than by clock | Worked. The close lands 30-120s late, so a per-round window attributes it to the wrong round. This is why the row read "the guest never saw it" for so long. |
| A kernel line at flow release when `rx_unread > 0` or `packets_rx == 0` | Worked, and fired on CI rather than in the soak. Silent on healthy traffic: 0 of 280 releases printed. |
| `durable_ms` on the stall line, per pass | Worked. At millisecond scale the durable write is ~90% of every stall this machine produces. |
| Buffering audit records; draining only when half full | Worked. Five fsyncs per connection became **0.041**, and blocking time for 121 connections fell from 1324 ms to 89 ms. |

**Ruled out, each of which would have been a reasonable place to start:** the
socket-to-flow map never ran out (`B-47`'s mechanism -- the one exhaustion line
in a 33 MB console is its own self-test); sshd's loop was not asleep (it waits at
most 50 ms and accepts four per pass); the server was not wedged (a `DEBUG3`
probe connected in 0.08s immediately after each failure); there was no drop or
retransmit storm (one `retransmit=6` line in the whole console, early, nowhere
near a failure); and the guest was not being descheduled by a loaded host (zero
wait overruns in a run with three multi-second stalls, and that wait is the
longest single call in the loop).

**Closed, by the refactor this section asked for.** Everything above narrowed
the window and none of it closed it, so the poll was made independent of sshd's
loop: `OD-011` chose the timer and stage three-b landed it, and the worst gap
between polls fell from **55-93 ms idle and 164-297 ms under load to 24 ms idle
and 29 ms under load**, with several thousand polls per run now taken from the
timer interrupt instead of from a syscall. One fsync can still stall for
seconds, but it no longer takes the network with it. **What that does not
settle:** a stall long enough to outlast the peer's own patience still ends the
connection, and `B-43` and `B-63` were never reproduced on demand, so this
records a window that is bounded rather than one that is gone. The full finding,
including the three separate causes that had to be fixed and the six candidate
mechanisms ruled out by measurement, is under `OD-011`.

**Reproduction, for whoever takes it:** CI is far better at this than the local
soak -- roughly one run in six to eight against three in 7138 rounds on Fusion,
none in 7235, none in 11690. The Debian 13 interoperability job is where it
lands. Running the soak again is the least likely thing to work.

### P-2 — A gate that outlasts its budget cannot report its own failure

Covers `B-39`, and retrospectively explains `B-72`.

`qemu-core-os-rc` kills each step at its budget; the gates it runs have budgets
of their own, and `smoke_timeout` multiplies the RISC-V ones by four. Where the
gate's wait was longer than the step's budget, the aggregate killed it first and
printed `exited 124` with no architecture, no phase and no reason.

Two steps had it. `fragmentation` waited 720s for a RISC-V boot inside 360 --
that is B-39, whose only evidence is one run that took at least 3.7x its usual
time and could be recorded as nothing but a timeout. `nvme` waited 1440s inside
900, which is why B-72 read as an anonymous timeout for two runs and why raising
its budget worked: it gave the gate back the ability to name the row.

Fixed in both, and `check-aggregate-budgets.py` now fails on either -- exercised
in both directions before it was wired in. **Residual worth knowing:** that check
reads two spellings of a wait out of gate source and cannot see a wait built any
other way, which its own docstring states. **Recommended refactor:** gates should
derive their wait from the budget they are given rather than choosing one
independently, at which point the inversion becomes unrepresentable and the check
becomes unnecessary.

### P-3 — This Mac cannot see what the runner sees

Nine defects (`B-65` through `B-73`) were invisible here and immediate on Linux:
a firmware path defaulting to `/opt/homebrew` and nothing else, `dd bs=1m`,
mtools missing from a job, a job with no RISC-V emulator, small-data sections
clang 18 emits and clang 23 does not, an argument silently discarded, a make
target that built half a machine, a truncated number read mid-arrival, and an
MSI-X interrupt QEMU 8.2.2 never delivers.

**What worked:** `tests/linux-parity` reproduces in about a minute what used to
take a push and a wait. Better still, three of the nine now have static checks --
`check-riscv-firmware-paths.py`, `check-portable-dd.py`,
`check-aggregate-budgets.py` -- which catch the whole class without booting
anything.

**What did not work, and is a real limit:** the parity container is arm64 and the
runner is x86_64. It cannot build an x86-64 userland at all (picolibc asks gcc
for `-m64`), and running it under emulated amd64 produces timings slower than any
real runner, so it predicts nothing. Three gates were once "reproduced" failing
there and all three had died on that build, saying nothing about CI. Every run
now prints the platform it ran on.

**Recommended:** an x86_64 Linux host for parity. One already exists -- the Intel
qualification host in `Platform recommendations` -- and pointing
`tests/linux-parity` at it would close the gap that `nvme-budget.sh` currently
refuses to guess at.

## What ships, and how far along it is

Three bootable deliveries, and the engineering each one waits on. Status is
what has been demonstrated, not what has been designed: `PARTIAL` means some of
it runs and the rest has not been tried, and is never a way of saying nearly
done.

| # | Deliverable | Status | Where it actually stands |
|---:|---|---|---|
| D1 | USB image, every architecture | `NEEDS HARDWARE` | Off the active work list: what remains is a physical test, not an implementation. Build 6 ships three USB kits, one per architecture, each carrying a hybrid ISO 9660 and GPT disk for that machine, so `dd` should produce a bootable stick on all three. Everything except the stick is exercised: `make release-image-gate` boots each of those images as a disk across five environments, and the installed-disk gate boots the partition layout they rely on twice a run. The unproven step is a stick in a machine, and `write-usb.sh` itself has never been run against a real device. |
| D2 | Network boot for blank machines | `TESTING` | A blank machine boots over the network and installs itself. `make qemu-netboot-gate` runs four stages: firmware asks DHCP for a boot filename and fetches it over TFTP; a medium holding the loader and nothing else boots the same way; that machine installs onto a blank disk; and that disk boots alone to a login prompt with SSH listening. Only the last two are evidence of an install. **Remaining:** the same sequence on physical hardware. |
| D3 | Ready-to-run images per environment | `TESTING` | Eleven kits from `scripts/build-vm-packages.sh` and `scripts/build-boot-media.sh`: QEMU, USB and netboot for each of the three architectures, plus VMware Fusion and Virtualization.framework for AArch64, which is the only architecture those two run natively on this host. Each kit carries the image for its own machine -- since build 6 there is no shared image to carry, and a kit that contained another architecture's would be a kit that boots nothing. `make vm-package-gate` boots all five VM kits out of their own archives and `make boot-media-gate` boots all three shipped netboot binaries. Kits are release assets, not committed. **Remaining:** physical media (D1). |

## Engineering these wait on

| # | Work | Status | Where it actually stands |
|---:|---|---|---|
| E1 | Disk partitioning, formatting and install tooling | `TESTING` | Works end to end: a running XAIOS partitions a blank disk, formats an EFI System Partition, copies its own loader, kernel, initial filesystem and entropy seed across, and the result boots on its own to a login prompt with SSH up. `kernel/fs/fat.c` is a FAT16 writer with long-name support, chosen over FAT32 because Fusion silently boots nothing from FAT32. `make qemu-installed-disk-gate` boots the installed disk, which is the only evidence that counts -- everything before it is the installer describing its own work. **Remaining:** the same run on physical media (D1). |
| E2 | Instruction-cost metric | `TESTING` | Recorded, committed and checked. Under `-icount shift=0` the guest clock counts instructions, and `make qemu-instruction-cost-gate` compares three single-threaded figures against `tests/fixtures/instruction-cost-baseline.json`. The four-thread figure is printed and never pinned, because under `-icount` the virtual clock advances for every vCPU. **Not a performance measurement:** see the [benchmark contract](https://github.com/Pummelchen/XAIOS/blob/main/docs/BENCHMARK-CONTRACT.md). |
| E3 | NUMA correctness | `TESTING` | The self-test proved node-0 placement and never checked node 1, so an allocator ignoring its node argument would have passed. It now asserts placement inside node 1's range and that every CPU maps to exactly one node through both lookups, held by `make qemu-x86_64-numa-gate` across a two-node and a four-node machine. **Remaining:** physical NUMA hardware. |
| E4 | Multiqueue and RSS networking | `TESTING` | Per-queue driver state, every advertised pair allocated and polled round-robin so a busy pair cannot starve the others, and `VIRTIO_NET_CTRL_MQ_VQ_PAIRS_SET` sent only after the buffers exist. RSS is now negotiated and configured from the device's own config -- supported hash types, table length and key size read rather than assumed -- and steering is measured by `make qemu-rss-steering-gate` on a four-queue tap: sixteen buckets across four pairs spread `[64, 62, 66, 64]`, and a one-bucket control collapses to `[256, 0, 0, 0]` and fails the same assertion. Two defects were found by insisting on real counts: the hash key's byte 13 was zero, which pinned the low bit of every flow's hash so only even buckets were reachable (512 frames landed `[37, 0, 27, 0]` -- enough spread to pass a gate that asked for "more than one queue"), and `max_tx_vq` was sent as an index where the device reads a count, cutting a four-pair device to three with pair 3 set up, polled and unreachable. **Remaining:** the gate needs Linux, root and a multi-queue tap, so it runs on the Intel host only -- macOS offers the feature bits on both transports and has no multi-queue backend for either, and virtio-mmio multiqueue is unexercised anywhere. Hash report is offered but deliberately not negotiated: it grows every frame's header and forces software hashing for a per-packet value nothing here reads. |
| E5 | xaibootFS and xaiFS at scale | `TESTING` | xaibootFS v6 records a file as extents rather than a block list, which lifts both limits at once and makes a node smaller: 1 GiB per volume against v5's 4 MiB, 1024 nodes against 256, and a format limit of a gibibyte per file. The write path still refuses at 256 KiB, because it stages a whole file through a static buffer that was never raised with the format -- refusing is deliberate until it streams. Resident cost is about 850 KiB for 256 times the capacity. Which version a new volume gets follows the disk rather than a flag day. **Remaining:** scale evidence on physical storage. |
| E6 | Cluster data plane | `TESTING` | A sealed frame crosses a real network between two guests. `make qemu-cluster-two-node-gate` and `-three-node-gate` run join, partition, recovery and ownership, each phase on its own TCP connection so each machine has to be ready on its own schedule. `engine/src/cluster.c` had framing, sealing and peer state since it was written and had never opened a socket. **Remaining:** distributed execution, which waits on D-05. |
| E8 | WebTransport C99 port | `IN PROGRESS` | The TLS 1.3 client handshake is written, tested and merged: it completes a QUIC handshake against a server flight from an independent Python implementation, and 1487 host checks run in CI and under the sanitizers. `userspace/wt/` carries the key schedule, packet protection, framing and transcript, the message parsers, the signature check, the pinned-key policy and the state machine. **Remaining:** seven tasks -- `B-84` to `B-89` and `B-91` -- with `B-90`, `B-92` and `B-93` fixed, under [Current tasks](#current-tasks) -- the largest is that nothing on the target drives the handshake from a socket yet, and there is no QUIC connection runtime under it. |
| E7 | Storage throughput, caching and power-loss evidence | `TESTING` | The block path moved 512 bytes per virtqueue round trip -- `buffer_size` had always been validated and then ignored. Raising it to a mebibyte and reading each byte once rather than twice changed the figures by orders of magnitude, against a baseline rebuilt from the same tree rather than remembered. Power loss is real: `make qemu-power-loss-gate` replays QEMU's `blklogwrites` journal through `tools/xaios_write_log.py`, because no emulator option loses an acknowledged write on its own. **Remaining:** physical storage; emulator figures are not throughput evidence. |

## Delivery order

| Order | Workstream | Status | Current boundary / exit gate |
|---:|---|---|---|
| 1b | Physical Apple/ARM, Intel desktop, and Xeon qualification | `NOT STARTED` | Named hardware must pass firmware, device, durability, security, ISA-state, NUMA, soak, and benchmark contracts. |
| 1c | Disk partitioning, formatting and install tooling | `TESTING` | A tool set that partitions a disk, formats xaibootFS and xaiFS on it, and makes it bootable with XAIOS as the only operating system on the machine. The tooling exists and installs -- see `E1` for what it is. The gates that prove it, `make qemu-installed-disk-gate` and `make qemu-setup-gate`, began as AArch64 QEMU only, which is why this row read "x86-64 has no install evidence at all" for so long. **That is now three architecture-specific variants rather than one:** `qemu-riscv64-setup-gate` already existed, and `qemu-x86_64-installed-disk-gate` is new; a `qemu-riscv64-installed-disk-gate` target was listed beside it, but it could not run at all -- see `B-103` -- it took adding x86_64 to the disk builder, which refused the architecture outright, and a per-architecture profile in the gate. The installed-disk boots pass on x86-64 (kernel, ordinal, state partition found by type, fsck, four vCPUs, shell, systest, ESP readable, kernel on the ESP, and the second boot seeing what the first wrote). **That phase now runs on all three architectures.** It is the first time the row can say that: a running machine installing onto a blank disk is exercised on AArch64, x86-64 and RISC-V, and on x86-64 the disk XAIOS wrote was then booted on its own with an SSH server running on it. What held it back was that the storage-administration window was a device's *position in the test bench's disk order* -- see `B-113`, which also records how it was fixed and what the fallback needed before it was safe to scan for a device at all -- and, once that was fixed, a byte floor in the install check that only AArch64's ESP could satisfy, which is `B-114`. **Exit gate:** a blank disk partitioned, formatted and booted on all four environments and on one physical machine. |
| 2 | Qwen 3.8 support | `NOT STARTED` | Begins after physical platform qualification is accepted or explicitly deferred; official tokenizer, layer, logits, 32-step decode, session, and physical gates must pass. |
| 3 | Kimi K3 text support | `NOT STARTED` | Begins after Qwen unless reprioritized; KDA, Gated MLA, exact top-16 MoE, MXFP4, and token parity are mandatory. |
| 4 | Kimi K3 multimodal support | `NOT STARTED` | Separate vision preprocessing/tower/projection/position and golden image gates. |
| 5 | DeepSeek V4 Flash 0731 support | `BLOCKED` | The exact official release label and immutable source must be verified first. |

## Model support boundary

| Model or format | Progress | Support boundary | Completion gate |
|---|---|---|---|
| Qwen 3.8 | `NOT STARTED` | Roadmap target; no architecture adapter is claimed | Pin an immutable official configuration before tokenizer, tensor, layer, prefill-logit, decode, session, backend, and physical parity work. |
| Kimi K3 text | `NOT STARTED` | Interface only | KDA/MLA/MoE/MXFP4/operator and target-token parity on a real checkpoint. |
| Kimi K3 multimodal | `NOT STARTED` | Roadmap only | Separate official vision and multimodal golden acceptance. |
| DeepSeek V4 Flash 0731 | `BLOCKED` | Roadmap only | Verify exact official source before architecture work. |

## Platform recommendations

Only open ARM/Intel/platform recommendations remain here. The complete numbered
catalog stays in `docs/PLATFORM-SUPPORT.json`; no secondary page owns progress
status.

| # | Recommendation | Status | Evidence / remaining gate |
|---:|---|---|---|
| P-05 | Physical Apple NEON evidence | `NOT STARTED` | QEMU cannot satisfy this physical gate. |
| P-07 | SVE/SVE2 backend | `TESTING` | `make qemu-aarch64-sve2-gate` executes the SVE2 canary and preserves per-task Z/P/FFR across scheduling and interrupts. A packed SVE inference kernel exists -- `xaios_packed_gemv_sve`, runtime-length vectors with `svwhilelt` predicating the final partial vector rather than a hand-written tail -- and is verified against the scalar reference before use (`packed=verified against scalar` on `-cpu max,sve=on`). It is selected ahead of NEON because SVE's width is the machine's. **Remaining:** physical Apple silicon; nothing here is performance evidence. |
| P-15 | RISC-V (rv64gc) as a third architecture | `TESTING` | Functional parity on the QEMU `virt` board, in the release image, gated three ways: `qemu-riscv64-gate`, `qemu-riscv64-boot-media-gate` and `qemu-riscv64-matrix-gate` at 1, 2, 4 and 8 harts. Boots from a kernel handed to QEMU and from its own disk through EDK2 via the signed A/B slot, and the unified release ISO carries it. Sv48 and Sv39, ecall syscalls over a full trap frame, PLIC, PCI over ECAM, both virtio transports, xaiFS at `/models`, IPv6, the hosted C99 library and xapt. **Remaining:** hardware. See [[RISC-V\|RISC-V]]. |
| P-16 | RISC-V message-signalled interrupts | `TESTING` | Implemented and gated on QEMU; not qualified on hardware. `kernel/arch/riscv64/aia.c` drives APLIC and IMSIC, discovered from the device tree and selected at run time, so `-machine virt,aia=aplic-imsic` works and the default PLIC board is unchanged. The supervisor-level pair is identified by the interrupt cause an IMSIC raises (9, not 11) and by `msi-parent`, because the machine-level devices carry the same compatible strings. Held by `make qemu-riscv64-aia-gate`, which requires markers that can only be printed after something was delivered. **Remaining:** hardware. |
| P-14 | Physical Intel/Xeon evidence | `NOT STARTED` | Physical firmware, ISA, NUMA, storage, network, thermals, and sustained-load gates remain. |

## VMware Fusion ARM64 remaining work

The qualified Fusion boundary is Apple Silicon VMware Fusion 26H1 (26.0.0),
four vCPUs, E1000E, AHCI, DHCP IPv4, and public-key SSH/SFTP. The items below
are intentionally not implied by that passing profile.

| ID | Item | Status | Evidence / remaining gate |
|---|---|---|---|
| F-06 | Fusion release-version coverage | `DEFERRED` | Postponed deliberately; nothing external blocks it. Fusion 26H1 evidence is not a compatibility claim for earlier or later releases, x86_64 guests, or physical Apple hardware. |

### Resolved, kept for reference

| ID | Item | Closed by |
|---|---|---|
| F-03 | Fusion network feature qualification | The IPv6 half: `e1000e` programmed its receive control with an empty multicast table, so every multicast frame was discarded in the NIC and no router advertisement reached the stack -- IPv4 never noticed, because DHCP is broadcast. `make vmware-fusion-network-gate` passes on the LAN with a real lease, a global SLAAC address, ICMPv6, SSH on both families and an SFTP round trip over IPv6. Outbound SSH and SCP from the guest and `direct-tcpip` forwarding through it: `make vmware-fusion-outbound-gate`, against a disposable container on this host's LAN address rather than the operator's Mac account, each with a control that fires -- a key the guest never held refused, a forward to a closed port failing with the guest's own channel-open failure rather than a local refusal, and a one-byte-corrupted download caught by content, because the client reports success on it. One of those controls was inverted by `B-37`, which stopped the client asking for a passphrase that does not exist. And local DNSSEC, which this row called unstageable for a good reason that `B-33` then removed: the guest now answers `selftest` from the committed chain and `forged.selftest` -- same zone, same key, one bit flipped -- so the pair is its own control, and a bridged Fusion guest returns `10.53.0.7` and `dnssec-unverified` respectively. Loss and reordering are not claimed: the LAN is not a controlled link. |
| F-04 | Fusion snapshot and sustained-load qualification | `make vmware-fusion-snapshot-gate` states the guarantee as three properties and requires each: a snapshot is a point in time (both halves -- data before it survives a revert, data after it does not), a revert lands on a filesystem the guest trusts, and a suspend is not a power cut. Nine checks, taken with the guest powered off, which is a deliberate choice. The sustained-load half: one boot held under continuous 256 KiB SFTP round trips, **1658 rounds and 869 MB, with free memory moving by zero pages** -- early mean 487777 against late mean 487777 across 1658 samples. That is the assertion only a long run can make. The same run reproduced `B-43`, so `make vmware-fusion-load-soak` exits non-zero: it is a reproduction harness and reporting what it reproduced is its job, not a failure of this item. |
| F-01 | Fusion multi-vCPU startup | Fusion starts all four vCPUs; two earlier diagnoses were wrong, and both rested on measurements taken where the code under suspicion never ran. The real defect was ours: a secondary published `online = 1` with its MMU still off, so the boot CPU switched to real atomics while other live CPUs could not execute them. `cpu_online=4` on a passing smoke gate. |
| F-02 | VMXNET3 networking | The driver's doorbell and control registers were hand-placed at one address shared with AHCI. Both now come out of the device-window arena, and the card carries traffic end to end on Fusion 26.0.0 -- zero transmit timeouts where there were 284, and a real LAN lease. `make qemu-vmxnet3-gate` holds it on QEMU's implementation of the same device. The qualified profile stays E1000E by choice: `XAIOS_FUSION_NIC` defaults to it. |
| F-05 | Fusion entropy and production-credential boundary | Fusion 26H1 exposes neither `EFI_RNG_PROTOCOL` nor RNDR, so images use a unique local development seed. The engineering half is done: a development seed used to be indistinguishable from a hardware one in the boot record, and the kernel now says which it has. **The remaining half is not engineering** -- production needs an operator-approved entropy and key-provisioning design, and credentials. |

## Apple Virtualization.framework ARM64 remaining work

XAIOS boots to a login on this platform with xaibootFS on a durable volume,
DHCP IPv4, SLAAC IPv6, SSH and all four vCPUs online, and the Mac can ssh into
the guest over vmnet
through `platform/virtualization-framework/vmnet-helper`, which is the only route in: the built-in NAT
attachment delivers no host-initiated frame, and bridging needs an entitlement
V-03 also waits on. `make vz-gate` checks that boot and writes
`build/vz-gate.json`. It is a development target: the gate needs macOS on Apple
Silicon and a signed harness, so it cannot run in CI and its result is not
qualification evidence.

| ID | Item | Status | Evidence / remaining gate |
|---|---|---|---|
| V-02 | MSI-X delivery for virtio on PCI | `TESTING` | Exercised by attaching QEMU's virtio devices on PCI against a real translation service: every device on the bus receives a distinct vector. Three defects were fixed to get there -- one translation table shared by all devices, identifiers reissued to a device whose predecessor polls and never registers a handler, and an assertion on any BAR above 512 GiB. **Remaining:** physical ARM PCIe. Virtualization.framework has no ITS, so its queues stay polled. |
| V-03 | Globally routable IPv6 | `TESTING` | The NAT attachment advertises only the unique-local prefix `fd4a:25c::/64`. A bridged attachment needs the `com.apple.vm.networking` entitlement, which Apple issues only with a provisioning profile; ad-hoc signing cannot provide it. **The blocker is now known to be only the entitlement:** the guest half was never proven while `e1000e` discarded every multicast frame, and with that fixed a bridged Fusion guest on the same LAN autoconfigures a routable address from a real advertisement (F-03). |
| V-04 | Multi-vCPU qualification | `IN PROGRESS` | Secondaries genuinely run: PSCI starts them with translation off, where exclusives are unsupported, so the atomic each one used to announce itself aborted and every boot reported `online cpus=1/4`. With that window made coherent, boots come up 1/1, 4/4 and 8/8, and ten consecutive eight-vCPU boots produce byte-identical `smptest` signatures. `make vz-gate` requires 4/4 and `make vz-stress-gate` covers sustained load. **Remaining:** this host has eight cores against a 128-256 core target and the platform offers no interrupt-affinity control. |
| V-06 | Graphical console | `TESTING` | Implemented. The platform's GOP reports `PixelBltOnly` with a zero framebuffer base, so firmware leaves nothing to draw into, but the display device is on the PCI bus regardless and `kernel/dev/virtio/virtio_gpu.c` claims it: resource created, framebuffer attached page by page, scanout pointed at it, transfer-and-flush after each draw. `boot_ui` accumulates a dirty bounding box and presents only that region. `make vz-framebuffer-gate` captures the guest's display through ScreenCaptureKit and requires the boot bar; it needs Screen Recording permission, cached per process at launch. |
| C-01 | Shared kernel state under genuine parallelism | `TESTING` | Addressed per subsystem, because the right fix differed per file. The network stack, service records and CPU-AI runtime took reentrant guards on their syscall-reachable entry points -- reentrant because ten of the network stack's exported functions call other exported ones. The resolver shares the network guard rather than holding its own; a separate one was a lock-order inversion, caught before it could bite. `security.c` and `agent_protocol.c` hold no tables, so their counters became atomics. **Remaining:** sustained multi-core evidence on a machine with more cores than this one. |
| C-02 | `network_stack.c` state escapes its module | `TESTING` | The obstacle to finer locking was never the lock: tables handed out their rows. The socket map returned interior pointers across the syscall boundary, where callers dereferenced them after the guard was released -- a live race once secondary CPUs ran -- and now returns a copy. No exported function returns a pointer into module state. Splitting the tables further was left open pending C-03, which then measured it as worthless. |
| C-03 | The socket path scales; the finding that said otherwise was noise | `TESTING` | Socket bind/close costs about 40 us per operation at four threads and the same at eight, three runs per configuration, so throughput rises with cores. Giving the listener registry its own guard made no measurable difference and that split was removed. The 437 us that opened this item was recorded while `vmnet-helper` held a core busy (B-03) -- contention with the host, not serialisation in the stack. **Nothing here is performance evidence:** see the [benchmark contract](https://github.com/Pummelchen/XAIOS/blob/main/docs/BENCHMARK-CONTRACT.md). |

## Core OS, network, and SSH phases

| ID | Item | Status | Evidence / remaining boundary |
|---|---|---|---|
| N-F3P | Physical SSH/network security qualification | `IN PROGRESS` | Consolidated QEMU network/SSH readiness evidence is available through `make qemu-qualification-readiness`; physical lossy-link, sustained-load, side-channel analysis, and independent SSH/cryptography review remain open. QEMU evidence cannot close this item. |

## Storage phases

| Phase | Status | Evidence / remaining gate |
|---|---|---|
| S-11P Physical production NVMe qualification | `IN PROGRESS` | Consolidated QEMU NVMe and crash-recovery evidence is available through `make qemu-qualification-readiness`; named physical devices must still pass queue scaling, interrupt affinity, FUA/flush/discard semantics, reset recovery, power-loss durability, sustained-load, and performance gates. QEMU evidence cannot close this item. |
| S-12 Production xaiFS trust-root and signing-key custody | `BLOCKED` | Offline trusted-replica payload repair is implemented and QEMU/hosted-tested. Production trust-root enrollment, private-key custody, replica authorization, and rotation decisions require named operators and deployment credentials. |

## Distributed AI server phases

| Phase | Status | Exit gate |
|---|---|---|
| D-05 Real local inference | `NOT STARTED` | Real Qwen correctness, typed state, scheduling, cancellation, backpressure, and metrics. |
| D-06 Authenticated cluster control | `TESTING` | The QEMU-testable tranche is done. `make qemu-cluster-two-node-gate` runs join, partition, recovery and ownership across two guests; `-three-node-gate` runs three that heartbeat every 500 ms and kills one emulator outright, so the survivors must notice from silence rather than from a `LEAVE`; `-partition-gate` breaks links instead of machines through `tests/scripts/cluster_fault_relay.py`, so one-way and two-way partitions can be arranged while every machine keeps running. Quorum is what could not exist at two nodes: a minority reports `owners=withheld` rather than deciding, because one expert with two owners is work done twice that nobody reconciles. Each gate carries its own control -- a healthy window longer than the detector's deadline that must declare no death, and a skip-kill mode in which every failure-detection check must go red. **Remaining:** execution across nodes, which waits on D-05. |
| D-07 Distributed placement/execution | `IN PROGRESS` | Hosted tests cover deterministic expert ownership, grouped routing, simulated node-loss rerouting, and stable node/expert reduction. End-to-end distributed activation execution depends on D-05 and D-06 and cannot be closed by hosted placement tests alone. |
| D-08 Benchmarks/diagnostics | `IN PROGRESS` | QEMU benchmark telemetry and a hashed qualification-readiness report are implemented; physical metadata-rich NUMA, bandwidth, PMU, thermal, storage, network, and redacted support-bundle evidence remain. |
| D-09 Production inference service | `NOT STARTED` | Authenticated API, streaming, cancellation, saturation, loss, and long-lived tests. |
| D-10 Support qualification/cleanup | `IN PROGRESS` | Documentation contracts and the consolidated QEMU qualification-readiness gate exist; physical, model, cluster, thermal, PMU, and durability qualifications remain. |

## Qwen 3.8 implementation

| Item | Status | Acceptance |
|---|---|---|
| Pin immutable official config/tokenizer/SafeTensors and parity corpus | `NOT STARTED` | Hashes and source revisions recorded. |
| Streaming SafeTensors/config/tokenizer importer | `NOT STARTED` | Bounded RSS and deterministic package output. |
| Package-owned tokenizer | `NOT STARTED` | Trusted tokenizer IDs match. |
| Official architecture probe and ordered configuration-derived layer plan | `NOT STARTED` | Unknown fields fail closed. |
| Scalar embedding, RMSNorm, and first projection | `NOT STARTED` | Python reference parity. |
| Every configured attention/recurrent/convolution operator, position encoding, FFN, residual, norm/head | `NOT STARTED` | Complete-layer and prefill-logit parity. |
| Separate prefill/decode plans and real per-layer state | `NOT STARTED` | State and reload continuity. |
| 32-step deterministic decode | `NOT STARTED` | Exact trusted continuation within documented tolerance. |
| Physical AVX2 and tiled prefill/verification kernels | `NOT STARTED` | Physical differential and performance artifacts. |
| Native model-executing macOS process and optional Metal | `NOT STARTED` | Real model plan runs end to end; CPU fallback remains authoritative. |
| AVX-512/VNNI/AMX, SVE/SVE2, persistent worker gangs, NUMA autotuning | `NOT STARTED` | Capability canaries, scalar differential, and physical evidence. |
| Typed paged state, prefix COW, ragged batching, exact speculation | `NOT STARTED` | Target-only and speculative deterministic outputs match. |

## Later model work

| Item | Status | Acceptance |
|---|---|---|
| Separate `kimi_k3` adapter from immutable official config | `NOT STARTED` | Config/tensor roles reject unsupported fields. |
| K3 KDA, Gated MLA, AttnRes, exact top-16 routing, shared experts, SiTU, MXFP4 | `NOT STARTED` | Scalar operator/router/expert parity. |
| K3 independently addressable expert shards and async residency/prefetch | `NOT STARTED` | Authoritative routing is unchanged by prediction. |
| Real K3 text checkpoint | `NOT STARTED` | Tokenizer/operator/router/target-token and production-width physical gates. |
| K3 MoonViT-V2 and multimodal pipeline | `NOT STARTED` | Separate golden image/text cases. |
| DeepSeek V4 Flash 0731 source verification | `BLOCKED` | Maintainer-approved immutable official source. |
| DeepSeek adapter and parity suite | `BLOCKED` | Depends on verified source. |
| Multi-terabyte sparse allocators and large pages | `IN PROGRESS` | Hosted model packages represent sparse offsets above 100 GiB; both QEMU targets cover 2 MiB mappings and x86_64 covers a 1 GiB leaf plus targeted SMP TLB invalidation. Physical capacity and performance qualification remain. |
| SRAT/SLIT/HMAT placement policy and local/remote byte telemetry | `IN PROGRESS` | The two-node x86_64 QEMU gate validates SRAT/SLIT/HMAT parsing, usable-memory intersection, deterministic preferred-node policy, node-local allocation, and local/remote byte accounting. Physical locality/performance qualification remains. |
| AI Cell/secondary-CPU real inference dispatch | `NOT STARTED` | Real model work executes on leased workers. |
| NUMA/machine expert ownership and stable failure-aware reduction | `IN PROGRESS` | Hosted tests validate deterministic owner selection, grouping, simulated owner failure, and stable reduction. Real NUMA/machine transport, remote activation execution, and multi-QEMU exactness remain. |

## Open decisions

| ID | Decision | Status | Required before |
|---|---|---|---|
| OD-001 | Select first physical Apple/ARM target and firmware/storage/NIC boundary | `NOT STARTED` | Physical ARM support. |
| OD-002 | Select representative AVX2 Intel desktop and hybrid-core/device baseline | `NOT STARTED` | Intel desktop support. |
| OD-003 | Select Xeon generation, sockets/NUMA, memory, NIC, and NVMe | `NOT STARTED` | Xeon support. |
| OD-004 | Provision production update/xaiFS trust roots and define custody/authorization procedures | `BLOCKED` | Rotation, revocation, offline recovery, and interrupted-activation rollback are implemented; private operator keys and process are required before untrusted deployment. |
| OD-005 | Define SSH fleet limits, identity, audit retention, lockout, recovery | `NOT STARTED` | Production SSH exposure. |
| OD-006 | Define supported NVMe/FUA/flush/discard/repair/power-loss contract | `NOT STARTED` | Physical persistent deployment. |
| OD-007 | Pin official immutable Qwen 3.8 fixtures | `NOT STARTED` | Qwen implementation. |
| OD-008 | Pin official Kimi/DeepSeek sources | `BLOCKED` | Corresponding adapters; DeepSeek exact label is unresolved. |
| OD-009 | Select expert-parallel interconnect and failure/ownership model | `NOT STARTED` | Cluster inference. |
| OD-010 | Define names, quality reporting, telemetry, and acceptance for opt-in approximate modes | `NOT STARTED` | Any approximate mode. |

**This is the decision analysis as it was written, and the outcome is recorded
under `OD-011` below: the timer was chosen, it is landed, and three of the costs
named here turned out to be removable rather than payable. It is kept because
the analysis is what made the third cause findable.**

**OD-011 is the decision on this list most likely to be under-costed, so the two
options are set out with what each actually needs.** The timer option is not a
small change. There is no interrupt-mask primitive in the tree to build on:
`kernel/include/xaios/arch_cpu.h` carries barriers, `relax`, `notify`, `wait`, a
counter and a stack pointer, and nothing that saves and disables interrupts, and
no `interrupts_save`-shaped helper exists anywhere in `kernel/`. So it needs one
per architecture -- the instructions are already used in isolation, aarch64's
`msr daifset, #2` in `gic.c` and riscv64's `csrrc sstatus` in `aia.c` -- and then
a change to `xaios_reentrant_lock`'s contract with an audit of its four users
(service, network, the resolver and the CPU-AI runtime, whose lock order the
header fixes), and then preemption, which the scheduler does not provide today
because it runs one thread to completion per worker. It also puts the whole poll
path -- the device drain and the packet processing, whose cost is set by how much
traffic arrived -- inside interrupt context, where its latency is everyone's.

The thread option is the opposite trade. A kernel thread on a CPU of its own
needs no lock change, no preemption and no new primitive, and it keeps the work
in thread context where it can be bounded; it costs one of the three worker CPUs
permanently, which is a quarter of a four-CPU machine.

**A third shape exists that neither option in the row names, and it may be the
cheapest correct one:** the timer interrupt drains the device into a ring and
sends only what a peer needs to keep waiting, leaving the rest of the stack to
the loop. That bounds the interrupt's work and removes the total-outage property
without a dedicated CPU, at the cost of splitting the poll path in two and having
to say which half may do what.

**And a measurement that changes what the timer option has to include.** The
first piece of it is now in the tree: `arch_cpu.h` carries
`xaios_interrupts_disable` / `xaios_interrupts_restore` /
`xaios_interrupts_enabled` for all three architectures, built on the
instructions each already used in isolation, with a self-test beside the other
boot self-tests. **That self-test reports `inconclusive`, not `passed`:**
`before=0 masked=0 after=0` at the end of boot, just before sshd starts, so the
enabling direction is never exercised because there is nothing enabled to mask.
The first version of it asserted `before == 1` and halted the machine, which is
how the masked state was found rather than assumed. **The boot CPU runs with
interrupts masked**, and the only unmask paths in the tree are
`gic_secondary_init`, which `smp.c` runs on the secondary CPUs, and
`kernel/sched/context.S`. A timer that polls the network therefore cannot be
built on "add a timer" alone: it has to establish where and why the boot CPU
enables interrupts, which is the same question as the preemption this row
already names. The primitive is landed and the self-test is honest about what it
did not test, because a test that reports a round trip it did not perform is
exactly the thing this page exists to prevent.

**The execution model is now established rather than assumed, and it is not
uniform across CPUs.** Both EL0 entry paths in `kernel/arch/aarch64/entry.S` set
`spsr_el1` to zero, so userspace runs with interrupts enabled; the boot CPU runs
kernel code with them masked; and the secondary CPUs unmask in `smp.c` and then
run kernel work interruptible, having first masked the scheduler timer on
purpose -- *"Keep the local scheduler timer masked until this CPU owns a
preemptible userspace run queue"*. That line is the shape of the whole problem:
the machine **already** takes interrupts in kernel context on three of its four
CPUs, so a network timer would fire there and call `network_poll_tick` from a
handler, which is exactly what the reentrant lock forbade.

**So stage two landed with stage one: the guard now masks interrupts for as long
as it is held and restores the state on release.** The prohibition is gone
because the condition it guarded against can no longer happen, the same guard
may now be taken from a handler, and the saved state is kept per guard because
the depth counter already says that only the outermost release undoes the
outermost acquire -- read before the guard is released, so a CPU that takes it
next cannot overwrite the state being restored. Verified by `make compile-check`
in every configuration and a full `make qemu-smoke` boot that passes its lock,
network and sshd markers with no halt. **What is left for the timer itself** is
deciding where the network tick is allowed to run, since the boot CPU takes no
kernel-context interrupts today while the secondaries already do.

**Stage three-a landed, and it is a race fix that was already there.** Putting
the tick into interrupt context needs `network_poll_tick` to be safe to
re-enter, and reading it for that found a live defect: `dns_transport_tick` was
called *after* `network_unlock()`, so it mutated the pending query and drove the
TCP flow carrying it without the guard that `dns.c`'s own comment says the
resolver shares -- racing every `dns_resolve_address` on another CPU, and
re-entering one on this CPU as soon as a tick could arrive in interrupt
context. It is now inside the guard, which is where that comment always said it
belonged. Verified by `make compile-check` in every configuration and a full
`make qemu-smoke` boot on node4 with the DNS and network markers passing.

**Done, and the two sessions together are the record of why it took three
passes.** `OD-011` chose the timer; stage three-b landed it: one secondary CPU
arms its own timer after `kmain` stops the shared periodic tick, and on that CPU
the timer interrupt does not tick the scheduler, which stays masked there exactly
as the port left it. The tick wakes the CPU; the idle loop polls the stack in
thread context through `network_poll_tick_from_carrier()`, which is the whole
poll including the power path -- safe there because thread context is where
`operations_tick()` quiesces storage, and it can stop the machine.
**No CPU gains or loses a preemption.**

**What the first attempt got wrong, because it is the useful part.** It read
"the tick stops when the carrier is given work", which was coincidence: in the
run instrumented to test that, no thread was dispatched to the carrier and the
tick still died. Three separate causes had to be removed, and only the third was
the timer:

1. **The hook point never ran.** `kmain` clears the *global* period before sshd
   starts, so no CPU takes a periodic timer interrupt afterwards and a poll in
   `intid == TIMER_PPI_INTID` is unreachable code. The tick has to be armed, not
   merely hooked.
2. **The idle loop could not take an interrupt at all.** `vector_entry` masks
   `DAIF` on every trap, and a secondary's loop could be re-entered with `I` set
   -- measured spinning with a pending timer *visible* in its CPU interface
   (`hppir1=27`) and `DAIF=0x3c0`, so it never took another one. A CPU that
   cannot take an interrupt cannot be woken by one either, which is how the
   scheduler moves work between CPUs. The loop now clears `I` every turn instead
   of once before it, and `wfe` sleeps again rather than spinning: the same run
   had turned 134 million times. **This is a defect in its own right and has
   nothing to do with the network.**
3. **The keepalive starved the timer it was keeping alive.** Re-pointing the
   comparator on every idle turn postpones the deadline indefinitely when the
   loop iterates faster than the period, which is exactly what a pending
   interrupt makes it do. The count froze at 18, then at 50. The keepalive now
   re-arms **only when the tick is not running** -- which is what it is for: a
   mask taken while this CPU ran a task -- and the count then tracked the loop
   one-for-one to 8000.
4. **RISC-V could not take the trap at all, so the poll moved out of the
   handler.** Polling from the timer handler worked on AArch64 and x86-64 and
   corrupted a trap frame on RISC-V (`/bin/c99-thread-context` returned to
   program counter zero), because that port's trap entry cannot yet take
   arbitrary kernel-context traps. The tick now only *wakes* the carrier and the
   poll runs from its idle loop, in thread context: no port has to be more
   interrupt-safe than it already is, none is special-cased, and the power path
   stays out of handlers. RISC-V was measured carrying it -- `network tick armed
   on cpu=2`, `tick=783` rising to `3625` polls, worst gap 15 ms.

Ruled out along the way, each by measurement rather than argument, so the next
person does not repeat them: a mask by this kernel (`timer_mask_local` is never
reached on the carrier); `g_cpu_states` being reused (the bootstrap region is
reserved from the NUMA allocator); the redistributor being asleep
(`GICR_WAKER=0x0`, both `ProcessorSleep` and `ChildrenAsleep`); PPI 27 being
disabled (`GICR_ISENABLER0=0x08000002`); the interrupt stuck active
(`GICR_ISACTIVER0=0`, and `ICC_CTLR_EL1=0x8c00` means `EOImode=0`, so the
unconditional `ICC_EOIR1_EL1` does deactivate); and the priority mask
(`ICC_PMR_EL1=0xf8` admits the timer's `0xa0`).

**Verified, and the numbers are the claim.** `make compile-check` clean on
aarch64, x86_64 and riscv64; `make qemu-smoke` passes; and
`make qemu-network-poll-cadence-gate` reports **24 ms idle, 25 ms across three
256 KiB SFTP round trips and 29 ms after ninety rejected connections, with 2890
of those polls taken from the timer interrupt** -- against 55-93 ms idle and
164-297 ms under load before. The gate's assertion that an announced tick must
actually advance the interrupt count is what caught causes 2 and 3, and it is
green. `make qemu-x86_64-smoke` and `make qemu-riscv64-smoke` also pass, and the cadence gate reports the tick firing on AArch64 and RISC-V.

**One latent deviation found on the way is now fixed.** Neither `gic_init` nor
`gic_secondary_init` waited for `ChildrenAsleep` to clear after clearing
`ProcessorSleep`, which the GICv3 specification requires before the rest of the
redistributor is used. Both paths omitted it, and neither was this defect -- the
boot CPU's timer works for thousands of ticks regardless -- but a mandatory
sequence was not being performed, and it had gone missing in two places at once
because the same three lines were written out twice. It is one
`wake_redistributor()` now, which polls until the redistributor reports itself
awake and is bounded rather than open-ended: a frame that never wakes logs
`gic: cpu<N> redistributor never reported awake` and lets the probe that follows
fail visibly, instead of hanging the machine with no output at all. Verified by
`make qemu-smoke`: the boot completes with no such line, so both paths take the
poll and pass it.
is a choice about the machine's execution model rather than a refactor to make
quietly.
### Resolved, kept for reference

| ID | Decision | Closed by |
|---|---|---|
| OD-011 | Choose how the network is polled when sshd's loop is busy | **The timer**, and it is landed rather than chosen, on **all three architectures**. One secondary CPU arms its own timer after `kmain` stops the shared periodic tick, and on that CPU the timer interrupt does not tick the scheduler, which stays masked there exactly as the port left it. **The tick's whole job is to wake that CPU**; the poll runs from its idle loop, in thread context, through `network_poll_tick_from_carrier()`. No CPU gains or loses a preemption, and the dedicated-thread cost of one of three worker CPUs was not paid. **Measured: the worst gap between polls fell from 55-93 ms idle and 164-297 ms under load to 24 ms idle and 29 ms under load**, with thousands of a gate run's polls taken by the CPU carrying the tick. Both objections that made this look impossible were removed rather than argued with: the stack guard now masks interrupts while it is held, and the poll was never handler work to begin with once it moved to the loop -- `operations_tick()` quiesces storage and can stop the machine, and it belongs in thread context. Three causes had to be fixed before it held -- an unreachable hook point, a secondary idle loop that could not take an interrupt, and a keepalive that starved the timer it was keeping -- and the detail is under `P-1`. **All three ports carry it, which took one further correction.** The obvious design polls from the timer handler; it worked on AArch64 and x86-64, and on RISC-V it corrupted a trap frame -- `/bin/c99-thread-context` returned to program counter zero, because that port's trap entry is deliberately minimal and cannot yet take arbitrary kernel-context traps. Polling from the idle loop instead -- the tick wakes the CPU, the loop polls in thread context -- needs no port to be more interrupt-safe than it already is, so all three carry the same mechanism with no port special-cased, and the power path stays out of handlers as a side effect. RISC-V was then measured carrying it: `network tick armed on cpu=2`, `tick=783` rising to `3625` polls, worst gap 15 ms. |

## Risk register

Risk status `TESTING` means mitigations exist but the risk remains open and is
checked continuously.

| ID | Risk | Status | Mitigation / closure gate |
|---|---|---|---|
| R-001 | QEMU timing presented as hardware performance | `TESTING` | Evidence vocabulary and benchmark contract; close only with continued claim audits. |
| R-002 | Documentation drift | `TESTING` | One tracker, `make docs-check`, and live-Wiki parity. `check-doc-freshness.py` now also fails the build on the two claims that expire quietly: an evidence commit of ours that has fallen far behind `HEAD` without saying so, and a review date on a page that git shows was edited afterwards. Three such claims were found expired in a single session -- a page saying a target had no automated gate months after two were added, a stale review date, and an evidence commit quoted as current from a hundred commits back -- so the residual risk is prose that is wrong in ways no pattern can see. |
| R-004 | Unreviewed SSH exposure | `TESTING` | Passwords off by default, bounded limits, OpenSSH/FreeBSD gates; independent review remains. |
| R-005 | Fixture keys used as production trust | `TESTING` | Fixtures are labeled; OD-004 blocks production trust. |
| R-006 | Storage durability inferred from sparse/QEMU tests | `TESTING` | Passing emulated async-NVMe and crash-recovery gates remain separate from physical S-11P and trust/repair S-12; only physical evidence can establish durability. |
| R-007 | Parser arithmetic or ownership error | `TESTING` | Checked arithmetic, malformed tests, sanitizers, immutable readers, fuzzing. |
| R-008 | Interfaces advertised as model support | `TESTING` | Separate progress and support-boundary columns plus golden gates. |
| R-009 | SIMD selected from CPUID alone | `TESTING` | OS-state checks, known-answer canaries, and scalar differential tests. |
| R-010 | Bounded fixture limits treated as server-scale targets | `TESTING` | Runtime-sized CPU/NUMA structures and explicit remaining bounded stores. |
| R-012 | Repository Wiki diverges from live Wiki | `TESTING` | Versioned Wiki source in `wiki/`, `make wiki-parity-check` reading the published pages back and comparing them byte for byte, and the layout and link gates in `docs-check`. **The middle one was claimed for months and did not exist** — every check read the in-repo copy, and `publish-wiki` pushed without reading anything back, so the risk was recorded as mitigated while nothing mitigated it. It was not theoretical: see `B-101`. The parity check is deliberately outside `docs-check`, which runs inside `qemu-core-os-rc` under a 120-second budget and must work with no network. **The mitigation has now been exercised in both directions on the live Wiki**, which is the part R-012 never had: against the state as found it failed naming the one page that had gone backwards (`Project-Tracker.md`, 186,340 bytes against 179,190, first difference at line 186), and after the publish fix it passes with all 26 pages byte-identical to the pushed tree -- the tracker restored from 179,190 bytes to 193,568, and two consecutive pushes both published from the correct commit. **Untested by that:** a divergence introduced by a publish run that fails, which the red run reports rather than this check, and a divergence older than the last push, which `publish-wiki` would overwrite without this check ever seeing it. |
| R-013 | A static-analysis finding is published and never triaged | `TESTING` | CodeQL runs on every push and its alerts are a backlog unless someone closes them. Thirty were open and every one has now been read: ten were real kernel defects (`B-95`), ten are BearSSL's DES, which is vendored, unmodified, and linked rather than called (`B-96`), and the remaining ten are test tooling whose weak fixture key, ECB against RFC 9001's own vectors, public per-run passphrase and QEMU-facing listener are the point of the gate rather than a defect in it. **The rule is that a finding is fixed, tracked, or dismissed with a reason on the alert -- not left open.** `check-code-scanning-contract.py` gates the workflow's permissions and job elevations, not its findings, so nothing enforces this but the practice. |

## Evidence gates

The default status-changing evidence set is documented in
[[Testing XAIOS|Testing-XAIOS]]. At minimum, source changes require the smallest
relevant compile/hosted/QEMU gates; documentation changes require layout,
status, JSON, link, and live-Wiki checks. A failed required gate changes the
affected item to `FAILED` until a passing rerun is recorded.

GitHub issues and milestones may provide discussion and execution history, but
their descriptive status must link back here rather than becoming another
independent tracker.
